# Z-MAX 标准容器框架 — 锁定依赖 (2026-08-08)
# 基线: transformers 5.5.4 + torch 2.11.0+cu128 (本地 4060 全模型验证组合 — 勿随意升级)
# 🐛 踩坑记录 (2026-08-08):
#   transformers 4.44.2 缺 qwen 相关模块 (SmolVLA-LEW 已移除 Qwen 导入, 但 5.x 才是验证版本)
#   transformers 4.49-4.51 缺 torch_compilable_check (eo1 引用) → import 失败
#   SmolVLA 不需要 qwen2_5_vl (modeling_smolvla_lew.py 已移除 Qwen 导入)
#   最终组合: transformers 5.5.4 + torch 2.11.0+cu128 (本地 smolvla_peg_long2 训练验证通过)
# 用法: pip install -r requirements.lock  (容器内由 Dockerfile 调用)

# ---- 核心 ML 栈 (锁定版本 — 全 7 模型可训的稳定组合) ----
transformers==5.5.4
torch==2.11.0
torchvision==0.26.0
accelerate==1.14.0
safetensors==0.8.0
tokenizers==0.21.4
sentencepiece==0.2.0
protobuf>=4.25,<6
datasets>=3.0,<6
av>=13.0,<15
opencv-python>=4.10,<5
numpy>=1.26,<2.2

# ---- lerobot 训练/评测依赖 ----
tensorboard>=2.17
termcolor
rich>=13
hydra-core>=1.3
omegaconf>=2.3
einops>=0.8
timm>=1.0
tqdm>=4.66
pyyaml>=6.0
huggingface-hub>=0.24,<0.37

# ---- 视频/图像工具 ----
imageio>=2.34
imageio-ffmpeg>=0.5
matplotlib>=3.9
Pillow>=10.3
