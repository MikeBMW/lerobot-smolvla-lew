# Crash Recovery Session — 2026-07-28

## Context

Hermes Agent system crash on WSL2 xspace. All persistent memory, `.env` credentials, conda environments, and local git history were lost. The user reinstalled Hermes from scratch.

## Repositories Restored

| Repo | URL | Local Path |
|---|---|---|
| lerobot-smolvla-lew | github.com/MikeBMW/lerobot-smolvla-lew.git | ~/lerobot-smolvla-lew/ |
| zmax-website | github.com/MikeBMW/zmax-website.git | ~/zmax-website/ |

Both cloned with `--depth 1`. Git auth restored with credential helper.

## Memory Recovery

Memory was preserved in the git repo at `docs/memory/`:

| File | Content |
|---|---|
| `xspace.md` | Agent's self-archive: identity, hardware, project context, skills inventory, user profile, collaboration protocol |
| `xiaofang.md` | Partner agent (小芳/Mac M1) reference profile |
| `sync.md` | Shared team memory: credentials, network topology, design specs, team roles, git strategy |
| `shared-memory.md` | Team structure table, repo aliases, server IPs, product names, naming conventions |
| `task-board.md` | Three-person project board: tasks per agent, git status |
| `hermes-jingjing.md.archived` | Prior agent archive (user calls me 静静) |

## Credentials Restored (from user)

- GitHub: `[REDACTED:github_token]` (MikeBMW)
- Feishu App ID: `cli_a87851ffe46b500d`
- Feishu App Secret: `t7cCB8TV8kShW7D0hN3Txg3fMuEuu8jt`

## Gateway State

- Feishu gateway running, WebSocket connected
- Bot `xspace` is in one group: `dataworld` (oc_c0b4048546145c5c581ddd1a9e8f565d)
- Bot is NOT in the "静界" group — user needs to add it manually
- No incoming Feishu messages have been received since gateway started

## Key Learning: Memory in Repo as Crash Backup

The project had a `docs/memory/` directory explicitly designed as a crash backup. Each agent dumped their full state there. This was the single most valuable recovery asset — without it, all project context would have been lost.

## Version Iteration

v1.0.4 → v1.0.5, committed as:
```
release: Z-MAX v1.0.5 — 系统重建恢复 · 2026-07-28
```
Pushed to `main` at 46ff55d.
