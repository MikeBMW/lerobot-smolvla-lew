# 全量同步 (2026-09-19 06:3x) — 我这边 ↔ 飞书端

> 两端共用同一仓库/工作树。本文件为**我这边**(CLI 会话, 2D→3D 感知方向)给对方(飞书端, L2/L3/L4+矩阵方向)的信息同步,
> 也作为统一待办清单。凡是有数字的都是本机实测, 出处写在括号里。

## 一、我这边: 2D 检测 → 3D 边界框 (老倪 09-18 晚交办)

**已交付 (代码在真链路, 非文档)**
- `src/lerobot/policies/yolo_3d/box3d_solver.py`: `predict_box3d` 5 档
  (`held` / `mono_pose` / `mono_scalar` / `fk_only` 诚实降级 / `no_3d` 拒绝); `fit(use_rrel, use_scale)`;
  **内参 K 约束模式** (只解手眼 rvec3+t3, 米制锚定); `calibrate_size` / `calibrate_rrel`; 前提自证
  `held_selfcheck` (FK 反投影框 vs 检测框 IoU)。
- `tools/box3d_live_box.py`: 真机实时 (同刻配对: 旁路同一行 tcp+quat+image.path) → YOLO 在役权重 → 3D 边界框;
  `--loop --collect` 边干边攒 + 自动拟合 + 前提自证才落盘 `models/box3d_state.json`。
- 配套: `box3d_box3d_selftest.py`(3D 框自检) · `box3d_identifiability_experiment.py`(可辨识性 8 臂) ·
  `box3d_fitpath_rehearsal.py`(现场动作前彩排, 含反例).
- `tools/calib_fetch_realsense_intrinsics.py` + `ros_camera_info_probe.py` + `ros_depth_hist_probe.py` +
  `orin_d405_factory_calib.py` (只读探针) → `models/real_cam_calib.json` **K 已填**。

**关键实测数字**
- 真机彩色内参 (出厂标定, `/realsense/color/camera_info`, 只读订阅): fx=394.06 fy=393.47 cx=318.44 cy=238.66
  @640×480 —— ⚠️ **不是仿真那套 fx=610**。已落 `models/real_cam_calib.json` (`ready=true`, `K_src` 记来源)。
- 可辨识性 (160 训练/40 留出, 真值只打分): K 已知解 手眼+off+R_rel → **中心 3.71mm / 8 角点 3.98mm / 姿态 3.1° / IoU 0.93**;
  不估 R_rel → 偏 **77mm**; 无 K 且尺寸自由 → 偏 **65mm**(退化); off 假设为 0 → 偏 90mm。
- 彩排 (走现场同一函数, 真 K): 正例 rms 0.86px、前提自证 IoU 0.874 → 落盘 → 留出 40 帧 **8 角点 5.23mm**;
  反例 (模块没被夹) IoU 0.226 → **拒绝落盘**。
- 零回退: 原 `box3d_solver_selftest.py` 与改造前基线**逐位一致** (rms 1.452px / 3D 6.91mm / IoU 0.835 / mono 24.68mm)。
- 真机实时现状: YOLO 单类 `peg` conf 0.70~0.73, 框 ~53×169px; 机器人静止 4 小时 (TCP 极差 0.0000m) →
  位姿多样性不过 → 解算器正确地拒绝拟合 (不是 bug)。

**我这边还缺 (需现场/人)**
1. 拖动模式下把模块(在爪上)走 **≥10 个不同位姿** → 自动标定落盘; 哨兵 cron `3cae27b292b6`(每 15min)保活采集 + 落盘只报一次。
2. `plane_z` (台面高度) 量一次 —— 只影响"模块躺台面"的 `mono_pose` 档。
3. **相机安装方式现场确认** (工装固定 vs 腕上): 决定 `held` 档公式能不能长期成立 (腕上要改成工具系固连)。
4. 画布 `zmax-studio` 需重启才吃到新节点代码 (📐 节点已输出 8 角点/尺寸/IoU/σ)。

## 二、复核飞书端两条: F08 真因与 F05 的并发覆盖风险

### F08 ❌ 两条 — 复核结论与飞书端描述不同
本机独立跑 `tools/verify_annot_sim.py` (06:26):
- ① 「是真渲染帧 (480x480+非纯色)」失败项是**断言里的画幅期望过时**: 代码 `v._rgb.shape[:2] == (480, 480)`,
  实际帧 **640×480** (新口径与真机同画幅) → 这是第 6 个"用例与代码脱节"例, **不是渲染质量/画面太暗**。
  (std=23.0 > 5 是通过的; mean=21 偏暗可另行看一眼, 但它不是本断言的失败项 —— 别去改渲染。)
- ② 「记录里设备标为 mujoco 渲染」: 实测 `data/yolo_annot_sim` 会话 `session.json` 里 `device=''` 而 `src='sim'` 已写 →
  是**写会话时漏写 device 字段** (真机会话都有 device, 如 'D405 (Docker tap 只读订阅)') → 数据集层一行修复。

### F05 ⚠️ 复测时别两端并发 (会互相覆盖)
`tools/verify_fiber_zero_regression.py` 的审计结果写**固定路径** `reports/zr_{scen}_f{fiber}_s{seed}.json`
(无 pid/时间戳)。09-19 06:23 两端的 F05 复测同时在跑 → 两臂 json 可能被对方进程覆盖 →
**"矩阵里 FAIL 疑为退出码非0" 这类结论有被并发污染的风险**。建议改成按 pid/时间戳分目录, 或约定同一时刻只一方跑。
(我已停掉自己那边的矩阵运行, 让飞书端的 F05 复测独占跑。)

### 纤维丛侧的一条独立读数 (审计 json, 06:23-06:24 落盘)
`summaries`: `l4.calls=8` 但 `cond_ready=0`、`w_zero=60`(60/60 帧权重为 0)、`intact_attach.intact_calls=0`、
`u_ff_src=analytic` → **零回退通过 ≠ 通道生效**: 条件没就绪、权重全程为 0, 与"A/B 无提升"一致。
待定位: `dit.calls=0` 是设计 (direct 档动作来自 intent_actor, 不走 DiT) 还是异常 —— 建议用 `probe_l4_callchain.py`
在 `L4`(direct 装配) 与 `L4line` 两场景各跑一次对比, 并在面板上如实标注"该档不生效", 不能按"零回退通过"当接上。

## 三、统一待办 (按阻塞优先级; owner: 现场/人 = 需要人动手, 我 = CLI 侧, 飞书端 = 飞书侧)

| # | 待办 | owner | 阻塞 | 判据 |
|---|---|---|---|---|
| 1 | 拖 10+ 位姿完成 2D→3D 标定 | 现场/人 | 需人动手 (1 分钟) | `models/box3d_state.json` 落盘 + 前提自证 IoU≥0.45 |
| 2 | F05 退出码复测 (独占跑) + 明确 dit.calls=0 是设计还是异常 | 飞书端 | 脚本固定路径会互相覆盖 | 同一场景两臂真跑 + 面板如实标注 |
| 3 | F08① 断言画幅过时 → 对齐 640×480 (别改渲染) | 飞书端 | — | 该子项转 ✅ 且 std 判据保留 |
| 4 | F08② 仿真会话补 `device` 字段 | 飞书端 | — | session.json device 含 mujoco/渲染 |
| 5 | `data/orin_6d` 缺 `data/*.parquet` → 新训练起不来 (auto_loop 空转) | 我 | 数据在哪要确认 | `data/*/chunk-*/file-*.parquet` 存在且能起训 |
| 6 | v10_full 只能影子 (相关 0.103 < 接管门槛 0.5) | — | 上述数据/训练 | seed104 相关 > 0.5 才谈接管 (SS_L3=1) |
| 7 | 在役深度话题来源/朝向查清 (全帧 2.0~3.0m, 与工作区 0.3m 不符) | 我 | 需确认相机安装 | 确认后才谈 `depth_align` 上真机 |
| 8 | depth→color 外参真值 (Orin 设备被占用取不到) | 我 | 设备忙 | `get_extrinsics_to` 能读出 |
| 9 | `plane_z` 量一次 | 现场/人 | — | 模块躺台面档可用 |
| 10 | 相机安装方式确认 (工装固定/腕上) | 现场/人 | — | 决定 held 档公式 |
| 11 | 纤维丛 6 条死线未接 (VLM 潜空间 z960×3 · 记忆×2 · 算子A×1) | 飞书端 | — | 每条有真数据流 + 同口径对照 |
| 12 | reacher 数据缺 2GB (98.9GB 解压未完) | 飞书端 | 磁盘/下载 | 解压校验通过 |
| 13 | 时序重采样 (L4 chunk → 引擎步长) 未实现 | 飞书端 | — | 步骤契约一致 |
| 14 | 提交/推送 2D→3D 代码与本文档 | 我 | 上次推送被中断 | GitHub 上有 commit |
| 15 | 画布 restart 使新节点生效 | 我 | 现场在用时再动 | 日志出现 3D 边界框行 |

## 四、给飞书端的口径提醒 (避免两端打架)

- 真机 3D 的位置语义: 现在**能**报 base 系 (K 已知 + 手眼自监督), 不是"只能报相机系"; 手眼待那 10 个位姿。
- `real_cam_calib.json` 的 `K` 已填 (`ready=true`), `T_base_cam`/`plane_z` 仍空 → 引用时按此口径。
- 真机在役 YOLO 类别名 = `peg` (旧 `optical_module` 已废) —— F08 那条修正的依据与此一致。

## 五、新增: 「环境 → 📝 任务指令」场景通道 + 标定向导 + 画布重排 (2026-09-19 早, 我这边)

老倪: 「增加一条从环境到 VEH.5.008 任务指令节点的数据通道 … 这个节点要能够看到场景 …
选一个模型 … 用大语言模型的视觉与理解能力推进真实场景的标定 … 画布 UI 重新编排, 要整齐」

**1) 数据通道 (画布可见 + 代码真有数据)**
- 画布: 3 条入线 `📦 metaworld 数据源 → 📝 任务指令 [in1]` / `🧪 环境渲染(引擎真图) → [in2]` /
  `🖥 Z700 真机信号 → [in3]`; 节点 desc 声明 in1/in2/in3 口径 (`tools/gui/gen_ss_vlm_channel.py`, 幂等)
- 代码: `tools/gui/node_logic.py::node_ss_llm_in` → `_vlm_frame()` 取图 (真机旁路同刻帧优先, 否则仿真渲染帧)
  → 后台线程调 `SceneVLM.describe` (主线程 0.00s 返回, 画布不卡) → `_SS_STATE["scene_vlm"]` +
  `_SS_STATE["llm_next_action"]` 供下游节点/面板消费

**2) 模型 (我选的: 开源 Qwen2.5-VL-3B 本地跑, 不依赖 key)**
- `src/lerobot/policies/left_right/state_space/scene_vlm.py` (策略层接口) + `tools/vlm_worker.py` (常驻子进程桥,
  协议走 fd1 副本, 库日志转 stderr) — 三条路径: ①`SS_VLM_URL/KEY` OpenAI 兼容 API (给了 key 才走)
  ②本地 worker (默认 `Qwen/Qwen2.5-VL-3B-Instruct`; 现已下 2.0GB/≈7.5GB, 下完自动成为默认)
  ③无模型 → **规则回退摘要**并如实标注 (不假装有视觉理解)
- 实测: 通道通 (真机帧 1.2s 帧龄 → worker 推理 4.3s → 返回) · 轻量 SmolVLM2-500M 跟不上严格 JSON,
  输出被如实判为"不是合法 JSON: 'true'" 而不是编造 — 语义质量以 Qwen2.5-VL-3B 为准

**3) 标定向导 (补上"缺少的人工标定工具")**
- `tools/calib_copilot.py`: 单帧体检 (`--loop` 常驻) / `--collect` 边采边拟合 / `--report` 出报告
- 每帧做两件事: ①**几何**-真机帧+同刻编码器位姿+YOLO 框进 `Box3DSolver` (与 2D→3D 同一条链)
  ②**视觉**-VLM 判"这帧能不能用"(模糊/太远/出画/遮挡/目标不在夹爪上) → 拒绝坏帧
- 输出: 当前采集进度 (帧数/平面性/姿态散布) + **下一步给操作员的具体动作** + 验收判据;
  合格帧攒够 → 自监督拟合 (手眼+off+R_rel) → 前提自证 IoU≥0.45 才落 `models/box3d_state.json`

**4) 画布重排 (`tools/gui/gen_ss_obs_layout.py`, 幂等, 只改 x/y/w/h)**
- 16 行按链路自上而下: L4 插拔链 → 数据源 → 大模型层 → L4 记忆/专家 → L3 记忆/高级 → L2 记忆/感知/融合/控制/决策/原子技能/执行 → 验证 → 可视化
- 可视区 **x 13500 → 4640 px** · 15 行内等距 300 · 行背景包住整行 · 旧 6 条碎裂的"L2 基础辅助功能"背景带合并成 6 条语义行带
- 验收: `verify_l4_zero_regression.py` **✅ 档位归属无变化 / L2 57→57 / L3 62→62 / L4 79→79 / 连线丢失 0**;
  `verify_l4_layout.py`: 方框重叠 0, 反向连线 7→40, 连线交叉 208→357 (**压缩跨度换整齐的代价**, 如实报)
- **VEH.5 编号按新 (y,x) 顺序重算, 但 VEH.5.008 仍是 📝 任务指令** (链路顺序恰好对上, 用户引用不漂)
- ⚠️ 踩坑: `verify_l4_zero_regression.py` 按**行背景名里有没有 L2/L3/L4** 推断档位归属 —— 重命名行背景会
  被判"档位回退"(实测踩过), 所以 L 档行名一律保留 L 标记
