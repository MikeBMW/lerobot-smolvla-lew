# 跨端同步 (静静 4060 ↔ 飞书端) — 2026-09-19 晚

> 两端共用同一工作树 `~/lerobot-smolvla-lew`，同步走本目录 + commit/push。
> 本文是今晚这一轮（19:00–22:00）的进度与结论，飞书端可直接引用。

## 一、产线 AOI 检测服务（金手指）— 已打通 ✅

**工控机 `192.168.23.23`**（与 Orin 同网段 192.168.23.x；SSH/RDP/VNC 全关 ✗，只有 HTTP）

| 端口 | 模型 | 相机 |
|---|---|---|
| **10082** | `gf` = 金手指 | D265250070 「OPT-CC1-GG50」 |
| **10083** | `housing` = 表面 | D265250099 「OPT-CC1-C050-GG3-00」 |
| 10081 | 同族第三个 Flask（映射未写明） | — |

- 唯一路由：`POST /capture_detect`，无参数无鉴权 → `{"code":200,"msg":"success"}`，实测 **0.53s**
- 语义：**纯异步** — 相机常驻 → 拍照存入工控机 `./goldfinger_images/`、`./surface_images/` → 立即返回；
  **YOLO 判决只打印在工控机终端，API 不返回结果** ✗
- 技能：`zmax-aoi-service`（devops）· 脚本：`tools/aoi_call.py gold|surface|10081`
- ⚠️ 待补：结果回传通道（建议 web 侧加 `GET /last_result`，或读图自行推理）——**对外不许假报 OK/NG**

## 二、L2 原子技能体系（本轮最大改造）✅

- **常驻执行器** `tools/l2_daemon.py`：两条常驻 ssh 通道（命令通道 + 位姿缓存），FIFO `~/zmax_data/l2_cmd.fifo`
  → **指令→动作延迟 60s → <1s**（实测：写 FIFO 同秒「已下发」，z 精确 +10.00mm）
- **注册表** `data/skills/l2_atomic/registry.json`：抬升/下降/前后平移/左右平移/合爪/张爪/到示教点（7 原子）+ 2 组合
- **UI**：画布「📚 工程记忆 · 技能与经验库」**双击** → 技能清单（选技能 → 填参数 → ▶开始）；菜单同入口
- **演示学习**：老倪手动演示 291s 位姿流(50Hz 14363 样本) → 切出 11 停稳点 + 夹爪事件 → 生成
  `data/skills/l2_muscle/光模块_抓放_演示学习_v1.json`（含 `contact_guard`：低位点留 5mm 余量）

## 三、分层口径（老倪 09-19 更正，覆盖旧说法）

**LLM 大模型层 = 场景理解(DeepSeek VL) · L4 INTACT = 工作安全 + 物理世界导航 · L3 = 长程序列规划 · L2 = 肌肉记忆操作**
文档：`docs/design/architecture_layers_v511.md`；画布 8 节点已改名 + 新增 💪 L2 肌肉记忆技能库

## 四、画布（本轮连续踩坑与修复）

- 「图深度重排」会把节点顶飞（前馈加速器 → x=9380 ✗）；我剔"右→左连线"时误伤主干 ✗
- 已修：主链排正 `DiT(1810)→前馈(2050)→动作调制器(3520)→安全边界(3856)→执行器(5090)`；
  恢复 6 条关键边（前馈→调制器 / 预测器→接触·性能流形 / 意图解码器·潜空间→DiT …）；
  现状 **83 节点 / 131 连线 · 右→左 0 · 功能孤立 0**
- 两坑写进技能：**未知 `type` 的节点会被画布静默丢弃**、**修布局必须带"主干白名单"+从 git 归档兜底恢复**

## 五、真机（人机在环）状态

- 首次完整抓取闭环 ✅：对准(视觉常数 -X→画面↓1.05px/mm、+Y→画面←1.12px/mm) → 夹紧(force40→开度185=夹牢)
  → 抬升 100mm（开度不变=未滑移）→ 放回槽位
- 夹爪 srv 有两次卡死（490/579 恒定 ✗），老倪走"别的命令"合上；**合爪通道待补充**（skill 里已记）
- 演示回放精度 0.2µm ✓，但低位点触底 ✗ → 已加 `contact_guard`
- ⚠️ 遗留：常驻执行器挂在本会话（会话结束即停 ✗）→ 建议做用户级 systemd 服务

## 六、版本

**v5.10.0 → v5.11.0**（四处版本号同步 + 更新日志）· Windows/macOS 已发布 v5.10.0：
`releases/download/v5.10.0/Z-MAX_Console.exe` / `Z-MAX_Console-macOS.zip` · 代码已 push
