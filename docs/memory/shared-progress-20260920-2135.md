# 跨端进度同步 (静静@4060 ↔ 飞书端) — 2026-09-20 21:35

## 本轮主题：L2「回槽位」两阶段技能（一号位 / 二号位）真机落地

### 成果（老倪现场验收）
| 技能 | id | 示教点 | 阶段1 | 阶段2 | 现场回位残差 |
|---|---|---|---|---|---|
| 一号位 | `L2.slot1` | slot1 (0.6471072, 0.5026424, 0.1135467) | +30mm 到正上方 | -30mm 落到槽位 | **0.4 µm** |
| 二号位 | `L2.slot2` | slot2 (0.6483203, 0.4524110, 0.1130842) | +30mm | -30mm | **0.6 µm** |

- 两技能**全程不发夹爪指令**（定义里没有 gripper 步骤）→ 抓持光模块不松爪。
- 槽距：一号位 ↔ 二号位 = 直线 50.2mm（dy -50.2）。
- 守卫（几何口径）：`z_floor`=目标 z 不得低于本槽位点（绝不下压进夹具）· 直线 >500mm 拒发 ·
  阶段1 相对下降限 400mm（转移段）· 阶段2 限 40mm（插入段）· 限速 30 · 锁点。
- 阶段串行铁律：每阶段**下发后用真值等到位**（阶段1 ≤1.0mm、阶段2 ≤0.5mm）才发下一阶段；
  未到位→中止剩余阶段、**绝不重发**。

### 代码（commit 131d96e9 / d8b39cff / 本轮）
- `tools/l2_daemon.py`：多阶段执行 `run_stages()`/`plan_stage()`/`_stage_timeout()`；
  **位姿来源改为直读话题**（`_pose_direct`/`_pose_best`，经 Docker tap 只读订阅）——
  常驻状态流的缓存会滞后整分钟级，曾导致 Δ 取旧值 + 两次"未到位"误判。
- 注册表热加载**一格延迟**的 bug（改完注册表后第一条指令仍用旧表）已修：每条指令前重读。
- 工具三件套（通用化，任意槽位复用）：
  `tools/record_point_watch.py`（盯真值自动录点）· `tools/register_slot_skill.py`（一条命令建技能）·
  `tools/test_slot_skills.py`（遍历所有 `L2.slot*` 自检：当前 2 槽位 × 15 项 + 全局 3 项，全过）。

### 现场两个坑（已修 + 已写进 skill `real-arm-motion-control`）
1. **`error_code=-18`**：`setPowerState(false before automatic mode): 该操作不允许在机器人当前运行状态下执行`
   = 还在**拖动模式**。表现是"点了技能没反应"。切自动(idle) 即可。
2. **超时不能写死**：驱动限速下 ≈2.8mm/s（speed=30），448mm 斜线要走 ~160s；
   写死 60s 会在半路判"未到位"中止，而指令已发不会撤回（臂自己走完，现场看着像"只走了一半"）。
   现按 `15 + 直线/eff × 1.6` 估算；另驱动自身 `wait_until_idle` 只等 30s → 长转移会带
   `ROBOT_IDLE_TIMEOUT/has_error=true`（动作其实已完成，别重发）。练习建议转移 ≤200~300mm。

### 数据留档
- `~/zmax_data/arm_slots_20260920/`：`points/`（taught_points + registry，19 个 L2 技能）·
  `code/`（daemon + 三件套）· `logs/`（一号位/二号位回位日志原文）·
  `field_run_1_slot1_return.md` · `field_run_2_slot2_return.md` · `MANIFEST.json`（sha256）

### 状态空间侧
- 「🅰️ 一号位」「🅱️ 二号位」已出现在 L2 技能清单（注册表热加载，无需重启 GUI），
  点一次=两阶段自动回位；GUI 与 FIFO 是同一条字节。
- 后续扩槽位（三号位/四号位…）：老倪拖到位 → 我盯真值自动录点 → 一条 register 命令建技能 → 自检即可。
