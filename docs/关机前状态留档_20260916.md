# 🗂 关机前状态留档 · 2026-09-16 (v5.6.7)

> 老倪: 「保存数据，小版本迭代，准备关机」。本文 = 数据清单 + 状态 + 下轮第一步。

## 一、本轮做完的 (有实证)

**L4 纤维丛联络层 (动作丛 → 接触丛 → DiT)** — 代码 `src/lerobot/manifold/fiber_bundle.py` (new) + 引擎接线 `tools/gui/state_space_sim_real.py`
1. INTACT **预测潜空间 z_pred** 真进链: 桥白名单原来只透传 z_t/z_goal/delta → 新键被**吃掉** (导出没接手); 零搜索 direct 不调 predictor → 用 `predict()` 逐位复刻 rollout_one_step 推完 chunk。
2. 丛映射标定 (n=**582** 真样本 · 8 seed×900 步):
   Φ:z_pred→接触丛 6 维 **R²_loso 0.471** (null 0.000) · A:z_pred→几何 z7 **0.607** · Φ_geo 0.586 · Φ_p 0.410; 闸值 R²≥0.30 且 null<0.10 → **过闸 ready=True**
3. 流形专家预测器**权重一字未改**, z 换源 `ẑ7 = A·z_pred + b` (运行证据: 预测器 z 来源 = `fiber(A·z_pred+b ← INTACT 预测潜空间)`)。
4. DiT 条件 token **214 维** (δ̂192 ⊕ ĥ_z6 ⊕ Φ(z_pred)6 ⊕ Φ_p(z_pred)6 ⊕ 标量4), 走同一 `apply_l4_cond`; 实测 DiT calls=25 ok=25 applied=200 β=0.5。
5. L4 行 **19 条边**审计: **有数据 13 · 死线 6** (VLM z960 ×3 需重训口径 / L4 记忆 ×2 / 算子A ×1)。
6. 零回退: L2 档 fiber 0/1 **逐位相同** `3d330ebc8b0c19e88cbd0706def2f455` · L3 档新代码根本不进。
   ⚠️ 仪器诚实: L3 逐位 hash **不可作判据** (625M CPU bf16 FP 级非确定, 同配置两臂亦不同 — 已复现 3 次)。
7. **同口径 A/B = 无实质提升** → `SS_L4_FIBER` **保持默认关, 不进默认档** (判据已从 1e-9 收紧到相对 1% 实质阈值)。

## 二、没做完的 (下轮第一件)

1. **对齐层未标定** (Step ①): `src/lerobot/manifold/l4_align.py` 已写且 selftest 通过 (对齐前 cos −0.974 → 对齐后 +0.944, 锥角/封顶生效), 但**对齐样本采集未落盘**
   (原因: 单轮 L4audit 120~300 步耗时 90~400s, 批量 3 seed 被 `timeout` 掐断 → `reports/l4_align_s*.npz` 为空 → `models/l4_align_map.json` 未生成)。
   下轮: 单 seed 单跑 (120 步, timeout ≥600) → `python3 tools/fit_l4_align.py` → `SS_L4_ALIGN=1 python3 tools/ab_l4_arms.py --steps 150 --seeds 0,2,5`。
2. **L4 提升证据仍缺**: 要出真提升需先 (a) 对齐后让注入过 L2 收口闸 (150 帧被否 79~120 帧) (b) 补插入/接触段标定样本修 cos∠ 跨阶段不稳 (0.80/−0.46/0.22; Φ 法向偏离维 R² 仍为负) (c) 再 3 seed×2 重复 + 视频。
3. 6 条死线边 (VLM 960 口径 3 / 记忆层 2 / 算子A 1) 未接。

## 三、数据清单 (全部落盘, 未入库的大件在此登记)

### ① 纤维丛标定 (真样本 + 标定结果)
- `reports/fiber_calib_s0.npz` — 186KB
- `reports/fiber_calib_s1.npz` — 219KB
- `reports/fiber_calib_s2.npz` — 535KB
- `reports/fiber_calib_s3.npz` — 365KB
- `reports/fiber_calib_s4.npz` — 535KB
- `reports/fiber_calib_s5.npz` — 511KB
- `reports/fiber_calib_s6.npz` — 196KB
- `reports/fiber_calib_s7.npz` — 214KB
- `models/intact_fiber_map.json` — 143KB

### ② L4 边审计 / 对齐基线取证
- `reports/l4_edge_audit.json` — 17KB

### ③ 同口径 A/B 原始记录
- `reports/ab_fiber_line_summary.json` — 3KB
- `reports/zr_L2_f0_s0.json` — 3KB
- `reports/zr_L2_f1_s0.json` — 3KB
- `reports/zr_L3_f0_s0.json` — 3KB
- `reports/zr_L3_f1_s0.json` — 3KB
- `reports/zr_L4line_f0_s0.json` — 7KB
- `reports/zr_L4line_f1_s0.json` — 8KB

### ④ 飞书汇报稿 (已发, 内容留档)
- `reports/fiber_report_feishu.txt` — 6KB
- `reports/fiber_ab_feishu.txt` — 2KB

### ⑤ 探针/审计日志
- `/tmp/l4audit.log` — 23KB
- `/tmp/l4audit2.log` — 23KB
- `/tmp/l4audit3.log` — 23KB

> 注: `models/` 被 .gitignore 忽略 (仓库策略), 标定文件靠 `tools/fit_fiber_map.py` / `fit_l4_align.py` 一键重建; 大件 (npz/报告) 不入库, 保留在本机 reports/。

## 四、环境/机器状态 (关机前)

- GPU: 纤维丛这一轮**没用 GPU** (全 CPU: INTACT 子进程 + 引擎 metaworld; 探针全程 `CUDA_VISIBLE_DEVICES=''`) — 关机前实测 GPU 占用 2684MiB/10% 来自**正在运行的 studio.py 调试会话** (VSCode debugpy, pid 896051/896056, 非本轮工作), 关 GUI 即释放
- 磁盘: / 已用 **283G / 396G (76%) · 剩 93G** —— 逼近"磁盘红线 300G"使用线, 建议下轮清 reports/outputs 大件
- 进程: auto_loop 守护 (pid 8334, 数据闭环心跳) **故意留着** — 关机直接关; studio.py 调试会话 + 一个遗留探针进程(pid 926111/926114) 已在留档时清掉探针, GUI 会话由用户自行关闭
- 训练: 无训练在跑; auto_loop 守护在跑 (pipeline 心跳)
- 时钟: 本轮期间系统钟被拨过 (NTP 已校正; 外部核对一致: 09-16 06:35 CST ↔ 百度 Date 22:35 GMT 09-15) → **下次开机先跑 `~/.hermes/scripts/hermes_cron_reclock.sh`** (13 个 cron 任务的 next_run 会被拨钟甩偏)
- 代码: commit 98cb64fd (纤维丛接线) + 43e01585 (A/B + 阈值修正) + 本次 v5.6.7 提交; tag v5.6.7

## 五、下轮启动顺序 (照抄)

```bash
cd ~/lerobot-smolvla-lew
bash ~/.hermes/scripts/hermes_cron_reclock.sh            # ① 拨钟后重算 cron
git log --oneline -3 | cat                               # ② 确认代码版本 (v5.6.7)
ls -l reports/l4_align_s*.npz 2>/dev/null || \           # ③ 对齐样本: 没有就单 seed 补采
  SS_PROBE_SEED=0 SS_L4_FIBER=1 SS_L4_ALIGN_DATA=reports/l4_align_s0.npz \
  timeout 600 ~/lerobot-venv/bin/python tools/probe_l4_callchain.py L4audit 120
~/lerobot-venv/bin/python tools/fit_l4_align.py          # ④ 标定对齐映射 (闸 cos≥0.9)
~/lerobot-venv/bin/python tools/ab_l4_arms.py --steps 150 --seeds 0,2,5   # ⑤ 三臂 A/B (基线/纤维丛/纤维丛+对齐)
```
