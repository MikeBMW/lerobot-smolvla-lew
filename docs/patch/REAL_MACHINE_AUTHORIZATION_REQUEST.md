# 真机操作 · 授权许可申请 (2026-09-26)

> 老倪: 「如果需要在真机上执行，给我发授权许可，我看现场安全后，你可以执行」
> 本文件 = 我**申请授权**的全部真机相关操作。**未获授权前，我不下发任何真机动作**（当前所有链路均只读）。
> 每项都写明：动作 / 预期 / 风险 / 现场前提 / 回滚 / 观测指标，便于你现场逐条核安全。

## 授权状态总览
| 编号 | 操作 | 类型 | 风险 | 现状 |
|---|---|---|---|---|
| **A1** | Orin → 云 状态上行（遥测心跳） | 只读遥测 | 低 | 脚本+清单已就绪，**待授权** |
| **A2** | AOI 金手指首轮标定（10082 拍照+框选） | 相机只读+人工放置 | 低 | 待你放件并框选 |
| **A3** | 工控机 10083 表面相机 `/picture` 补丁 | 工控机软件 | 低 | 补丁已备，**待授权部署** |
| **A4** | 10082 拉长口径补丁（判据图口径统一） | 工控机软件 | 低 | 补丁已备，**待授权部署** |
| **A5** | 2D→3D 标定采集（小范围动臂 + 相机对齐） | ★真机动作 | 中 | **待授权**（需你人在现场） |
| **A6** | T_base_cam / plane_z 实测（标定几何） | ★真机动作 | 中 | **待授权**（依赖 A5） |
| **A7** | ring_pose 示教（夹爪合拢位姿） | ★真机动作 | 中 | **待授权**（需你人在现场） |
| **A8** | 真机试抓取 五段计划 S0~S5 | ★★真机动作 | 高 | **待授权 + 计划批准** |
| **A9** | L2 力控插入（6N 接触） | ★★真机动作 | 高 | **待授权**（依赖 A5/A6/A7） |

---

## A1 · Orin → 云 状态上行（低风险，建议最先授权）
- **目的**: 云端/APP 现在看不到机器人流（`/api/relay/orin/status` = online:false）。补上后你在网页就能看到 Orin 实时状态与推理计数。
- **动作**: 在 **Orin** 上放 1 个脚本 + 1 个 systemd unit；每 5s 发一次心跳到 `https://datadrive.world/api/relay/orin/heartbeat`。
  脚本 `tools/orin_state_upload.py`（**只读**: 读本机推理服务健康/内存/温度，不下发任何动作）。
- **预期**: 5s 内 `/api/relay/orin/status` → `online:true, model=…, infer_count>0`；断开后 30s 内自动回落 false（防"假在线"）。
- **风险**: 低（无动作、无 ROS 写）。**现场前提**: Orin 能出网（>=1 次 HTTPS 成功）。
- **回滚**: `systemctl disable --now zmax-orin-upload && rm /home/*/tools/orin_state_upload.py`（无残留）。
- **观测指标**: 云端 `last_seen` 秒级更新；`infer_count` 单调递增。

## A2 · AOI 金手指首轮标定（低风险）
- **目的**: 现有数据集 4 张 960×960 **boxes 全空** → 无法训质量检测头。需要第一轮人工框选作为种子。
- **动作**: 你在 10082 相机前放 1 件光模块（金手指朝上）→ 我调 `/capture_detect` 取图并抓现场显示窗口 → **你框选**（或我按几何先验给初框，你确认）→ 落 `data/yolo_aoi_annot/`（标注 JSON + 原图，含时间戳）。
- **预期**: 得到 ≥10 张带框样本；判据图口径 = **手选框 > 原图自裁 > 拉长图**（老倪口径）。
- **风险**: 低（相机只读，不动作）。
- **回滚**: 不适用（只新增文件）。
- **时长**: 每件 ~1 分钟。

## A3 / A4 · 工控机侧补丁（低风险，需现场在工控机执行）
- **A3**: 10083 表面相机四路由仍 404 → 部署 `/picture` 补丁（脚本已备 `docs/patch/`）。
- **A4**: 10082 拉长口径补丁（判据图与训练口径统一 960×960 同源）。
- **风险**: 低（只加工控机 HTTP 路由，不动机器人程序）。
- **回滚**: 每处都有 `.bak_<ts>` 备份，一条命令还原。
- **验收**: 四路由 200 + 判决字段齐全；与 10082 口径对照表一致。

## A5 · 2D→3D 标定采集（★首次真机动作，需你在现场）
- **目的**: 真机 3D 定位需 K + 手眼 + plane_z；现在这三样都是**仿真值/未实测**，是 sim-to-real 最后一块。
- **动作**（小范围、低速、分 8~12 个位姿点）:
  1. 臂按预先生成的位姿点序列移动（每点 ±15mm 内、末端朝下、速度 ≤5%），停在每个点
  2. 每个点抓 1 帧相机图 + 读真值 `tcp_pose`（50Hz 真值）→ 存 (图, 位姿) 对
  3. 全序列结束后**自动回安全位**（不用你手动）
- **现场前提**（我会逐步请示）: ① 工作区无人员/异物 ② 你手放在急停上 ③ 我先读一次 `tcp_pose` 与关节限位确认无异常 ④ 目标点先 dry-run 报给你坐标与行程
- **预期**: 得到 ≥10 组 (图,位姿) → 解算 T_base_cam（判据: 重投影残差 <2mm）与 plane_z（判据: 平面拟合残差 <1mm）
- **风险**: 中（臂移动）。**回滚**: 随时 `move_joint` 回安全位；每次 `move_joint` 后**必下电**并回读真值确认（老倪红线条）
- **我需要的授权措辞**: 「A5 授权：允许在 XX 位姿点范围内低速移动并采集，我在现场看急停」

## A6 · T_base_cam / plane_z 实测（★依赖 A5）
- **动作**: 用 A5 数据解算标定文件（`zmax_robot_spec/calib.json`）→ 只写标定文件，不动臂。
- **判据**: 重投影 <2mm 且平面残差 <1mm；**不达标不写文件**（沿用"L4 标定闸判死"的诚实口径）。
- **风险**: 低（纯计算）。**回滚**: 旧 calib.json 备份保留。

## A7 · ring_pose 示教（★需你在现场）
- **目的**: `L2.pull_module` 合爪没夹住 → 需要真机示教 ring_pose（夹爪合拢时的位姿）。
- **动作**: 停在候选位姿 → 我读 `gripper` 力矩/开度与 `tcp_pose` → **你确认**合拢姿态 → 写入技能库（原子技能 L2 模板）。
- **预期**: 合爪成功、力矩在阈值内；判据 = 连续 3 次合拢开度一致（±0.2mm）。
- **风险**: 中（小范围动作 + 接触）。**回滚**: 松开夹爪 + 回安全位。

## A8 · 真机试抓取 S0~S5（★★需你批准计划 + 现场）
- **前置**: A2/A5/A6/A7 全部达标；五段计划 S0（接近）→S1（对位）→S2（下降）→S3（夹取）→S4（提升）→S5（插入）逐段批准。
- **动作**: 逐段执行，**每段结束停下回读真值并请示下一段**（不连跑）。
- **风险**: 高（接触/碰撞）。**安全**: 速度上限 + 力矩上限 + 每段超时 30s（超时 `success=False` **不重发**）+ 急停在你手上。
- **验收**: 每段位姿偏差 <1mm（几何真值口径）+ 画面自标状态（横幅/帧龄）；全流程录像留证。

## A9 · L2 力控插入（★★依赖 A8）
- **动作**: 6N 力控插入（沿工具 Z 退 60mm → 推进 → lissajous 微动）。
- **风险**: 高（接触力）。**安全**: 力上限 8N 硬限 + 插入深度上限 + 超时中止。

---

## 我的执行纪律（每次动作前后）
1. 授权前：**零动作**；只读链路照常跑（帧龄/画像/事件预测都在线）
2. 授权后：每次动作前**先报**（位姿点/行程/速度/风险）→ 你点头 → 执行 → **回读真值** → 报结果
3. `rt` 后**必下电**；`move_joint/line` 不下电；超时 `success=False` **不重发**
4. 每步落证据（真值 JSON + 图/视频 + 判据），任一判据不过**就地停**并如实报负结果
5. 动作类提示词（来自网页/HIL）**一律拒答**，直到你在这里明确授权

## 现在等你的一句话
- 只读类（A1~A4）：**「A1-A4 授权」** 我立刻开工（Orin 心跳 + 工控机两个补丁 + AOI 标定准备）
- 动作类（A5~A7）：**「A5-A7 授权，我在现场」** —— 我会逐步请示，每步停下等你确认
- 高风险（A8/A9）：先给我**五段计划 S0~S5 的批准**，再单独授权执行
