# 关机前状态留档 — 2026-09-26 晨 (静静)

> 本单在 **worktree `/home/ubuntu/zmax_rel` (main 线)** 上生成 —— 见下方「⚠️ 检出目录事故」。

## 一、本次完成 (已入 v5.15.6, commit 见仓库)
| # | 内容 | 证据 |
|---|---|---|
| 1 | **L5「Web 智能体桥 · 远程提示词」节点** (老倪: 「开通 L5 新节点与 web agent 交换信息, 位置 DeepSeek 左侧, 你来构图」) | 画布 87 节点/165 连线 · 桥在 x=1544/y=554 (DeepSeek 左侧) · 档位级审计 **R2 真接** · 取证 **27/27** |
| 2 | 通道 = ECS 中转**纯追加** `agent/{prompt,reply,status}` (游标式 append-only jsonl, GET 只读幂等) | 公网端到端: web POST → 本地派发 → web GET 回执含真数据 (87 节点/165 连线) |
| 3 | **ESC 中转双进程静默死掉修复** (zmax_relay 39053 + ws_relay 8765 都不在 → `/api/relay/*` 与 `/ws` 全 502) | 拉起后全端点 200; `/ws` 426=可升级 (服务活了) |
| 4 | **网络性能优化实证 + 开机自启** | 远端下载 4.30→**5.12MB/s (+19%, 5/6 轮)** · 无收益项拒绝留证 · `zmax-net-optimize.service` enabled |
| 5 | **AOI 几何口径对照** (训练口径=线上同源 960×960) | 死白 4.51% vs 20.28% · `reports/aoi_geom_parity_*.json` |
| 6 | **sim-to-real 上真机前预检 8/8** + 修 2 个真 bug | `reports/sim2real_preflight_20260926_*.md` · 全程零动作下发 |
| 7 | 能力清单新增 **L5 层 + L5-C01**; 技能新增 `web-agent-canvas-bridge` `linux-network-perf-boot` | `capability_levels.py` · `~/.hermes/skills/devops/` |

## 二、⚠️ 检出目录事故 (老倪请知悉)
- `/home/ubuntu/lerobot-smolvla-lew` 这个**共享检出目录已被切到分支 `mac-hw`** (并行「APP/DDS 硬件监控」线, 该线在本目录每几分钟自动 commit 一条 `chore(hw)` 遥测)。
- 后果: **main 线上的文件不在磁盘上** —— `tools/gui/studio.py` 显示 **v5.6.0** (旧版), `tools/zmax_net_optimize.sh`、`tools/verify_web_agent_node.py`、`tools/sim2real_preflight.py`、`src/.../web_agent_bridge.py`、`flows/state_space_obs.json` **全部缺失**。
- 影响:
 1. **重启 studio 会加载旧 GUI (v5.6.0)** —— 老倪要看的 L5 桥节点只有在 main 检出里才在。
 2. `zmax-web-agent-bridge.service` / `zmax-net-optimize.service` 指向那些脚本 → **下次启动会失败** (现在跑着的是内存里已加载的代码)。
- 处置: 本次发布**不动那个检出**, 在独立 worktree 完成:
 ```bash
 git worktree add /home/ubuntu/zmax_rel main     # main 线独立检出 (本次发布在此)
 # 建议: 硬件线用自己专属 worktree, 不要再切共享目录的分支
 # 想恢复本目录到 main: 先让硬件线停手 → cd /home/ubuntu/lerobot-smolvla-lew && git checkout main
 ```

## 三、关机前在跑 (别手滑)
| 项 | 状态 |
|---|---|
| **L3 b6 训练** (smolvla_lew, `zmax_train/config_l3_b6.yaml`) | 07:07 起 · 400 步 × ~2.9s ≈ **19 分钟/轮** · GPU 100%/5386MiB · ⚠️ `save_freq=500 > steps=400` → **中途不落 ckpt**, 关机即全丢 (该线 07:03 那轮已被重启过一次) |
| 硬件遥测线 | 每几分钟 commit 一条 `chore(hw)`; `/tmp/hw_sync.log` `dds*` 日志活跃 |
| 本机 8 个在役服务 | 全 active (注意上面 §2 的启动依赖风险) |
| GPU | 100% (训练占用) · 不要在此刻断电前手动清 |

## 四、待现场 (授权后即可开工)
1. AOI 首轮标定 (金手指/外观缺陷框选) — 数据集 `data/yolo_aoi_annot` 现 **0 标注框** (4 张 960×960 已同源)
2. 10083 表面相机 `/picture` 路由 (实测四路由仍 404) — 补丁 `docs/patch/opt_surface_10083_add_picture_route.md`
3. 10082 拉长口径 短边×2 + 裁减对齐 score≥0.95
4. 2D→3D 标定采集 (哨兵 paused, 需在场摆件)
5. `T_base_cam` / `plane_z` 现场测量 (真机 3D 最后两环)
6. `ring_pose` 示教 (`L2.pull_module` 合爪未夹住)
7. 抓取五段计划 S0~S5 批准 + 位姿来源
8. **任何真机动作授权** (老倪红线: 未授权不下发 · 本次全程零动作下发)

## 五、下次开工建议顺序
① 把检出目录理清 (worktree 分线) → ② 现场 1/2/3 (AOI 标定 + 两补丁) → ③ 认知头换靶子 (多步 K=5/10 + 事件级, 必须同表赢持久基线) → ④ 门控细化 (E0 吞 4 阶段) → ⑤ L4 哨兵提示词更新 (标定闸已判死, 别再用过期证据)

## 六、归档
`~/zmax_data/release_5.15.6_20260926/` (工具/源码/文档/证据 + MANIFEST.md + sha256)
