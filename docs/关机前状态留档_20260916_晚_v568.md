# 关机前状态留档 (v5.6.8, 2026-09-16 08:45)

## 一、本轮产出 (全部已落盘 + 已提交)

代码 (已 push, commit b42255ab + 本次 v5.6.8):
- `src/lerobot/manifold/lie_intent.py` — SU(2)/SE(3) + 接触丛 twist (孔系) + 切空间增益融合 + Φ 标定, 自检 13/13
- `src/lerobot/manifold/adaptive_gain.py` — 卡尔曼式自适应增益 (K_nav/K_flow/K_mm), 自检 11/11
- 引擎 `tools/gui/state_space_sim_real.py` — SS_L4_LIE / SS_ADAPT_GAIN / SS_QUALITY_GATE 接线 + 真值采集
- `tools/intact_direct_rollout.py` — 直驱档 w_eff=cos → K_nav (含方向/幅度闸)
- `tools/gen_l4_demo_video.py` — yaw 出口 Arm C (SU(2) 残差 + 夹持后几何闭环)

工具: fit_lie_intent · fit_lie_quality_gate · diag_lie_lateral · diag_lie_yaw · diag_gain_ab · diag_ff_entry · diag_ff_entry

数据 (models/ 与 reports/*.npz 按仓库策略不入库, 已打包):
- 打包快照: `/home/ubuntu/l4_snapshots/zmax_lie_l4_20260916.tar.gz` (873K)
- 样本: `reports/lie_calib_s{0,1,2,3,4,5,7,100}.npz` (含李群真值 ee/peg/hole 位姿) · `reports/l4_align_s{0,2,5}.npz`
- 标定产物: `models/lie_intent_map.json` (146K, Φ_su2/Φ_se3 ready) · `models/lie_quality_gate.json` ·
  `models/l4_align_map.json` · `models/intact_fiber_map.json`
- A/B 原始记录: `reports/ab_gain_*.json` · `reports/lie_yaw_*.json`
- 一键重建: `python tools/fit_lie_intent.py` / `fit_lie_quality_gate.py` / `fit_l4_align.py` / `fit_fiber_map.py`

文档: `docs/design/zmax_lie_intent_manifolds.md` (四步全表) · `docs/design/zmax_adaptive_gain.md`

## 二、结论一句话

李群意图路径**标定过闸**(Φ_se3 LOO R² 0.712 · Φ_su2 0.806)且 DiT 真消费 (cond 214→223);
但**任务级没有证明提升** ⇒ SS_L4_LIE / SS_ADAPT_GAIN / SS_L4_LIE_YAW 全部保持 **opt-in**, 不进默认档。

## 三、下轮第一件事 (按优先级)

1. **几何 token 定标** — 现在 ‖ξ‖≈0.023/‖ω‖≈0.012 在 214 维条件里几乎被淹 (cond_norm 只动到 1e-4 位);
   按各自的经验尺度归一后再看 DiT 响应 (①的诚实缺口)
2. **② 的收尾** — Arm C 与脚本臂**同步数+同停止条件**再 A/B; SU(2) 对准只在**夹持后**段参与
   (预抓取段 twist 不可控, 已证); 用 `yaw_grasp_probe.py` 的物理判据 (抬升 Δz>0.08m) 做成功率对照
3. **质量闸判据改成逐阶段吸引子** — 现在用"孔口"当唯一吸引子, 对未抓件段 (接近/抬起/转移) 不是正确梯度
4. **意图口径改 pred⊕goal + 逐阶段标定** (③ 实测: v_x 0.438→0.569, v_z 0.511→0.703)

## 四、关机清单

- 遗留进程: `auto_loop` 守护 (数据闭环心跳, 故意留) · `studio.py` (用户窗口, 关窗即释放)
- ⚠️ 下次开机: 先跑 `~/.hermes/scripts/hermes_cron_reclock.sh` (RTC/NTP 拨钟会把 14 个 cron 的 next_run 甩偏)
- 复现关键命令:
  ```
  # 采李群真值 (快, 每 seed ~20s)
  SS_L4_DIT=0 SS_L4_FIBER=1 SS_INTACT_EVERY=1 SS_PROBE_SEED=0 \
    SS_L4_FIBER_DATA=reports/lie_calib_s0.npz \
    /home/ubuntu/lerobot-venv/bin/python tools/probe_l4_callchain.py L4audit 400
  # 标定 + 质量闸
  /home/ubuntu/lerobot-venv/bin/python tools/fit_lie_intent.py
  /home/ubuntu/lerobot-venv/bin/python tools/fit_lie_quality_gate.py
  # 四臂 A/B (每臂独立进程)
  SS_MUSCLE_PATH=reports/tmp_muscle_gain.json SS_PROBE_SEED=0 \
    /home/ubuntu/lerobot-venv/bin/python tools/diag_gain_ab.py gain 600
  ```
