# Z-MAX 状态空间工程 · 下一轮迭代计划 (2026-09-26 冻结 v5.15.10 之后)

> 本轮指令(老倪): 「合并任务, 清理没必要的标定工作, 现场不要进行复杂操作, 清理系统, 清理跟主线无关的任务,
> 清理DNS, 状态空间工程重启, 准备进入下一轮迭代」

## 一、已砍掉(不必要 / 不宜现场复杂操作) —— 不再作为待办
| 项 | 砍掉理由 (有实测支撑) |
|---|---|
| **A5 手眼标定采集** (2D→3D) | 三条追踪路线**全部量化否决**: 强度差分(跨位姿曝光 64.5% 变异) · 位移一致性(Δu 562→103px 不成比例) · NCC(12/12 ≈0.84 泛匹配地板, corr≈0.03)。根因=相机逐帧自动曝光+场景纹理贫乏。**属"没必要硬做"的标定工作** |
| **A6 T_base_cam / plane_z 解算** | 依赖 A5 数据, 无输入即无从解算; 相机内参也不在本机(工控机 SSH 关) |
| **A7 ring_pose 示教** | 需现场示教动作, 属"复杂操作", 本轮不做 |
| **A3 10083 补丁现场粘贴** | 需现场改产线机程序(109 行补丁) = 复杂操作 → 降级为"现场窗口允许时再说" |
| **A4 工控机切 v4 模板法** | 需现场改产线机配置 = 复杂操作 → 降级; **但风险已记录在案**(见下) |
| **画布交叉优化(1395 对)** | 纯视觉整理; 实测行内重排反而 +9.2% 交叉(跨行长连线主导) → 非主线, 转 backlog |
| **Orin SDK 桥鉴权 / 归档 1.79GB 清理** | 非当前主线, 转 backlog(归档件按规只列不删) |

**降级不等于遗忘 —— 两项真实风险仍挂着(写在这里避免丢失):**
1. 工控机 `crop_info.score = 0.325`(v3 固定窗) vs v4 验收口径 ≥0.95 ⇒ **它返回的 OK/NG 在当前裁减口径下不可信**;
   降级期间: AOI 判据一律以**本机自裁口径**为准(已量化更优: 饱和 55.0%→13.6%, 细节 10.0→89.0)。
2. 10083 表面相机没有 `/picture` 路由 ⇒ 表面相机目前**只能触发检测、不能取图**; 需要取图时再上补丁。

## 二、主线(下一轮迭代目标, 按优先级)
```
M1 · state_space 策略达标 (上真机前的唯一硬缺口)
    现状: 引擎闭环口径 done=true·末端 0.9mm·1/1; 5 种子统计 **20%**
    目标: S2 零样本测 RealityGap → S3 真机低 lr 微调 + **ensemble 必开** → 成功率 ≥60%(5 种子)
    判据: 同口径(visual39 + _l3_pre + 128 图 + task 串)对照, 每次报前/后 + 平凡基线

M2 · 事件级认知头进产品化闭环
    现状: 同源 300 段 CV 6/6 · 闭环 7/7 · 引擎逐帧真调(default engine_v3)
    目标: ① 6 个事件概率接进控制台可视化(3D/Scope 面板) ② 接进 L2 收口势函数(执行永远由 L2 收口)

M3 · MoveIt 完成度 (本机规划已通, 差收口)
    现状: 镜像 zmax-moveit:humble + 配置包(限位来自 URDF) + /compute_ik 有效解 ✓
    目标: ① 补齐 meshes(碰撞几何) ② 规划接口接进画布 n_moveit 真执行函数 ③ **规划→执行闭环验证**(规划→SDK 桥小步执行→真值核对)

M4 · 交互面(已上线, 转维护)
    /agent.html(Web 智能体桥) · /hil.html(HIL 人机在环) · 首页四入口 · ECS 中转 /hil/state
    目标: 只做稳定性(回执延迟 5s→2s 可选) 与新功能不冲突

M5 · 工程节奏
    版本: v5.15.10 已冻结(commit f32e5cf9 + tag) → 下轮小版本 v5.15.11
    服务: 10 个在役服务 · 开机自启已核 · DNS/缓存清理已做(本轮 129MB + DNS flush)
    台账: docs/TASK_LEDGER_20260925.md (唯一真源) · 本文件 = 下一轮计划
```

## 三、现场轻量项(允许, 不复杂) —— 有空就做, 不做不影响主线
```
L1 · AOI 缺陷样本: 台面放一件**有缺陷的光模块** → 我采缺陷类标注(现在 8 帧全 count=0, 只有 OK 类)
L2 · Orin 上行已通(A1) → 如需机器人状态更细(关节/力矩)可加(桥已就绪, 一条命令)
```

## 四、本机可再生资源与红线
```
磁盘: use ≈298G / 红线 300G (MoveIt 镜像 2.58G + AOI 采样 + 手眼图 是新增大头)
  → 如需腾空间: sudo docker image prune(仅删悬空) / 旧 AOI 帧可删(已存台账) / 归档件 1.79G(需点头)
断链: stable-wm-cache/datasets/dmc/reacher_random.h5 为既有断链符号链接(非本次清理造成, 未删)
GPU : 空闲(persistence=Enabled) · 训练/评测按需起
```
