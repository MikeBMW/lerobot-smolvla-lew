# Z-MAX 版本管理规范 (VERSION.md)

> 语义化版本: `主.次.补丁` — 主(架构重构) · 次(功能模块) · 补丁(修复/小优化)

## 版本号位置 (改版本必同步)
1. `tools/gui/studio.py` — `ver = QLabel("Z-MAX vX.Y.Z")` (品牌版本小字) + `setWindowTitle(... vX.Y.Z ...)` (窗口标题, 两处) + changelog 注释前缀
2. `tools/gui/update_checker.py` — `CURRENT_VERSION = "vX.Y.Z"`
3. `tools/gui/version_sync.py` — `zmax_ver = "X.Y.Z"`
4. `tools/gui/docs_sync.py` — `"version"` + `"zmax_version"` 两键
5. `git tag vX.Y.Z` — 打 tag 并 push

## 变更摘要规范
- 窗口标题注释里按 `vX.Y.Z: 变更点1+变更点2` 追加最新版本摘要到最前。
- 只写"做了什么 + 根因"，不写过程。

## 版本历史

| 版本 | 日期 | 内容 |
| **v5.11.5** | 09-22 | 🎯🔧 真机 J5 力矩超限**当场修好 (SDK 一条调用, 不需要安全密码/厂家)** + 视觉引导抓取技能 (双路判据) + 离线自检加固 — 老倪: 「机器人还是报警, 你能修好么? 现在动不了了」「你先把这个动作编写成一个视觉引导的技能, 保存数据, 小版本迭代; 等我确认安全了再实际发送」 ①**J5 修好**: 根因 = 力矩传感器**零点偏置** (不是负载大) —— 修法 `calibrateForceSensor(all_axes=True, axis_index=0)` (xCoreSDK `Cobot_6` 接口; 官方文档口径: SDK 控运动时建议每次运动前标定) → **J5 −22.0031 → +0.5449 Nm** (距 RSC 22.000 门槛余量 **+21.35 Nm**) · **J1 +29.08 → −0.258** (竖直轴重力恒 0, 应≈0) · J3/J6 同步回到模型量级 · 10s 复读极差 0.109/0.391 Nm (稳定非跳变); **更正上一版错误结论**: v5.11.4 写「SDK 93 个接口无任何力矩/碰撞力接口」是**错的** —— 接口在 `Cobot_6` (xMateRobot 基类), 权威清单读 `xcoresdk_python/Release/linux/xCoreSDK_python/__init__.pyi`, 别用 `dir()` 关键字筛 (会漏); 现场随后真实点动 (L2.forward 50mm) 无任何报警 = 实测闭环 ②**视觉引导抓取技能** `L2.grasp_vision`: 判位 = **双路独立证据一致才认** (①YOLO peg 框底心 ↔ 槽位示教点手眼投影: Δx≤12px ∧ 框底−投影∈[−5,25]px ②模块竖直带「棱边能量」列剖面峰列归属 ≤20px), 执行 = 槽正上方(+30mm) → 下降到抓取位(禁下压) → 合爪 force40 → 抬升 50mm; **视觉门在执行器内 fail-closed** (占位点 `slot_vision` 解析不出/两路冲突/帧不新鲜(负帧龄或>5s) → **拒发**), 守卫沿用 z_floor(绝不低于示教抓取位)/直线≤500mm/下降≤40mm; 实测判定 **slot2 有光模块** (YOLO Δx=3.3px + 像素棱边峰距 0.0px; slot1 Δx=43.1px / 峰距 46.4px) ③**离线自检隐患修复**: `test_slot_skills.py` 原来只关位姿直读, **运动步仍走真通道** (等于离线自检能真下发机械臂) → `chan_send/_call_remote/_service_call` 三条出口全换记录器 + 逐用例断言下发条数 ④新增 `tools/vision_grasp_skill.py` (判定+前置闸门+编排+事后验收, **默认 dry-run**, `--apply` 才真发) · `register_vision_grasp_skill.py` (幂等注册) · `slot_occupy_verify.py` (像素级复核+证据图) · `test_vision_grasp_skill.py` (**30 项全绿 · 真机零接触自证 sends=0**) · `bump_version.py` (版本一条龙); 技能表 24→25 条 |
| **v5.11.4** | 09-22 | 🧪📦 **全层模型重训(新数据) + 真机 J5 力矩根因取证 + 发布补齐 (v5.11.2/v5.11.3 只提交未打 tag ⇒ 本次一并出 Windows/Mac 包)** — 老倪: 「保存数据, 小版本迭代, 发布 windows 和 mac 版本 / 同步数据, 进行依据真实数据的仿真」 ①**L4 INTACT 新抗干扰数据域内训练**: `optical_insert_v6_disturb.h5` (4.1GB · **1743 回合 / 87150 帧** · 24 维 skill_ctx · 三档真干扰 mixed · success-only) —— 5 轮接力 (1ep×1000步, 从在役权重续, 记忆通道不零化) **local_mae 0.0340→0.0310 · goal_mae 0.0350→0.0320 · action_loss -3.794→-3.834 单调改善**, `skill_ctx_usage` 恒 1.000 ②**判闸 (训练同源自监督回放 · 同帧同权重只切通道)**: **on 0.0282 vs zero 0.1075 → Δ-0.0793 (误差 3.8×)** = "L4 真在用 L2 原子技能" 的硬证据; **同口径对照** (同一份新数据只换权重): 老 v6r11 0.0343 → 新 v6d5 0.0282 (**+17.8%**); 诚实副作用: 老 v5 数据上哨兵 on-MAE 0.0289→0.0312 (轻微域漂移, 待观察) ③**L3 SmolVLA+LEW** 300 步有界训练完成 (loss 0.206~0.212 · action_loss 0.165 · lew_loss 0.0006 · ckpt 1.29GB) ④**真机 J5 「总超限」根因取证 (全程只读, 未下发任何控制指令)**: 静止(六轴速度全 0 · 末端离台面 40mm) 实测 `[J1 +28.98, J2 -15.92, J3 -25.14, J4 -11.38, J5 **-21.93**, J6 +0.84]` Nm vs 按现场 URDF 算的重力模型 `[J1 **0.00**, J2 +28.92, J3 -23.05, J4 +3.32, J5 -2.03, J6 -0.12]` → **J5 差 20Nm (11×)** 且 **竖直轴 J1 读 29Nm 物理上不可能来自重力**; FK 自校 (末端 vs `cartPosture` 差 3mm); 纯外力假设需 132N 且仍余 22.8Nm 残差 ⇒ 排除外部负载; 控制器原文 `#30400 5轴传动力矩 22.035724 > 限定力矩 22.000000` / `#13036 RSC 关节[5]碰撞力超限` / `#41447 传感器与模型偏差 Axis1 30.547 vs 0` / `#13047 RSC 参数需硬重启` / 警告 `#41461 力控工具TCP>0.15m → 参考点退法兰` `#35100 六轴母线欠压`; **硬重启控制柜前后读数 Δ≤0.15Nm ⇒ 重启无效**; 结论 **读数与模型不自洽 (非负载大)**, 静止基线 21.93 贴 22.000 门槛 ⇒ 一动即报; 修法 = 示教器"回机械零位 + 力矩传感器清零" (SDK 93 个接口**无**力矩限位接口; 现场无安全密码) ⑤新增只读探针 `tools/rokae/` (六轴力矩/工具负载/控制器日志原文/SDK 接口全列/URDF 重力对照 + README) + **URDF 入库** `config/robot/xms5_r800_w4g3b4c.urdf` ⑥**YOLO 几何自动标注器** `tools/ss_yolo_geom_labels.py` (episode 自带相机参数 → 3D 有向盒投影出框, 免手眼标定; 自检 6/6: 光轴→中心 0px · 往返 5e-17m · 相机后方点剔除; 实测 720/720 帧出 peg+slot 框 · 中心逐帧位移中位 0.04px) — **诚实限制: episode 相机是工作台全景, 模块仅 8px(640²)/3px(224²) ⇒ 微调 YOLO 前必须先换近景视角重渲染** |
| **v5.9.0** | 09-19 | 🧭🛡 **真机首次适配准备 + 全系统安全检查 + 场景同步全系统** — 老倪: 「我要和你一起进行标定适配过程 … 由 L4 负责安全, L3 负责流程, L2 负责操作; 再次检查全系统逻辑, 冗余检查后, 我们开始人机在环操作 … 保证安全的人机操作」 ①首份真机安全检查单 `docs/design/real_machine_safety_checklist.md` (层责任表 + 只读体检 + 8 条作业闸门 + 风险 R1-R9 + 可标定性表) ②**真风险**: `collision_detection_enabled=False` (撞了不停 · R1 · 未解除不做插拔类动作) · `tool_load` 全 0 (R2) · 产线 `/motion` 会抢控制 (R3) · `/robot_status` 被截断 → estop 字段取不到 (R8) ③场景同步全系统 `tools/sync_scene_state.py` → `data/scene_state.json` + 总装上下文 + 宏观记忆 `scene` 段 ④冗余检查: L4 双实现标注 R9 / 同名函数已修 / 4 对重复连线保留 ⑤**功能↔用例矩阵 20/20** (F09 硬写节点数→与流程对账) ⑥可标定: K ✅ / 手眼+off+R_rel ❌ 待标 / plane_z ❌ 待量 ⑦画布 82 节点 158 连线 · 孤立 0 |
| **v5.8.0** | 09-19 | 🧠🔗 **大模型层连接拓扑补全 + 环境数据入技能编排器 + 工程记忆→总装记忆同步** — 老倪: 「技能编排器怎么没有输入呢? 环境数据要输入给技能编排层的大语言模型啊」「其它节点怎么都是悬浮在那呢? 你要设计好连接拓扑关系」「当前的工程记忆, 要同步到大模型层的总装记忆节点」 ①画布拓扑: 新增 📚工程记忆节点 + **31 条连线** (环境→编排器 · 状态/安全否决→异常推理器 · 三层记忆+图谱+工程记忆→总装 · 意图丛/技能词典/记忆图谱/意图直读 从**全悬空接上真实读写** · 数据源→模式→档位→各层 · 标定层↔2D→3D/潜空间), 悬空节点 0。 ②布局按**数据流层级定列** (DFS 反馈边→DAG 最长路→x=层级): 右→左连线 基线7 → 首版40 → **13 条(全部为真双向上报/下发, 自动标 ↩)** · 重叠 0 · 穿框 47→25。 ③环境输入: `_skill_spec_from_env` 读 真机/仿真帧来源+帧龄 · 场景理解 · 宏观记忆建议 → 规格文本, 缺路如实打印 (实测仍注入宏观建议)。 ④工程记忆→总装: `src/lerobot/memory/eng_memory.py` 真读 docs/memory(59)+Hermes记忆(2)+技能(163) = **224 文件/997 记忆条目/1581 技能小节** → **追加式**写入 `macro_memory.engineering` (幂等指纹+原子写+回读), 飞书端其它键保留。 ⑤修同名覆盖: `node_ss_skill` 被原子技能处理器二次定义 → 更名 `node_ss_atomic`。 ⑥验收四条全绿 + 零回退 ✅ (档位归属不变/L2 57/L3 62/L4 79/连线丢失 0) |
| **v5.7.0** | 09-18 | 🕐🤖 **真机 J6 首次真运动端到端打通 (30° 指令实到 29.99975°) + 时钟回拨事故修复 (真机输入图像"不实时"的真根因)** — 老倪: 「为什么真机的输入图像不是实时的了」「让J6旋转30度，逆时针」「来回旋转30度」「保存技能，中版本迭代，发布windows mac版本」 ①**时钟回拨事故**: 12:15 NTP 把系统钟**回拨 8h** ⇒ 用 `time.time()` 差值做节拍判据的常驻循环判据变号 (tap 采样恒假=停落盘 · 解码节流恒真=永不解码) → `cam_rs.png` 冻在最后一帧 13 分钟; GUI 判新鲜度用 `now - mtime`, mtime 在未来 8h ⇒ 帧龄 **-28800s ≤ 10s 阈值** ⇒ **旧帧被判"新鲜"**(踩不拿旧图冒充实时红线)。修: `ss_remote_tap.py`(采样/解码/帧龄全改 `time.monotonic()`, 帧龄钳非负) · `ss_bypass_run.py`(逐帧节拍同修) · `yolo_input_viewer.py`(**负帧龄一律拒用并标「⏰ 时钟异常」** · 断流自愈计时改单调钟 · `age_s` 必须落在 [0,5]s) · 回归自检 `tools/verify_clock_skew_guard.py` 全绿。纪律: 节拍/新鲜度一律单调钟。②**真机 J6 首次真运动**: 运动类接口**全是 service 无 action** (`/target_relative_joint` 相对 · `/move_joint|line|pose` TargetPose · `/move_sequence` · `/gripper_driver` · 力控原语 · `/robot_stop`); 关节名必须用驱动 `joint_name` 值 `XMS5-R800-W4G3B4C_joint_1..6`。**解锁序列 = 关闭拖动 → 切自动模式(idle) → 伺服上电(on)** (缺一必被拒: 控制器 `#10011 切换至自动模式失败 → 关闭拖动`)。解锁后 J6 **+0.5235987756 rad → success=True**, 实测 **+29.99975°**(误差 4.4µrad), 其余五轴 ≤0.0014°, TCP 圆弧 **12.71mm**(理论 12.53mm), 旋转轴与 FK 的 J6 轴夹角 **0.180°**; 反向 -30° 误差 0.0006°, 回转 TCP 差 1.2mm。③**坑**: `/move_joint` 动作**跑了但返回 success=False/ROBOT_IDLE_TIMEOUT**(`wait_until_idle` 30s 超时) ⇒ 判完成只看真值+`operation_state=="idle"`, **不可凭 success=False 重发**; **rt 动作后驱动自动下电**(抱闸保持, 六轴速度 0); 控制器报警历史在 Orin `/tmp/tashan_robot_run.log` 的 `[robot_status]` 行(含 repair)= 最快根因入口; Orin `header.stamp` 比本机慢 26.24h; `collision_detection_enabled=false` 与 `tool_load` 全 0 待补。④工具/留档: `tools/ss_fk_xms5.py`(URDF/FK + 灵敏度 + 腕部偏置 J4↔J6=136mm) · `~/zmax_data/arm_j6_first_test_20260918.md` · 技能 `real-arm-motion-control` ⑤红线: 未碰 `/hmi/command` · `/execute_external_task` · `/state_machine/*` |
| **v5.6.24** | 09-17 | 🏷 **真机 YOLO 标定工位打通 (拖框/选中间步/删除) + 真机位姿真值记录 + 机器人动作自动标注设计** — 老倪: 「左键拖不出框 / 改选中的类别不好使 / 标号的数据在哪里 / 加两个删除按钮 / 记录光模块 x y z 对齐 metaworld / 设计用机器人实际动作自动标注」 ①**修两个真 bug (offscreen 取证)**: (a) **拖框锚点被每帧鼠标移动覆盖** (`_edit_drag` 写 `("new", start, pt)` 而非 anchor) ⇒ 松开时只剩最后一小段位移 (<4px) 被当误点丢弃 = 「左键拖不出框」; 修后显示 (100,100)-(260,220) → 原帧框 (50,50,130,110) 正确, 旋转窗 180° 同测 (189,129,269,189) 自动换算回原始坐标 (b) **两窗只同步框集合、不同步选中项** ⇒ 在旋转窗 (相机翻转时最常用) 点选后「🏷改选中类别 / 🗑删选中 / ↩撤销」取不到选中 → 看着像按钮坏了; 修: selectionChanged 互相同步 + 这几个键改走 `_active_pane()` ②**类别口径纠正**: 真机类名必须 `peg` (不是默认 `optical_module`) — 依据唯一口径源 `yolo_3d/frame_source.py:40 CLASS_MAP{peg→光模块}` 与下游 `real_yolo_perceive.py` 的 `det3d["光模块"]/["hole"]`; 且 `save_sample` 对未知类名会**自动追加进 classes.txt** ⇒ 旧名污染类别表 (id 顺序乱); 已改 classes.txt/DEFAULT_CLASSES/GUI 兜底并把已存 3 张标签 id1→id0 救回 ③**数据管理**: 窗口新增「🗑 丢弃当前帧」「🗑 清空本会话」(图+标注**成对删**, 不留孤儿) + CLI `--clean`(清孤儿标注) / `--reset --yes`(清空重来, 保留类别表与审计流水); 实测临时根: 孤儿标注删/孤儿图保留/配对不动、reset 清 19 文件且 classes.txt 保留 ④**真机位姿真值 (单一来源 `tools/real_truth.py`)**: 读采集容器落盘 state jsonl 末行 (尾部 64KB 反扫) / status.json → TCP+四元数+关节+新鲜度; 保存样本写 `annotations.jsonl.truth` + 侧车 `sessions/<会>/truth.jsonl`, `--build` 聚合 `dataset/truth.jsonl`; **按 metaworld 39D 段位对齐** ([0:3]hand=TCP 真值 · [4:7]光模块=TCP+R·夹具偏移 · [7:11]peg_quat · [36:39]hole=示教 goal); 夹具偏移/示教几何未标定时**填 null+原因, 不编造**; 窗口加 1Hz 真值行 (实测 TCP=[0.43678,0.32978,0.222018] base_link · age 0.02s) ⑤**机器人动作自动标注 (设计+数学核)**: `docs/design/real_autolabel_by_robot_motion.md` — 机器人自己当标定物: 探针运动 (夹着模块走 9~12 位姿, 相邻帧差分取运动域中心当模块像素, 配对用**两帧 TCP 中点**消除半采样周期偏置) → DLT 解 3x4 投影矩阵 P (=K·[R|t], 已含手眼外参) → 批量把真值投影成 YOLO 框; `tools/real_autolabel.py --selftest` 全绿 (rms 1.4e-13 · 还原内参 fx610.000/fy607.000/cx318/cy242 · 1px 噪声→0.811px · 薄片绕光轴转 90° 框 14.5x37.5→37.7x14.4 正确互换) ⑥**模型引擎页一键训 YOLO**: 新增「🚀 YOLO 训练」节点 (policy=yolo · 步数=epoch · 与 🚀 ACT 训练 同列对齐) — 原 `_train_yolo_detector()` 写了却没有节点传 `policy="yolo"`, 从界面到不了; 同时修该函数: 解释器自动选带 ultralytics 的 gui-venv311 (旧写死 ~/lerobot-venv 实测没装 → 一点就报错), 数据源优先**真机标注** data/yolo_annot/dataset (`yolo_annot_train.py --base auto` 域适应微调 imgsz640) 否则回退仿真 data/yolo_peg, 仿真分支真用 GPU; 踩坑: match_node 取最长关键字, 裸 `"YOLO"`(4字) 抢走 `"训练"`(2字) → 单独注册 train_yolo("YOLO 训练"); 另 edges 源索引是 3 (YOLO 检测) 不是 2 (共享 🧩 定义被跳过) 否则连线静默丢弃 ⑦**运维**: `zmax-studio` `Restart=on-failure→no` (journal 实证 11:51 `The X11 connection broke` → 退出码 1 → 5s 又拉起 = 反复重启; 老倪「别自动重启」) · **采集容器 DDS 假死** (长跑后 jsonl 在长但所有话题收包恒 0, 新建容器同话题立刻 49.6Hz ⇒ 启动撞上 NTP 拨钟致 RTPS 发现不自愈) → `docker restart` 即恢复 · 标定工具写出路径与采集器读取路径**口径对齐** ($SS_OUT/real_cell_geometry.json), 否则示教完 tap 仍报「无示教几何」 ⑧**零回退取证**: 模型引擎 65 节点/101 连线 vs 基线 64/100 — 旧 64 节点索引→名字逐条不变 · 旧 100 连线一条不少 · 布局仅第 0 行第 10 列新增 ⚠️ 诚实缺口: 本批 14 张人工标注里 6 对为同帧重复 (有效约 8 张), 且**无 truth 侧车** (真值记录于本版才落地) · 夹具偏移/示教几何/模块物理尺寸均未标 → 自动标注目前只有设计+数学核, 未接批量实现 · `--label` 仍需 `--size` 实测值, 不猜 |
| **v5.6.23** | 09-17 | 🧪 **sim→real 域差专题: 域随机化 (DR) 三臂同口径对照 — 结论: 纯仿真 DR 不足以跨真机** — 老倪: 「我要用 yolo 目标检测的模型, 运行在 orin 上, 能够感知实际的机器人环境」 ①**三臂 A/B** (同基座 `peg_v1` · 同超参 25ep/batch16/imgsz480/seed0/4060 · **只差数据**): `peg_v1`(仿真 1800) ‖ `ctrl_sim`(仿真 1800 重训=控变量) ‖ `dr_mix`(仿真 1800 + **DR 9000**) → 真机 19 帧 × 4 朝向 = **76 次推理全 0 检出**; 真机最高 conf 0.0107 → **0.0301**(ctrl) / 0.0177(dr), 仍只阈值 0.25 的 1/10; 仿真 hold-out 三臂 **3.00 框/帧 · 三类齐全 60/60** = **零回退** ⇒ **真机数据微调 / 教师蒸馏为必做**(都要 Orin 在线); DR 数据保留作真机微调的**防遗忘混合集** ②**DR 生成器** `gen_yolo_data.py --dr`(**默认关 = 原路径零回退**, 同进程等价性 **120/120** 标注逐字符一致): 场景层(光照位置/方向/强度/色温/环境光 · 12 材质颜色全随机 · 纹理换随机噪声 · 相机 ±3cm/±5%fovy/±5° 旋转) + 图像层(亮度/伽马/对比/色偏 · 高斯噪声 · 运动/离焦/降采样模糊 · 暗角 · 遮挡 · 缩放裁切与 ±8° 旋转(框同步) · **灰底 114 letterbox** 复刻真机帧进 imgsz=480 的版式); 9000 张, 同 `--scene-seed` **逐位可复现**(300/300 文件一致) ③**新工具**: `tools/eval_sim2real_yolo.py`(多臂双侧同口径评测: 真机 4 朝向 + 仿真 hold-out + 逐臂 json + 目检图) · `tools/analyze_real_det.py`(位置先验: 光模块现场在画面**下方正中** x∈[0.25,0.75] y∈[0.55,1.0] / 朝向一致性 / 分帧组) · `tools/real_frame_selfcheck.py`(测集自检: 本轮 **11/19 帧**目标区几乎无结构 → 0 检出不能全算模型锅) · `tools/verify_dr_labels_geometry.py`(唯一标记+模板匹配, **60/60**, 最大错位 7.3px) · `tools/build_dr_mix_dataset.py` · `tools/train_sim2real_arms.py` · `tools/orin_mjpeg_server.py` + `tools/verify_real_frame_provenance.py`(真机帧真迹取证) ④**方法论坑入档**: metaworld 场景随机向量走**全局 np.random**(`env.reset(seed)` 不生效) → 跨进程逐位比对不成立(同一份旧代码跑两次 300/300 图都不同), 零回退只能同进程等价性 + 训练/评测用落盘固定数据集 + 显式 `--scene-seed`; 模板匹配验证标签必须用**唯一标记**(另有 patch 缩放因子/旋转中心两处易错点); **0 检出先做测集自检** ⑤文档 `docs/design/zmax_sim2real_dr_experiment.md`; 归档 `~/zmax_data/sim2real_dr/20260917`(MANIFEST 含 sha256/口径/复现命令; 数据集同盘硬链接零额外占用)。⚠️ 诚实缺口: 真机精度仍未验证(无真机标注数据); 真机 3D(内参 K + 手眼外参 + plane_z)未标; Orin 直连不在(USB 千兆网卡未接, 192.168.23.x 网段不存在) |
| **v5.6.22** | 09-17 | 🏷 **真机 YOLO 标定工位: 画框标注 → 数据集 → 训练一条龙** — 老倪: 「现在要采集真机图片训练 YOLO。在右键打开的窗口增加标定功能: 标定工程师根据图像圈选光模块、输入类别、保存当前图片, 而且 YOLO 模型可以通过保存的图片进行模型训练。做好标定工程、数据保存、数据文件夹路径的设计」 ①**窗口即标定工位** (`tools/gui/yolo_input_viewer.py` 第 2 行标定行 + 第 3 行数据行): ✏️标定模式(入口**恒常显**) · 类别combo+＋新类别 · 💾保存标注 · ⏭保存并下一帧 · 🏷改选中类别 · ↩撤销 · 🗑删选中 · ✖清空框 · 🧊冻结/▶实时 · 标定员 ‖ 📦构建数据集 · 🔍数据体检 · 🚀训练YOLO · 📂数据目录; 画面控件 `tools/gui/yolo_label_widget.py` (拖框/移动/四角缩放/右键删/Ctrl+Z/1-9 选类别); 快捷键 Enter/N/F (**输入框有焦点不抢键**) ②**框永远存原始帧像素坐标**: 在 180°/90° 旋转窗圈选自动换算回来 (实测 0/90/180/270 拖框 ≤3px 落回目标), 两窗共享同一组框, 右窗(旋转)同步可见 ③**数据目录契约** `data/yolo_annot` (data/ 已 .gitignore): classes.txt(行号=class id) · sessions/<会话>/{frames,labels,session.json}(溯源: 相机身份/seq/帧龄/标定员) · annotations.jsonl(追加流水) · dataset/{images,labels}/{train,val}+data.yaml+stats.json(**训练唯一入口**, 硬链接省盘, 样本<8 时 val 复用 train 并显式标注 val_overlap_train) · meta.json; CLI `--init/--build/--check/--stats/--import-yolo-dir` ④**体检能抓错**(反向验证: 注入 class id 越界+中心>1 → 必报 2 错, 恢复 0 误报) ⑤**训练器** `tools/yolo_annot_train.py`: 先体检(有错退出 2) → 基座 auto=现有权重域适应微调 → ultralytics → **训练后真推理验证**(val 抽样检出数/conf) ⑥输入图像窗口同时补齐: 原始+旋转180° **双画面并排**(同一帧源, Qt 原生像素变换) · 链路断流自愈(实测 17s 恢复) · **关窗收口**(客户端 1s 停 + Orin 节点自退 11s) · 竞态收口 `_collect_if_closed`(否则留 10Hz 孤儿客户端) · 拔外接屏/换分辨率自动拉回屏内 ⑦**数据根随输入源分开**: 真机 data/yolo_annot(optical_module) ‖ 仿真 data/yolo_annot_sim(peg/hole/hand) —— 仿真框混进真机 classes.txt 会静默训出语义打架模型 ⑧Orin 节点三修: server_fps 恒 null · 自退看门狗(独立线程+os._exit) · 单实例守卫误判(pgrep 匹配到拉起命令自己的 ssh 命令行 → 只认 argv[0]=python) ⑨**取证全绿**: tools/verify_annot_labeling.py / verify_annot_ui_real.py(真桌面按钮不截断+两窗并排+窗口在屏内+截图) / verify_annot_sim.py(仿真源同样可标定+真机集零污染) / annot_smoke_real.py(真机 8 帧 → **真 GPU 训练跑通** box_loss 3.75→3.03) ⚠️ 诚实缺口: 精度未验证(烟雾框为程序化占位框), 首轮人工标定建议 100~300 张 |
| **v5.6.21** | 09-17 | 🧰 **运行资源维护 + L2 YOLO 模型单次加载 + VS Code 崩溃归因(纠正)** — ①三份 append 证据流上 logrotate(copytruncate 200M×5 压缩), journal 370M→198M, swappiness 60→10, 收回卡死 12h 的 LibreOffice (330MB) ②`ss_yolo_on_real.py` 修掉每 0.5s 重建 YOLO 模型 (改单次加载+预热) ③VS Code 三次 SIGTRAP 崩溃**不是内存不足** (无 oom/oomd, VmHWM 283MB; 17.4GB 是核心转储体积), 实为**打开本仓库工作区才触发** (98,527 文件/.git 1GB) → 重目录排除 + 关硬件加速, 待验证升级 1.138.0 ④归档 ~/zmax_data/ss_remote/20260917_0641 (state 443,224 / proposal 443,197 / bypass 394,628 行) |
| **v5.6.20** | 09-16 | 🎯 **真机图像 → L2 YOLO 旁路检测 (输出只有可视化)** — ①输入对齐: 真机帧 RGB→**BGR** (ultralytics 口径) → L2 既有光模块权重 (hand/peg/hole, peg=光模块, imgsz 480, conf 0.4) ②自检: 同域真图 480² → hand 0.966 · peg 0.957 · hole 0.94 ③唯一出口 = 旁路可视化节点 (真机位姿 + YOLO 标注图 + 三曲线), 零下行; 常驻 ss-yolo-bypass 每 0.5s 取帧 ④真机数据源按"ROS2 服务客户端"设计: /hmi/snapshot [interfaces/srv/HmiSnapshot] + tools/ss_srv_source.py (只调查询类, 绝不碰 command/execute/move 等动作接口) + x86_64 接口包 tools/ros2_interfaces ⑤红线核查: 停掉遗留 orin_shadow (11h) 并 quarantine ⑥阻断如实: 真机图像 0 帧 (realsense 驱动未跑 / foundationpose 空闲) |
| **v5.6.19** | 09-16 | 📷 **旁路实时显示: 真机位姿 + 实时图像 (RealSense 优先)** — ①旁路窗口新增「真机位姿」(TCP+四元数+frame+六关节位置/速度+位置变化率+机器人电源/运行/报警/急停/碰撞) 与「实时图像」(320×240 预览, RealSense 优先/FoundationPose 兜底/多话题状态) ②**修静默失败**: 图像订阅 raw=True 却从未反序列化 → self.img 恒 None (1Hz 定时器 deserialize + 纯 Python PNG 落 cam_rs.png/cam_fp.png) ③只认新鲜帧 (≤5s), 旧图/离线测试图不冒充 ④采集补每话题发布者计数 + robot_status 截断 300→1200 (原截断破坏 JSON) + 稀疏字段回看 12 帧 ⑤现场实况: RealSense 发布者 0 (未装驱动) / FoundationPose 在线但空闲无帧 → 面板如实显示原因, 有帧即显示 |
| **v5.6.18** | 09-16 | 🔀 **📦 数据源节点直接切「仿真/真机」(不加连线) + 真机通道带图像/位姿** — ①撤掉单独 📡 传感器节点及 2 条连线 → 在 📦 metaworld 数据源节点本体做拨钮 (单击拨钮 / 右键菜单 = 切换; 节点上画出 「数据源: 仿真/真机」状态), 双击语义不变 ②切真机 → 真机帧 (TCP 位姿+姿态四元数+frame / 六关节 / 六维力 / 夹爪 / 机器人状态 / 图像) 写 module._bypass_obs + 报新鲜度与缺口 ③图像通道 = /foundationpose/tray_reference/debug_image (vision_tag 在发布; RealSense 发布者 0≈驱动缺; 触觉自定义消息暂不可订), 容器内纯 Python PNG 落盘 + Z700 面板预览 + 三态如实标注 ④采集补发布者计数/机器人状态/关节名/TCP 姿态 ⑤画布 80→79 节点 · 100→99 连线, 旧节点零变化 ⑥tools/verify_src_switch.py 全绿 |
| **v5.6.17** | 09-16 | 🔭 **旁路接控制台 (真机信号源 + 两个观察器)** — ①📡 旁路真机传感器 (数据源层): 双击把画布数据源 **metaworld → 真机旁路** (4060 远程只读订阅 Orin 落盘真机帧: TCP 位姿+四元数/六关节/六维力/夹爪/机器人状态) ②📈 旁路实时可视化 (可视化层): 当前阶段(13段)/残差/接触概率 实时曲线 500ms + 六层调用数 + 零下行自证 + 缺口计数 ③🖥 Z700 真机信号 (可视化层, 输入=🌍物理世界输出): 全部真机信号面板 (TCP/六关节含关节名/六维力/夹爪/触觉/电源运行报警急停碰撞/产线阶段) ④采集侧补 TCP 姿态四元数 + /robot_status + 关节名 ⑤画布 77→80 节点 / 97→100 连线, 旧节点逐字段零变化, 无新增反向·穿框线 ⑥取证 tools/verify_bypass_viz.py 全绿 |
| **v5.6.16** | 09-16 | 🍎 **macOS .app 连挂 5 个 tag 的真根因 = `pipefail` + `ls \| head`** — mac job 日志里下载/解包全成功, 挂在 `ls -la reports/ | head -8` 后的 `ls: stdout: Broken pipe` → exit 1。GitHub Actions bash 步骤带 `-o pipefail`: head 读满即退 → ls 收 SIGPIPE 返回非 0 → 整步失败 (Windows 同一步同时刻成功, 故长期被误判为「静态站对 mac 偶发不可达」)。修 = 该行补 `|| true` (win/mac 两处 job 同步); Windows .exe 不受影响 (v5.6.15 Release 已含 160MB exe, 只缺 mac zip) |
| **v5.6.15** | 09-16 | 🛰 **Orin 零程序 · 只转发感知 (红线整改 + 标定桥真口径)** — 老倪: 「不要在orin上增加新程序, 生产设备不能被干扰, 你先只是转发orin的感知信号」。①**Orin 侧全清**: 自研进程/自启项(crontab @reboot)/ss_edge·ss_shadow·ss_infer 文件全部移除, 生产 8765 网关与 18 节点不受影响 (pgrep 空 / crontab 空 / 域内 zmax_ss 话题 0) ②**采集改走 4060 侧 Docker 远程只读订阅** (ros:humble --network host, ROS_DOMAIN_ID=0): 跨机 DDS 直订 `/robot/tcp_pose`(49.8Hz 真值)+`/real_joint_states`(100.4Hz), 节点自证 endpoint 仅 `/parameter_events`(rosout 已关)=零数据发布; 写回 Orin 的 ss-bridge 停用 ③**标定桥真口径**: z7=[手/头−目标, 手/头−光模块, 夹持], 手/头=真机 tcp_pose, 目标/光模块点由 tools/ss_geom_calib.py 现场示教 (可在 4060 容器内跑); **几何未示教→z7=null 拒算不编造**, input_map 逐条落盘 (现 placeholder_v0, 示教后自动切 tcp_pose_v1) ④tools/ss_archive_remote_data.py 一致性快照+MANIFEST(条数/坏行/跨度/sha256), 数据不进库 ⑤坑留档: /robot/force_torque 同名双类型 (WrenchStamped 建订阅报 invalid allocator→只订 JointState) |
| **v5.6.14** | 09-16 | 🔌 **硬件工具箱真机链路修通 (直连 Orin 局域网)** (补录) — ①直接连通: 本机 USB 千兆网卡(RTL8153)+`/etc/netplan/99-orin-lan.yaml`(192.168.23.50/24, 不设网关→上网仍走 WiFi) 打通 Orin 192.168.23.66(0.5ms), 旧「必绕 ECS/Mac」前提作废 ②发现硬件: `HardwareDiscoveryThread.ORIN_USER` 废弃 `nvidia`→`tashan` + ssh 免密 + 显式 `ROS_DOMAIN_ID=0` → 实跑 18 节点/46 话题/8765 listening ③崩溃修复 (VEH.3.04): `Z700_ROS2_NODES["real"]` 是 list 却调 `.get()` → AttributeError → Qt 槽未捕获异常 = qFatal 整进程中止 → 列表/字典兼容 + `topLevelItem` 判 None + 渲染总闸 ④塔灯改「直连优先 + 回读验证 → relay→Mac 兜底」 ⑤`auto_loop.build_dataset()` 判据改产物 (`data/chunk-000/*.parquet`) ⑥关机前留档: Orin 18 节点体检 + RealSense D405 已接 (`/dev/video0-5`) 但 Orin 未装 `realsense2_camera` ⇒ `/realsense/*` 无发布者 |
| **v5.6.13** | 09-16 | 🚀 **发布链路修复 (Windows .exe + macOS .app 双平台发版)** — 查 tag 构建结果: v5.6.9~v5.6.12 四个 tag **Windows .exe 全部成功、macOS .app 连挂 4 次**, 且全挂在同一行: `Download MLP operation video + pre-extracted frames` (拉 `https://datadrive.world/models/mlp_video_pack.zip`, mac runner 上 5 秒即挂; 同一时刻 Windows job 同一步成功 ⇒ 外部静态站对 mac runner 偶发不可达, 不是代码问题)。演示视频只是**内置素材**, 不该卡整次发版 ⇒ `.github/workflows/build-win-exe.yml` 两个 job 同步改成不致命: ①3 次重试 + 强制 `--http1.1` + `--connect-timeout 20 --max-time 240` ②3 次全失败 → `::warning::` + **占位放行** (写 0 字节 `reports/mlp_insert_success_final.mp4` + `_mlp_cache_mlp_insert_success_final/.placeholder`, 因 PyInstaller `--add-data` 路径必须存在) ③结果写 `$GITHUB_STEP_SUMMARY` (`video_pack=ok` / `missing`) 可溯源 ④YAML 校验 jobs = build(win,13 步) · build-ab-baseline(win,6 步) · build-mac(macos-latest,16 步) |
| **v5.6.12** | 09-16 | 🔎 **前馈 forward「是否真执行」与调试器解耦 (老倪第三次: L4 跑这段还是进不了断点)** — ①`src/lerobot/policies/left_right/state_space/parallel.py::forward` 真身开头加**硬停开关 `SS_FF_BREAK=1`** (照 `ZMAX_DEBUG_BREAK` 惯例; `debugpy.breakpoint()` 且仅 `is_client_connected()` 时触发) → **不依赖断点绑定必停** (spec_from_file_location 时代断点不绑定过的保底手段), 用来把"代码没执行"和"绑定失败"分开 ②真身每 100 次打一行执行证据 `🧠 前馈加速器: MLP 真身执行 #N (域内 … · 守卫 …)` ③引擎进度行 (老倪正在看的那行 `[step/max] 阶段=…`) 末尾直接追加 `· 前馈 MLP真身 N/守卫 M`, 未启用时补 `(未启用: SS_USE_MLP≠1 → forward 被解析覆盖)` — 实测两臂: SS_USE_MLP=1 → `n_mlp=30/30 · 守卫 0`; 不设 → `0/0 (未启用…)` 且 `n_calls=-1` (函数一次没进) ④**诊断口径**: 日志显示 N>0 而断点不停 ⇒ 绑定/断点位置问题 (断点必须设在 `obs = np.asarray(…)` 首行, 不是 `def`/docstring 行 — docstring 无可命中字节码); 显示 0 ⇒ 档位/勾选问题 (须 L4 档 + 勾「🤖 L4 用 INTACT 节点执行」或「🧠 模型执行」; L4 纯演示档走 L4Demo 独立链, 链上没有 FeedforwardAccelerator) |
| **v5.6.11** | 09-16 | 🧩 **L4 档新增勾选框「🧩 L2 兼容 (前馈 MLP + YOLO)」** — 老倪: "L4 档加一个勾选框" (不再靠环境变量): FlowBar 工具栏 (与「🤖 L4 用 INTACT 节点执行」「🎯 L4 意图 → DiT 精炼」同排) 新增 `chk_l2_compat`, **默认勾选**; 勾选 = L4 引擎路径内 `SS_USE_MLP=1` (前馈蒸馏 MLP 真身进 forward, 否则装配期被 `analytic_forward` 覆盖 → 断点没有可命中点) + `vision=True` (R1 YOLO 每帧真检测, 否则日志恒「YOLO 未启动」); tooltip 明写实测代价 (终点 0.42→6.82mm · 墙钟 2.1×) 与等效环境变量 `SS_L4_L2_COMPAT=0`; 装配块主线程读控件 → `self._l2_compat_on` → 判定 `_l4_cap ∧ ¬demo_cap ∧ _l2_compat_on ∧ env≠0`, 日志打印「L2 兼容勾选框 = ✅/⬜」; L2/L3 档不受影响 (零回退); 验证 `tools/verify_l2_compat_checkbox.py` offscreen **11/11** (存在/文字/挂布局 FlowBar/同排/默认勾选/tooltip 500 字含实测代价/点击可切换/装配块读控件/状态参与判定/仍受环境变量约束) |
| **v5.6.10** | 09-16 | 🔌🐛 **L2 兼容默认开 + 六层模块断点绑定修复** — 老倪「YOLO 检测模型接入 L4 怎么还没改好? 这段 forward 在 L4 跑还是进不了断点」: ①上一版 `SS_L4_L2_COMPAT` **默认关** ⇒ L4 跑时 `SS_USE_MLP` 没被设 → 装配期 `state_space_sim_real.py:399` 又把 `accel.forward` 覆盖成 `analytic_forward` ⇒ **断点没有可命中点** ⇒ 改**默认开** (还原: `SS_L4_L2_COMPAT=0`), L4 引擎路径日志打印"L2 兼容已开"+实测代价 ②**真根因 (更隐蔽)**: 引擎 `_load()` 用 `spec_from_file_location` 加载六层模块 (parallel/perception/cognition/safety/execution) → **debugpy 断点不绑定** (函数真执行、日志有输出, VSCode 就是不停; zmax-console 技能「VSCode 断点调试坑 根因⑤」已实证) ⇒ 改 `exec(compile(src, 真实绝对路径, "exec"))` + 注入 `__file__`/`__name__` + 注册 sys.modules, 失败才退回 spec 加载 (不静默降级) ③**实测验证** (gui-venv311): parallel/cognition/execution 类方法 `co_filename` 不在六层目录的 = **0**; exec 加载后跑 20 步 = `实例覆盖 forward=False · forward 指向 FeedforwardAccelerator.forward @ parallel.py · n_mlp=20 (每帧真身) · n_guard=0` ④**看断点必须三条同时满足**: L4 档且勾「🤖 L4 用 INTACT 节点执行」/「🧠 模型执行」(默认 L4 纯演示档走 L4Demo 独立链, 链上没有 FeedforwardAccelerator) + **VSCode F5 启动** (直接 `python studio.py` = 非调试模式, 断点不生效) + 改码后**重启 GUI** |
| **v5.6.9** | 09-16 | 🧩🔌 **L4 档「L2 兼容」接线 (档位内, 默认关) + 画布补线 + 同口径 A/B** — 老倪「运行 L4 时 YOLO 未启动? L2 应该和 L4 兼容, 也要连线」: ①**真因两处** (都定位到行): **vision 只给 L3 档** (`simulink_module.py:11471` `vision=(cap=="L3") and not _model_exec`) → L4 恒 vision=False → 引擎日志分支恒取 "YOLO 未启动"; **前馈 MLP 真身永不进入** (`state_space_sim_real.py:399` 无 SS_USE_MLP 时用实例属性把 `accel.forward` 覆盖成 `analytic_forward`; 实测真身 **0 次且 n_guard=0** ⇒ 不是 D_GUARD/DOMAIN_SIGMA 挡的; 域内真值 d_guard 0.131~0.178 门 0.25 · max\|xn\| 1.583 门 4.0 全过) ②**接线**: 只在 L4+引擎路径 (勾「🤖INTACT 节点执行」/「🧠模型执行」) 档内设 `SS_USE_MLP=1` + `vision=True`(every=1); L4 纯演示档走 L4Demo 独立链不动; 非 L4 档 pop 回原状 = L2/L3 零回退; 开关 `SS_L4_L2_COMPAT=1` (**默认关**) ③**画布连线**: flows/state_space_obs.json 文本级插 2 条 (`📡传感器融合→ssintact` in2 / `⚡前馈加速器→ssintact` in3, 均标 ↩) + ssintact desc 补 **三路入线口径** (in1 数据源 / in2 L2 感知 / in3 L2 执行); 节点 77 不变 · 连线 95→97 · 旧连线逐字段 0 变化; 零回退 `verify_l4_zero_regression`: 档位归属不变 · L2/L3/L4 执行集 55/60/77 逐项不变 · 旧连线 0 丢失 ✅ ④**同口径 A/B** (同解释器 gui-venv311 · seed104 · 120 步 · cap=l4 · 每臂独立进程): 臂A 现状 `n_mlp=0 · YOLO 未启动 · 终点 0.42mm · 3.3s` ‖ 臂B 接线 `n_mlp=120 (每帧真身) · YOLO 240/240 检出 100% · 终点 6.82mm (16× 回退) · 6.9s` ⇒ **接线成功可验证, 但精度回退** (vision 后 YOLO 检测值替换 R0 真值 + MLP 在 seed104 分布边缘) ⇒ 按门槛 **不进默认档** ⑤**画布代价** (同工具改前/改后): 反向 2→4 (新增两条必经) · 重叠 0 · 穿框 44→45 · 交叉 145→161 (+16, 根因 L2 行 x≈3826 在右 · L4 行 x=387) ⇒ 结构性修法 = 重排 L2 行 (下一轮) ⑥新增 `tools/ab_l4_l2_compat.py` + `docs/design/zmax_l4_l2_compat_wiring.md` ⑦**环境发现**: `ultralytics` 只在 **gui-venv311** (8.4.126), 不在 ~/lerobot-venv → 任何用 lerobot-venv 跑 vision=True 的路径会 `ModuleNotFoundError: ultralytics` (A/B 第一遍即因此挂掉) |
| **v5.6.8** | 09-16 | 🧭🎚 **意图李群化 (SU(2)/SE(3)) + 卡尔曼式自适应增益 + 质量闸** — ①新增 `src/lerobot/manifold/lie_intent.py`: SU(2)/SE(3) 运算 (exp∘log 互逆 4.5e-16 · 群合成 1.4e-17) + 接触丛 twist `e=log(T_hole⁻¹·T_peg)` (**孔系表达**, 整体刚体变换后不变 8.4e-17 ⇒ 跨场景可比) + 切空间增益融合 (含旋转走测地线, K=0 逐位零回退) + Φ 标定 (PCA+ridge+LOO 闸), 自检 **13/13** ②Φ 标定 (352 对逐帧真值 · 8 seed · 教师=末端位姿逐帧差分, 无写死几何): **Φ_su2 LOO R² 0.806 / Φ_se3 0.712** (null<0.08); 逐维最强 = 绕插拔轴旋转 ω_z **0.817**, v_x 0.438 / v_z 0.511, 最弱 v_y 0.056 ③横向归因 (`diag_lie_lateral`): 排除"信息缺失"(σ 10.9/4.0mm) 与"非线性"(二次特征不抬升) → 口径+阶段混合: 双路意图 pred⊕goal 抬到 v_x 0.569 / v_y 0.202 / v_z 0.703 ④DiT 真消费: 条件 **214→223 维** (ξ6+ω3 真进 DiT), act_norm 0.1766→0.1625 (响应真实但小 ⇒ 几何 token 需按自身尺度定标) ⑤引擎接线 (`SS_L4_LIE=1`): Δz→ξ/ω 进 DiT 条件 + ξ_v 作导航方向 (幅度按 L2 包络收窄) + 逐帧列 lie_xi/lie_omega ⑥**卡尔曼式自适应增益** (`SS_ADAPT_GAIN=1`): 先验=L2 肌肉记忆/解析伺服, 观测=L4 导航+L3 流程, K=P/(P+R); 事件 (σ超门/无标杆/新息异常/停滞/换段) → Q 抬升, 熟场景 P 落地板 → **K 硬置 0 = 逐位纯 L2** (实测 K 尾 0.0 · 肌肉命中 564/600 帧), 泛化场景 K≈0.34~0.4; **600 步 11 臂 A/B = 无收益** (gain 变体终点 47~51mm vs base 23.4mm) ⇒ opt-in, 不进默认档 ⑦**质量闸** (`SS_QUALITY_GATE=1`): 逐阶段 LOSO 判"上层是否优于 L2" (梯度=孔口−末端; 转移段 cos_L4 0.661 < cos_L2 0.852 → **否决只记录不抬**), 在线取证 allowed_frames=gain.applied ⑧修 bug: `def _aligner` (方法) 与 R1 视觉实例属性 `self._aligner` **撞名** → `SS_L4_ALIGN=1` 必 TypeError (此前未标定从未触发); 改名 `_l4_aligner`; 齐层标定完成 (cos −0.325 → **LOO +0.931**, ready) ⑨yaw 出口 Arm C (`SS_L4_LIE_YAW=1`): SU(2) 残差切空间融合 + 夹持后几何闭环; ⚠️ 插入段 A/B 出**失败证据** (基线插入 49.4/50mm 成功 vs Arm C 后夹持验证失败 Δz 0.7mm → 步数/时序不同「指令等价≠轨迹等价」+ ②段空夹爪 twist 不可控) ⇒ **未验证, 不宣称** ⑩新增工具: `fit_lie_intent` · `fit_lie_quality_gate` · `diag_lie_lateral` · `diag_lie_yaw` · `diag_gain_ab` · `diag_ff_entry` + 设计文档 docs/design/zmax_lie_intent_manifolds.md · zmax_adaptive_gain.md |
| **v5.6.7** | 09-16 | 🧬 **L4 纤维丛联络层 (动作丛→接触丛→DiT) + 方向/幅度对齐层 + 同口径多臂 A/B** — ①INTACT **预测潜空间** z_pred 真进链: 桥白名单原来只透传 z_t/z_goal/delta, 把新导出的键**吃掉** (连接线通了数据却被桥丢, 典型"导出没接手"); 零搜索 direct 规划本不调 predictor → 用模型自己的 predict() 逐位复刻 rollout_one_step 推完 chunk (z_pred/z_pred_last/z_pred_seq) ②新增 `src/lerobot/manifold/fiber_bundle.py`: 丛映射 Φ:z_pred→**接触丛 6 维** (582 条真样本 · 8 seed×900 步 · LOO R² **0.471**, null 0.000) + 丛映射 A:z_pred→几何 z7 (0.607) + 水平提升 h_z = Φ(z_pred)−Φ(z_t) + 挠率 κ=‖h_z−h_geo‖ + 曲率 Ω (线性 Φ 曲率≈0 / 二次 Φ 曲率>0 作对照) ③流形专家预测器**权重一字未改**, z 换源 `ẑ7 = A·z_pred + b` (证据: 预测器 z 来源=fiber) ④DiT 条件 token **214 维** = δ̂192 ⊕ ĥ_z6 ⊕ Φ(z_pred)6 ⊕ Φ_p(z_pred)6 ⊕ 标量4 (同一 apply_l4_cond; 性能丛按口径只作条件 token, 不作幅值权重) ⑤L4 行 **19 条边**审计 (tools/audit_l4_edges.py 只认运行时计数): 有数据 13 · 死线 6 (VLM z960 3 条要重训口径 / L4 记忆 2 条 / 算子A 1 条, 全部明写不糊) ⑥零回退: L2 档 fiber 0/1 **逐位相同** (hash 3d330ebc8b0c19e88cbd0706def2f455) · L3 档新代码根本不进; ⚠️**仪器诚实**: L3 的逐位 hash 不可作判据 (625M CPU bf16 FP 级非确定, 同配置两臂 hash 亦不同, 已复现) → 改用结构性判据 ⑦**同口径多臂 A/B (3 seed×150 步, 每臂独立进程) 结论 = 无实质提升** (fiber=1 vs 基线: rel_V/rel_dist seed0 +1.95%/−36% · seed2 −0.06%/−1.77% · seed5 +0.47%/−3.67%); 判据**自我收紧** 1e-9 → 相对 **1% 实质阈值** (原阈值会把 1e-5 噪声判成"提升") ⇒ **SS_L4_FIBER 保持默认关, 不进默认档** (未证明提升不进默认档); 结构性原因: L2 收口闸 150 帧否决 **79~120 帧** (L4 注入大多没到执行层) + cos∠(预测潜空间,几何联络) 跨阶段 0.80/−0.46/0.22 不稳 + 标定样本自由空间占比大 (插入段在白名单外) ⑧新增工具: `fit_fiber_map` · `audit_l4_edges` · `verify_fiber_zero_regression` · `ab_fiber_line` · `ab_l4_arms` · `feishu_notify` (+ 探针 L4line/L4audit 场景 · np/torch 播种 · 审计 json · 设计文档 docs/design/zmax_l4_fiber_bundle.md) ⑨**未完成 (下轮第一件)**: 方向/幅度对齐层已写 (`src/lerobot/manifold/l4_align.py` 线性映射+锥角截断+幅度封顶 + selftest 通过), 但对齐样本采集 (SS_L4_ALIGN_DATA, 3 seed×120 步) 因单轮耗时超时未落盘 ⇒ `models/l4_align_map.json` **未标定**, 对齐层运行时待验证 |
> ⚠️ **版本历史缺口 (诚实标注)**: v5.6.2~v5.6.6 的条目未写入本表 (逐版明细见 `tools/gui/studio.py` 顶部变更摘要注释)。
| **v5.6.1** | 09-15 | 🎨 **补画布缺线: INTACT 意图解码器 → 流形专家预测器** (老倪: "怎么没有直接连接流形专家预测器节点呢?") — 查证: 这条通路**代码里真实存在、画布漏画** (decoder.py 产 `m_int`, 语义"给流形专家预测器的意图"; 引擎 `state_space_sim_real.py:1380` 取 `d.m_int` → `:1419` 喂 `WorldModelPredictor(z,a,m)`, 对齐 INTACT 四槽语法 m_t) · **活跃口径是 z7** (z=引擎几何潜空间 R7 + m=引擎几何意图 target−peg_head, 由 📐2D→3D 那条线供给) → 该口径下解码器 `m_int` 只当**门控** (w=m_int_weight×SS_L4_INTENT_LINE_W×ready); z_t 口径 (z=INTACT 潜空间192+m=m_int) 才是直连边, 但已被离线证伪 (z_t→流形 LOSO R² 全负) 未启用 · 修: 文本级插入 1 条连线 (t_port=in3, 插在 lkild3 前让水平线走上槽位) + 两端节点 desc 补"哪条入线属哪个口径" (自解释) · **验证**: 真画布 节点 77 · 连线 91→92 · 反向 2→2 · 重叠 0→0 · 穿框 44→44 · 交叉 143→145 (+2, 新线必经) · 零回退 L2/L3/L4 执行集 55/60/77 逐项不变 · 旧连线 94 条 0 丢失 · 档位归属零变化 · 渲染图 reports/canvas_l4_l3_layout_20260915_065526.png · 配套技能: zmax-console 新增「画布边必须与代码通路一致 — 缺线要补 + 标口径」规程 |
| **v5.6.0** | 09-15 | 🐛🎯 **三个真根因 bug + L2 插入成功率 4/12→5/12 + yaw 直连验通** — ①**最隐蔽的评估污染 (所有涉 L4 的 A/B 曾全部失真)**: L2 势场构建 `ObstacleField.from_engine` 在引擎**运行中**又新建第二个 `RealStateSpaceSim` 并 `_reset(104)` 采几何, 而 metaworld 底层 MuJoCo sim **进程内共享** → 正在运行的场景被改写 (实测 peg 瞬移 Δ=[+1.2mm,−17mm,0]); 修: 传现成 geom, 只读不写 (修后闸开逐帧复现解析链 = 收口行为正确) · ②**肌肉记忆跨 run 持久化污染评估** (默认开且每局 save; 热态下 30~65% 执行帧是记忆回放而非实时计算) → 加 `SS_MUSCLE_PATH` 隔离, A/B 默认冷口径 (冷 3/8 vs 热 4/8) · ③**插入卡死真因不是杆是夹爪**: 接触对取证同轴帧 78% 为 rightclaw/rightpad↔治具顶板, 抓取点↔头距离 失败 seed 112~124mm vs 成功 129~132mm(设计130) → 加**抓取点闭环补偿** (离头不足则回退重抓 + 沿杆轴远头平移缺口) → seed3 27.37mm→0.84mm 完成, 成功 seed 逐位不变, **基线 4/12→5/12 零回退** · ④三个"试了没提升"的旋钮 (入口 z 容差收紧/降落受阻回退/滑脱抓取点平移) 实测无收益 → 全部默认关 (未证明提升不进默认档) · ⑤**yaw 直连端到端验通**: L4 演示全链 success=True (φ*=−45.3° 流形决策 · 试抓头真前向 351 次 · 插入 49.6mm · 拔出 56mm · AOI ok 悬停60帧/距焦点7.7mm · 光耦合 η=1.0000) · ⑥**m_stop 交权专家**: 接线 + 判据取证 (专家 mani_risk 不区分成功/卡死; 训 12seed/7398帧 stop_head 局级 OOF AUC 0.80 但答不了"何时停") → 不接线, 保持默认关 · 配套技能: robot-policy-eval-pitfalls ⑰⑱⑲⑳㉑ |
| **v5.5.56** | 09-14 | 🔍🧪 **四个「运行时口径 ≠ 训练口径」根因全部修掉**: ①**记忆条件通道 0×0 死锁** (INTACT-JEPA/train.py): 入口层 skill 新列与 `skill_enc` 末层同时零初始化 → E(s)=0×0 → 反向两侧梯度恒 0 ⇒ 训 3 轮后该层仍 100% 为 0、判闸 on/zero 输出逐位相同 (实测 ∂L/∂新列=∂L/∂末层=0.000e+00, ∂L/∂老列=9.222e-01); 修: 新列改 U(±1/√in_dim) + 末层保持零初始化 ⇒ 暖启动逐位不变但梯度可通 (0→1.165e-01), 新增 `_break_skill_deadlock()` 续训自愈 ②**运行时图像口径** (tools/intact_worker.py): 训练侧 uint8→ToImage /255+ImageNet (实测输入 [-2.118, 2.429]), 运行时把 h5 里 float32 的 0~255 原始像素直接喂 → 尺度差 ~100× ⇒ 编码器退化、动作头恒定带偏移 ("std 比 0.08"+"打不过常数基线", 与训练轮数无关); 修在唯一入口 (自动判量级) ⇒ 同一 ckpt std 比 0.08→**0.61~0.76**、偏置 +0.041→-0.006 ③**判闸哨兵两个真 bug**: 按 epoch 号去重致续训轮次永远判不出 (实锤静默) → 改按 family 去重 + 兼容别名; 取消"只判 1/2 与偶数轮"限流; 修报尾 f-string NameError ④**直驱反归一化口径**: 默认 stats 错用 zmax_action_stats.json (zmax_insert.h5, n=18635) 而 v5/v6 权重是 optical_insert_v5_disturb (n=149100) 训的 ⇒ dx std 0.153 vs 0.074、grip mean 0.120 vs 0.828; 改同源 + `audit_stats()` 不一致直接 SystemExit ⑤配套 `tools/intact_replay_bias_probe.py` (逐维偏置/MAE 分解/pearson + goal 口径对照, 证伪"外来 goal 致塌缩") ⑥**连带作废**: v5/v6/v6r2 历史判闸 ❌、"越训越塌"、直驱 92.2mm 全建立在坏口径上 → 作废留证 ⑦数据侧复核: skill_ctx 非零 3~8 为主 / 共享构造器重算**逐位相同** (0.000e+00) ⑧修后实测: v6r5 ep1 on 0.0402 / zero 0.0389 / std 比 0.71 ⇒ "不塌缩"翻正, 但赢常数/有提升仍 False (400 步 ⇒ **无提升证据, 不声明提升**); 直驱 260 步 0/1 (插入 156.8mm · 真推理 65 次无错) |
| **v5.5.55** | 09-14 | 🎯🧠 **L4 意图→DiT 真接 + L3 模型执行真跑修复 + 断点取证**: 标定硬结论 (13 折 LOSO, z_t→流形 6 维测试 R² 全 ≤0 ⇒ 拒绝写 models/intact_l3_map.json, 不造假映射); decoder l4_cond = INTACT 意图增量 δ=z_goal−z_t 单位向量 (192 维, 无需标定); 融合 u/act=(1−β)·L4+β·DiT (β 默认 0.5); L3 ckpt device_processor 写死 cuda → 无卡时每步 return None 且每步重载 625M ⇒ SS_L3_DEV 可覆盖 + 按运行设备 override + 失败熔断; 实测 L4 24 步 24/24 真跑 · Δact=0.1139/帧 · loss 行=0 |
| **v5.5.54** | 09-14 | 🎯🧠 **L4 运行真接进 policy 层 + skill_ctx 真喂**: 查实两个真因 —— ①L4 跑的是模型直驱 `install_direct_act`, 直接 `node.step()` 绕开 policy 层 → `service.run_once` 断点永不命中; ②v6 权重 (skill_dim=24) **缺 skill_ctx 直接硬闸拒绝** ⇒ 运行路径真推理 0 次。修: 直驱每帧走 `service.run_once(decode=True)` (解码器真执行 + u_ff + 证据) 且**逐帧构造 24 维 skill_ctx** (单一构造器 + L2 `from_real_data` + x=obs[0:3] 坐标红线); service 支持外部节点注入; 修 root 解析跳两级致证据写到 /home/ubuntu/reports 的 bug; 实测真推理 60/60 · 错误无 (原 0 次+报错), u_ff_src=`intact(chunk×K_ACT=0.5)`; L3 条件通道未标定 → 诚实拒绝
| **v5.5.53** | 09-14 | 🎯 **L4 INTACT 调试配置改好 + 稳定权重指针**: 4 个配置原写死上一代轮次 `optical_insert_v4_s3072/weights_epoch_2.pt` → 改指 `checkpoints/intact_l4_current/` (config.json + 单个 weights.pt 软链, 按官方 load_pretrained 文件夹格式); 切换 `bash tools/l4_use_ckpt.sh`; 指针与显式路径**逐位同数**验证; launch.json 与 GUI 生成模板两处同步 (右键重写会覆盖); 新增 `tools/check_debug_cfg.py` 自检 PASS; 顺带修引擎 L4 档默认权重 (11385) 同类写死 + 把写死的判闸标注改动态
| **v5.5.52** | 09-14 | 🧹 **磁盘压回红线内 + 守护 v4.1**: 系统盘 307G>300G 红线 → 手动清被取代的旧代数据集 (`optical_insert_v3.h5` 4.9G sha `3c8becc3…` / `v4.h5` 6.8G sha `98eda89d…`) + 旧代/无效链权重目录 + v5 中间轮 ep1/ep3 ⇒ **307G→294G**; 引用 v4.h5 的 v5 判闸 cron 先 pause; 守护脚本加 `--dry-run`、INTACT 权重"只留最后轮+保护暖启动源"、在跑目录改按 train.py cmdline 判定 (原 mtime 口径会误判刚清理过的目录); 台账 `reports/disk_cleanup_ledger_20260914.json`; 保护区(disturb 数据/暖启动源/在跑目录/官方数据集)复核全在, 训练零中断
| **v5.5.51** | 09-14 | 💾 **数据保存 + 关机收口**: 新抗干扰数据集 `optical_insert_v5_disturb.h5` (7.37GB / 149,100 帧 / 2,982 窗口 / sha256 d3831406…) + 数据卡 (规模/口径/干扰分档 0.90·0.78·0.60/溯源) + `v6_resume.sh` 可续训脚本 (自动换轮次名) + `V6_RESUME.md` + 判闸哨兵自动认最新 v6* 目录
| **v5.5.50** | 09-14 | 🛡 **画布补线: 安全执行边界 → 全部原子技能 + 通用算子**: 原 `sslimit` 零出线、②对位~⑧完成 零入线 (断链)。修: `sslimit` 移到原子层行首 + 11 条「🛡限幅后控制」前向出线 (逆向 0), 并给 ssa/ssb/ssc/sssk1 补 in2 端口。体检: L2/L3/L4 执行集 55/60/77 不变 · 83 条原连线零丢失 · 重叠 0; 代价: 逆向 2→3 · 穿框 39→48 · 交叉 110→133
| **v5.5.49** | 09-14 | 🧠 **L4 看到/复用 L2 原子技能 + 抗干扰数据 v5**: skill_ctx 24 维契约 (相位13+L2技能权重8+d_perp+arc_frac+grip) 单源共用; `IntentActionActor(skill_dim=24)` 零回退扩展 (默认0形状不变 / 末层零初始化 + 入口层老列复制新列置零 → 暖启动逐位等价 0.0 差); 新采集器三档真干扰 (light/med/heavy) + success-only + 逐帧 skill_ctx; 判闸用同权重 on/zero 消融证『有提升』; 修 project_polyline 4元组解包坑 (曾被 sink 静默吞掉 → 整列 skill_ctx 缺失)
| **v5.5.48** | 09-14 | 🐛🎯 **记忆层喂错坐标系 (根因) + 夹爪通道 + 纯场探针**: 桥原来喂 `peg_head()` 给势场, 而冠军轨迹/引擎肌肉记忆用**夹爪真实位置** (`self.x = obs[0:3]`) → 差 176mm(z) ⇒ 场在自己坐标系外求梯度、意图=噪声。改用 obs[0:3] 后 d_perp 0.1358→0.0002~0.02m、相位 SK01→SK07 正常推进 (探针硬证据)。另修: 夹爪通道原来完全不混 (纯场驱动夹爪永不闭合 → 光模块抓不起来), 现按弧长取冠军 champ_u[3] 同权重混入。遗留: 抓取咬合时序 (相位切换太快) → 下一步夹爪粘滞/节拍
| **v5.5.47** | 09-14 | 🧲 **记忆层: 场权增益调度 + 阶梯台数据一致性修复**: 第一版 30 格实测 (模型直驱 0/30 · 记忆层介入 3000/3000 步但 w̄ 恒 0.1 · 与 off 差异仅噪声级) 定位两条根因 — ① 场权写死 0.1 → 改增益调度 `w=max(w_max·max(conf,w_floor), w_far·far)`, far 远场场主导 (w_far=0.85/d_near=30mm/d_far=150mm, 管内原公式不变=不回退) ② 模型动作塌缩: actor 无本体/几何输入, 数据集 39D observation 从未被消费。另修**数据一致性真 bug**: 记忆状态文件(muscle/assembly/shared)是可变状态 → 同 seed 解析链 65.26→58.02 漂移; 现纳入 manifest + 每格复位到快照 + 状态留档。新增 docs/design/zmax_memory_integration.md
| **v5.5.46** | 09-14 | 🧲📊 **记忆层集成阶梯 (L2 准确性 → L3 调度 → L4 抗干扰 → 总装仲裁)**: 桥新增 `--cap {l2,l3,l4}` 并透传 run(cap=…) — **原来不传 cap ⇒ 引擎从未注入干扰**, 现在 l4 档真注入来料移位/转向 (±3.5cm/±15°/90°转台) + 恢复预算×2, 解析链与模型同吃一份干扰 (同口径) · 新工具 `tools/mem_ladder_integration.py`: 数据一致性 manifest (权重/stats/代码/开关 全 sha256) + 5 臂×2 干扰档×N seed 可续跑矩阵 + 6 道不后退闸 + append-only 历史 CSV · 无人值守哨兵 `mem_ladder_watch.py` (每 20min, 崩溃报告/自动重启/结论推飞书) · 冒烟: 真干扰注入 + L2 介入 60/60 步 w̄=0.1 + 零搜索 · 已启动 30 格全量阶梯
| **v5.5.45** | 09-13 | 🔬 **INTACT L4 调试配置 (VSCode) + 模型侧单步驱动器**: 新增 4 条 launch 配置 (policy 层 E2E / GUI 节点路径 / 模型侧 in-process (INTACT venv) / 光模块插拔链) · **模板与文件同步** (右键会重写 launch.json, 新配置必须写进 open_in_vscode 模板, 并有回归断言 7 条全在) · 新工具 `tools/intact_worker_debug.py` (INTACT venv 里 in-process 起 Runtime, 重放真输入) + `INTACT_KEEP_INPUT=1` 留档真实输入 reports/intact_last_input.npz · 实测: 留档 2.4MB → 加载 5.3s · trained=True · act 0.1s · actions(1,8,8) std=0.3853
| **v5.5.44** | 09-13 | 🐛 **修「右键 VSCode 打不开真实源码, 还停在原来的 GUI」**: 根因 = `get_node_location()` 对未登记 `_EXTERNAL_LOC` 的键退回 node_logic.py 自身 + `open_in_vscode()` 不看节点 `params.source` · 修法 = 节点 params.source 优先 (可选 source_symbol 动态定位, 不写死行号) + 登记 INTACT 家族/光模块链映射 + 流里两节点 source 改指 `src/lerobot/policies/intact/service.py` · 实测 `code -g .../policies/intact/service.py:209` (工具 tools/verify_vscode_source_loc.py, 4/4 PASS)
| **v5.5.43** | 09-13 | 🏗 **L4→L3 意图编排下沉 policy 层 (桥接复用, 不抄代码)**: 新增 `src/lerobot/policies/intact/service.py` (IntactIntentService/IntentReport/get_service) — 建桥+接数据源+真推理+解码+证据落盘+日志文本 全部搬进 policy 层, GUI 只剩瘦调用 (~90 行 → ~15 行) · 沿用 /home/ubuntu/INTACT-JEPA 一字不改 (拷代码=依赖冲突 + paper_runtime 冻结运行时 + 实现漂移) · 修 INTACT_DEVICE=cpu 被 adapter --device 默认值覆盖 · 补 ensure_goal → goal_src 如实标注 · **E2E 只走 policy 层**: trained=True · chunk(8,8) · 零搜索 · u_ff 4D · L3 条件未标定诚实拒绝 · 断言 6/6
| **v5.5.42** | 09-13 | 🎨 **L4 层并成一层 + 下游依次右移 + 连线整齐**: L4 五个节点统一一行 (INTACT策略 40 → 意图解码器 396 → 流形专家预测器 752 → 接触流形 1088 → 性能流形 1424, 56px 等距; 行带 360→170 高) · 下游依次右移 (DiT→1810 / 前馈→2160 / 状态机→3520 / 执行器→4800 / 物理世界→5140 / 验证·可视化→5500+) · VLM→396 与 43D obs→1048 顺带消掉 4 条反向线 · 连线数组按来向全局重排 (端口 slot 单调) + DiT 输入端口语义化 · 真画布取证: **反向连线 13→2** (余 2 条 = 语义闭环回流 ↩), **方框重叠 1→0**, 交叉 130→110 · 零回退: L2/L3 执行集逐 id 不变, 连线拓扑零丢失
| **v5.5.41** | 09-13 | 🎨 **L4 INTACT 区 UI 重排 + 重新连线**: 按画布自身排版规则(节点最小 280×110 · 同行 56px 间隔 · 端口=源右缘→目标左缘)反推坐标 — L4 行加高 180→360 三通道, 下方行带整体 +180px (档位判定零变化) · ssdec x 700→980 / ssff 1140→1290 让解码器+流形三路输出全部前向进 DiT · 真画布取证: **解码器→DiT 由倒退 300px → 前向 84px**, 方框零重叠 · 余 13 条反向线全在 L2 固有区 (SK→执行器 8 + 闭环回流 2 + 旧遗留 3)
| **v5.5.40** | 09-13 | 🎯 **L4 INTACT 策略化 + 连线 (metaworld → INTACT → decoder → L3)**: 代码整体迁移到 src/lerobot/policies/intact/ (配置/策略/解码器 + runtime/ 真算法; 旧路径兼容转发) · 数据源直连 metaworld (新 MetaWorldSource, 真渲染帧 224² + 39D) · 新解码器 (u_ff 先验 act×K_ACT 无需标定; L3 流形条件需标定, 未标定拒绝+计数) · 新节点「INTACT 意图解码器」+ 3 连线进 L3 · 引擎 SS_L4_INTACT 三档 + 全计数取证 · **零回退证明**: L2 档 55 / L3 档 60 节点逐 id 不变 · 节点级真跑 chunk(8,8) 零搜索 1396ms/步 · 修 goal 帧缺失 (引擎直喂帧路径每帧报错 → ensure_goal 三级兜底, 兜不到才显式报错)
| **v5.5.39** | 09-13 | 势场收敛修复 (λ∝k_att·σ² + 锥形项 k_lin → 98~127 步收敛 <1mm) · 覆盖闸 d_max · 相位按状态判 · 新增逐层对照工具 mem_layer_ablation.py · 验证 26/26 |
| **v5.5.38** | 09-13 | 🧲 **分层记忆势场**: L2 技能势场(冠军轨迹 Φ_SK) / L3 流程势场(Σw_k(t), 谷底按时序移动) / L4 全局势场(+孔壁障碍+世界模型项) / 总装机记忆联络(逐层开关 data/memory_layers.json, 默认全关=零回退, 跨层仲裁+台账) · L4 INTACT 链按层混入 −∇Φ 意图(blend_action) · 新画布节点「🧲 总装机记忆·势场联络」。实测势场 26/26 (真数据) · e2e 全关 0 介入/开 L2 真介入。发现 muscle_memory io.entry/exit 与 champ_x 锚点不同源(15~145mm) |
| **v5.5.37** | 09-13 | 🌍 **L4 光模块插拔链**: 把红方块抓取改造成光模块抓取插拔 —— L4 链条任务化(data/intact_sw_task.json, 默认 optical_insert = Z-MAX 引擎真物理 + 本域微调权重) · 新桥 intact_sw_optical_bridge.py(跨 venv: 引擎 gui-venv311 + 模型 INTACT venv) · u 口径反变换 · 新工具 action_stats_from_h5.py · 修 status 竞态(旧终态误判) · 查看器补阶段/深度/env动作。实测节点级 11/11: 1800 帧 1800 次真推理 解析链 2/2=100%(插入 65mm/全链) 模型直驱 0/2(过冲→诚实标注) |
| **v5.5.36** | 09-13 | 🧭 3D 视图去画中画 → 三个 dreamview 窗口 (L2 / L3 / L4·stable-world); 档位预设贯通 DreamView3D(level=)/open_ss_3d(level=); 验证 10/10 |
| **v5.5.35** | 09-13 | 🐛 修「点 SW实况窗口没反应」: 按钮回调连到 DreamView3D 的同名方法 → 构造期 AttributeError; 补 SWLiveWindow._open_viewer; 加真显示全按钮点击回归 |
| **v5.5.34** | 09-13 | 🎛 L4·SW 互动查看器 (像 dreamview 一样拖帧看任意帧信号): 时间轴/单步/播放 + 信号表 (模型动作[0..3]/std/done/推理次数) + 4 条动作曲线带游标; 数据源自动扫描 + status.jsonl 逐帧信号; 验证 11/11 |
| **v5.5.33** | 09-13 | 🐛 修「实况窗口画面不动」: L4 ▶运行 只弹窗没启动桥 → 现在弹窗同时真启动 SW 引擎链 (已在跑则复用); 端到端验证 7/7 (帧号 step_000000→step_000001 = 画面在动) |
| **v5.5.32** | 09-13 | 🎬 L4 档 ▶运行 自动弹出「SW 实况」独立窗口 (L2/L3 不动; 全局单例与 3D 小窗 ⤢ 共用; 画布日志留痕); 验证 7/7 |
| **v5.5.31** | 09-13 | 🎬 SW 实况独立窗口 (小窗太小 → 正常窗口): SWLiveWindow 可拉伸 760×860 · 倍率 ×1~×4 (默认 ×3=672px) · ⏸暂停 · 📌置顶 · 📂视频目录; 两个入口 (3D 左侧按钮 / 小窗 ⤢ 放大); 单例; 验证 15/15 |
| **v5.5.30** | 09-13 | 🎬 A: stable-world 渲染帧贴进 3D 视图本体 — 3D 视图右上角「SW 实况」画中画 (逐帧真图 150ms 轮询 + 状态行真值 + 图层勾选 + 随窗重贴 + 打开视频目录); 数据源 = L4「🌍 SW 仿真世界引擎链」产物 reports/intact_sw/; 验证 9/9 (真帧 std=32.46, 关/开 生效, 小窗在视口内, 整窗 grab 非全黑) |
| **v5.5.29** | 09-13 | 🐛 修「启动状态空间即崩」: 背景行标题省略号传给 QFontMetrics.elidedText 的宽度是 float → paint() 内 TypeError → Qt Abort 整个 GUI; `_wrap_title` 入口 int(avail) + paint 传 _aw; 单元对照 int/float + 背景行真实 paint + 整画布 render 三重验证 |
| **v5.5.28** | 09-13 | 🌍 L4 · SW 仿真世界引擎链 — 独立链条 4 节点 (数据源渲染图 → INTACT策略·cube → stable-world 引擎 → SW渲染视频) + L4 色带自动触发 (只增不改, L2/L3 零影响) + 跨 venv 子进程桥 tools/intact_sw_bridge.py (与 ②debug 同源, 逐帧流式: 52 帧 std=30.26 · 动作真下发 env.step · 模型真调用 52 次零搜索 · 官方 save_panel_videos 3 面板视频) |
| **v5.5.27** | 09-12 | 🎨 画布节点 UI 统一优化 (老倪: 字号/字数/不挤不裁) — 根因 `QFont("Arial")` 在本机解析成 Liberation Sans (无中文字形→逐字回退) + 标题 9/8/7 自适应 = 大小不一; 统一为 Noto Sans CJK SC + 固定 9pt Bold/8pt + 最多两行 + 超出省略号(悬停看全名) + 自动撑宽 (state_space_obs.json 70 节点实测: 撑宽后单行 70/70 · 两行 0 · 省略 0) |
| **v5.5.26** | 09-12 | 🤖 INTACT→L4 继承: L4 档把控制权交给 INTACT 节点 (install_direct_act 直驱 · 真推理真下发 · 任务未完成属模型能力不足, 诚实标注) + 数据集页重构 (纯数据 UI: 本地数据总表/详情/数据日志; 训练结果搬到训练台) + 数据集查看器修复 (h5 真帧 · 真实回合帧数 · 翻帧 · 可缩放 · 翻转开关) + INTACT 标准机器人切换 (reacher/pusht/cube/tworoom · paper_runtime+prior_only 口径; tworoom 100% / cube 83.3%) |
| **v5.5.25** | 09-11 | 🎯 **① 来料角随机化 (60~120°) + 试抓头泛化验证 (含诚实边界)** — ①`stage_turntable90(target_deg)` + env `SS_L4_TT_DEG` + CLI `--tt-deg` (来料角可配, 默认 90) ②探针扩到多来料角 (`--tts`): 5 角 × 13 候选 × 2 布局 = **130 次真实试抓**, 成功 45/130 → **正确角随来料角移动** (60°→成功区 -90~-15°; 90~105°→-60~0°; **φ=+90° 恒败** = 脚本固定角) ③**留一角度**交叉验证 (`train_yaw_head.py --group-key tt`): 精确"最优角一模一样"仅 1/5, 但**操作性判据 (预测角在实测里真能夹住)**: `argmax P(成功)` **5/5 ✅** vs `argmax 预测Δz` 仅 3/5 (60°/75° 选错) → 执行器裁决规则改为 **P(成功) 为主 + 0.25·预测Δz** ④**端到端未见角** tt=72°/108° (训练网格外): ② 试抓头 φ*=-45.3° (27 决策 / 351 次前向 / 权重 v2) → 真实夹持 Δz≈12mm → ③④⑤⑥⑦⑧ 全绿 **success=True ×2** ⑤**诚实边界 (必须说清)**: φ=-45° 在**全部 12 个测试来料角**下都可行 → 固定 -45° 也能过 ⇒ 现有证据**不足以声称"自适应"** (头 v2 输出恒为 -45.3°, 区分不出"学到万能角"与"学到自适应"); 已明确下一步验证条件 = 制造 -45° 不可行的工况 (更极端长宽比 / 来料姿态+位置组合偏移 / 决策移到插入相位) ⑥新增 `tools/eval_yaw_head_ops.py` (操作性判据核算); 权重 v2 随包 (CI md5 6a6915b3…) |
| **v5.5.24** | 09-11 | 🎯 **yaw 试抓头 (act_dim 4→5) — φ* 成为真正的最优对准角; L4 零件改矩形截面 40×16mm (真实光模块)** — 老倪: "把 yaw 纳入预测器输入重训, 用候选角+真实试抓成败做监督, 让 φ* 从边界值变成真正的最优对准角"。①**先证伪**: metaworld 原 30×30mm **方截面**件 → 抓/插对 yaw 免疫 (28 次真实试验 全成功, 无最优角可学); `tools/rect_sweep.py` 5 候选×抓0/90°×插0/90° 扫描 → 定 **40×16mm**: 抓 0° 成功 (Δz 105mm) / 抓 90° **物理失败** (指间开口不足) / 插入 0°/90° 均成功 = 有区分度且不回退 ②**② 段夹持判据改真实抬升试探** (原来闭夹后无条件建刚性锁 → 成功恒真 = 假成功): 锁之前抬 12mm, 模块随动 >6mm 才算夹住, 否则真失败中止 ③**探针数据集** `tools/yaw_grasp_probe.py`: 3 布局 × 13 候选角 = 39 次真实试抓 → 成功 12/39, 最优区 φ≈0° (3/3), φ=±90° 全败 ④**新增 yaw 试抓头** `src/lerobot/manifold/yaw_head.py` (输入 z7 + 4D 动作 + **候选角** = act_dim 4→5; 输出预测 Δz + P(成功)) + `tools/train_yaw_head.py` (留一布局交叉验证): 训练 acc 0.949 / 留一 0.92·0.92·0.85, **预测最优角 3/3 命中实测最优角**; 执行器 `yaw_actuator.py` 支持 `grasp_head` 打分 (候选搜索放开全周 ±90°) ⑤**实测默认链 (seed 0/2/5/7)**: ② yaw 指令 **-0.3°** (试抓头决策, 13 次前向, 权重溯源) → 真实夹持 Δz≈12mm → 抬起 0.122m → ③④⑤⑥⑦⑧ 全绿 (插入 49.0-49.8mm / 拔出 56-59mm / AOI PASS / η=1.0); **脚本固定 +90° 基线: ② 夹持验证 0.1mm → 物理失败中止** (角度已 load-bearing) ⑥3D 面板来源行 = 「🎯 yaw 试抓头决策 (真实试抓监督)」; 权重随包 (CI 从 ECS 下载 + md5 校验 + 校验门) |
| **v5.5.23** | 09-11 | 🧠 **L4 默认改用流形预测指令** (老倪: "3D 显示脚本开环 Arm A / 下发 yaw +0.0 不能接受, 必须用真实的流形预测的指令") — ①「🧠 流形 yaw 执行」**默认勾选**: L4 演示档 ② 段夹爪偏航角逐帧由流形预测器决策 (每帧真调 `WorldModelPredictor(z7+a4→z'→流形6维)` → 候选角打分 → φ* → 下发角), 3D 面板来源行 = 「🧠 yaw 指令来源: 🧠 流形预测器决策」+ φ* / 前向次数 / trained ②**L4 自动视频导出同步带 `--mani-yaw`** (视频与 3D 同源, 不再出现"脚本开环"视频) ③脚本开环 Arm A 降级为**取消勾选时的对照回退** ④诚实标注保留 (面板/tooltip): v5 预测器在候选编码下代价单调退化 (argmin 落候选边界) + ② 段 yaw 不 load-bearing (两臂任务结果相同 6/6) — 修打分退化需下阶段 (yaw 纳入预测器输入 act_dim 4→5) |
| **v5.5.22** | 09-11 | 🧭 **3D 里「yaw 指令来源」可见 ((A) 方案)** — 老倪: "现在我能看到 3D 视频的操作是实际的流形预测发出的动作指令么?" → 此前不能 (默认档连预测器都没加载, mani_pred 是 0 占位, 3D 无来源标注)。本次: ①画布工具栏加「🧠 流形 yaw 执行」勾选 (L4 演示档 ② 段 yaw 由流形预测器逐帧决策 = Arm B; 不勾 = 脚本开环 Arm A 默认, 动作行为与 v5.5.20 一致) ②**3D 面板新增两行**: `🧠 yaw 指令来源: 流形预测器决策(每帧真调 φ*→下发角) / 脚本开环 Arm A(固定计划角) / 引擎解析链(metaworld 4D 无 yaw 维)` + `下发 yaw xx° · φ*(预测器) xx° · 预测器前向 N 次 trained=xx` ③**流形预测通道默认开** (SS_MANI_PRED=1): `gen_l4_demo_video` 里 `mani_pred` 由 0 占位改为**真实前向输出**, tr 新增 `mani_yaw`(下发角)/`mani_phi`(决策角 φ*) 逐帧真值 → 3D/总线/L 面板都能逐帧核对 ④**打包版带真权重**: CI 从 `https://datadrive.world/models/l4_mani_predictor_v5.pt` 下载(md5 4acf227d… 校验) → `--add-data models/` + 构建后校验权重在包内(缺即 fail) — 否则打包版 trained=False 只能给随机对照 ⑤GUI 走主线程读控件 (worker 禁碰 QObject — 崩溃铁律) ⑥默认零回退: 不勾 = Arm A 脚本链, 与 v5.5.20 完全一致 |
| **v5.5.21** | 09-11 | 🧠 **L4 夹爪 yaw 指令改由流形预测器决策 (A/B 接线 + 诚实负结果)** — 老倪问"L4 的夹爪旋转指令是谁发的? 是流形预测节点么?" → 答: 此前不是 (L4Demo 脚本开环固定角)。本次把预测器真正接进指令链并出 A/B 实证: ①新增 `src/lerobot/manifold/yaw_actuator.py` (**断点可进** `ManifoldYawActuator.decide`): 每帧真调 `WorldModelPredictor(z7+a4→z'→流形 6 维)`, 候选偏航角 (默认 φ_ref±45°, 15° 步长) 逐一代价打分取最小 → 下发 (slew 0.03 rad/步, 与脚本臂同速); 候选-姿态假设编码 = "残余失配 δφ=(来料朝向−φ) 绕z旋转相对几何"(工程假设, 已标注); 权重重 `SS_MANI_YAW_W_*` ②`gen_l4_demo_video.py --mani-yaw` (Arm B) + 预测器真实加载 (权重 `models/l4_mani_predictor_v5.pt`, trained 标注) + `tr["mani_yaw"]` 列 + **`mani_pred` 由 0 占位改为真实前向输出** ③`tools/ab_mani_yaw.py` 同口径 A/B 脚本 (3 seed×2 重复) ④**实测: 两臂 6/6 全成功** — Arm A(脚本) yaw=+89.4° 固定 / Arm B(预测器) yaw=**-44.7°** (决策 26 次/前向 182 次/轮, trained=True), 插入 49.1 vs 49.4mm · 拔出 54-59mm · AOI 全 PASS · η 全 1.0 → **差异只在 yaw 指令本身, 不在任务结果** ⑤**诚实负结果**: 该链路 ② 段 yaw 不 load-bearing (③ 治具回正 + 刚性锁掩蔽, -44.7° 偏 45° 仍全链成功); 且 v5 预测器在该候选编码下代价**单调退化** (实测 +45.3°→-44.7° 代价 0.168→0.0955 单调降 → argmin 恒落候选边界 = 选择无对准信息; 根因: 预测器输入无 yaw 维 act_dim=4 + 训练分布未覆盖 yaw 变化) → **不声明增益, L4 默认档保持 Arm A 脚本开环 (零回退风险)**; 后续方向: (a) yaw 纳入预测器输入 (act_dim 4→5) 或单独训 yaw-conditioned 打分头 (b) 把决策放到 load-bearing 位置 (④ 试抓/插入相位, 用真实试抓成败做监督) ⑥日志证据纠偏: ② 段原写死 "夹爪 yaw=90°" → 改打印**实际下发角 + 臂别** (A/B 下实测与事实一致) ⑦视频: `l4_armA_scripted.mp4` / `l4_armB_mani_yaw.mp4` (同链路同视角, 差异只在 ② 段转的角度) |
| **v5.5.20** | 09-11 | 🏆 **L4 档干扰动作渲染修复 (老倪最高优先级: "L4 没有干扰旋转, 必须渲染出来")**: ①根因 (静态核实+实测): L4 档 09-11 改走引擎真链路, 但 metaworld stock XML **无 `shell_yaw` 关节** → `_inject_peg_jitter` 的"体壳水平转 90°"被 try/except 静默吞掉 (no-op), 物理只转可成功域 ±15° 且发生在第 0 帧之前 → 画面上没有任何旋转动作 = 与 L3 无差别 ②修复: L4 档回到 **L4Demo 真机构链** — 来料转台 `tt_yaw` 关节 100 帧 0→90° 连续转动 + 光模块随治具同步真物理转 90° + 夹爪绕z姿态适配抓横放模块 + 治具回正 + 标准抓取 + 插入49mm + 拔出56mm + AOI + 光耦合η; 渲染帧与 3D 双可见 (**实测 success=True · 1641 帧 · 29s 本机复现**, peg_yaw 0→97° / hand_yaw 0→96° / tt_yaw 0→1.6rad) ③**打包根因 (Windows/macOS 发布版 L4 从来出不来干扰)**: `gen_l4_demo_video.py L4_XML` 与 `gen_l4_demo_scene.py MW_ASSETS` **硬编码 `~/lerobot-smolvla-lew/gui-venv311/...`** → 打包版一律找不到 L4 场景 XML (无转台/耦合台 = 无干扰机构) → 改按 metaworld 包实际位置解析 (frozen `_MEIPASS` 兼容) ④CI 双平台: 构建前预生成 L4 场景 XML (`gen_l4_demo_scene.py --peg-real-inertia`) + 构建后校验 package 内 metaworld stock XML 与 L4 XML 在位 (**缺即 fail, 防回归**) ⑤`ensure_scene()` frozen 跳过 (sys.executable=app 二进制起新实例坑) ⑥**L3 档零改动** |
| **v5.5.19** | 09-11 | **Windows/Mac 修复: metaworld XML 资产未打包 (点运行报 XML 不存在 + 无轨迹)**: ①根因: metaworld 运行时用相对路径读 assets/sawyer_xyz/*.xml (非 importlib.resources), pyinstaller --collect-all 只收 Python 模块收不到数据资产 → exe 解包目录缺 metaworld/assets/sawyer_xyz/sawyer_peg_insertion_side.xml → env 创建失败 → 无轨迹/动画 ②修复: 双平台加 Resolve 步骤解析 metaworld assets 路径 + pyinstaller --add-data assets→metaworld/assets (361 文件 37.9MB) |
| **v5.5.19** | 09-11 | 🏆 **L4 档全链修复 (模型接管 + 干扰可见) + INTACT 二态意图落地**: ①**L4 档改走引擎真链路** (原 `demo_l4=True` 委托 L4Demo 演示 → 引擎六层控制器/模型/流形/意图**全被绕过**, 且演示链无"拔出/AOI" → 老倪报"L4 档没有拔出光模块"+"动画一直是老样子"); 现 L4 = 引擎 full 13 段 (插→拔→AOI检测→放回) + L4 干扰注入 + R0 真值 ②**默认 ckpt 校正**: v8/030000 (旧) → v10_1h/004000 (已验证), 且启动**打印实际模型路径** (让"跑通的模型"与"运行时模型"可核对归因) ③**L4 干扰可视化三层修复** (老倪"没看到光模块旋转角度"): 引擎 `_shell90=False→True` (视觉表达被显式关掉) / 引擎补写 `tr["tt_yaw"]` + `meta.demo_geom` / 3D `_apply_meta` 读 demo_geom **脱离 `meta["demo"]` 分支** → 转台盘+白色十字刻度随 yaw 旋转可见 ④**INTACT 二态意图层** (`src/lerobot/manifold/intent_pair.py`): m_local≡−e_par(接触流形切向, attached) / m_goal≡−∇V_p(性能流形梯度, stop-gradient), 共享算子 `SharedIntentEncoder`, 引擎 L4 每帧真算 (343 帧零回退; 各阶段同构 cos: 自由空间 +0.93 平行 / 接触约束转负 = 分工) + 技术路线文档 `docs/design/zmax_intact_alignment.md` ⑤**阶段2 恰当动作似然头** (`likelihood_head.py`: 混合高斯 + 行为对齐 + 条件动作商一致性): **②非对称梯度硬核验通过** (∂L/∂m: 局部 0.3379 / 目标停梯度 **0.0000** / 目标不停 0.2625) ⑥「🧠 模型执行」开关 (L4 档可选, 默认关): 实测模型在 L4 干扰布局下**卡在插入段** (训练数据无干扰布局 → 数据覆盖问题, 非代码问题) ⑦**实测全链**: 862 步 · 完成=True · 插入113/拔出164/AOI检测135 · **AOI PASS** |
| **v5.5.18** | 09-11 | 🏆 **模型接管闭环成功 90% (18/20) — 撤回此前错误结论**: ①真凶: v5.5.13 批量替换渲染调用时把 `_render_frame` 内部也替换成 self._render_frame() → **无限递归** → RecursionError 被 except 吞 → **永远返回黑帧** → L3 模型推理拿到全黑图 (模型被蒙眼) → 一切闭环失败均为假象 ②修复 (另一会话定位): 改用 getattr(env,'render') 真渲染 ③修复后实测 v10_r2 模型 (DAgger R2 训): **18/20 = 90%** (10 seed × 2 重复, 9 个 seed 2/2, 步数与解析链完全一致 343/370/385/346/355/390/378/387/386 — 稳定可重复), 仅 seed 105 失败 ④**撤回**此前 v5.5.16/17 的「模型不可行/0-15/DAgger无效/L3L4无增益」结论 — 均基于黑帧假数据 ⑤L3L4 联合 & 视觉版结论同样需在正确渲染下重验 ⑥教训: 批量替换后必须全量语法+行为验证 |
| **v5.5.17** | 09-10 | 🔬 **L3L4 联合训练实证 (负结果留档) + 夹持态回退守卫**: ①L3L4 真联合 A/B 消融 (共享编码器: 动作头+流形头, 损失=动作MSE+0.3·流形MSE, 3705 帧 14 成功 seed 专家轨迹): A(仅动作) act_MAE=0.00828 vs B(联合) 0.00878 → **流形辅助无增益** (流形 6 维是 obs 几何的确定性函数 = 信息冗余, 还分散编码器容量) ②关键洞察: 离线动作精度 MAE 0.008 (学得会) 但闭环 0/15 → 瓶颈是闭环分布偏移+布局泛化, 非离线数据/辅助任务可解 (需 RL 闭环或真机数据) ③模型路线累计验证: v8/DAgger R1/v10_fast/v10_1h 闭环 0/6~0/15, L3L4 联合无增益 → 当前条件下不建议继续离线训练投入 ④夹持态回退守卫 (已夹持禁回对位防追漂移目标死循环, seed80 实锤) 已入 v5.5.13/14 |
| **v5.5.16** | 09-10 | ⚠️ **L3 模型真执行单次成功但可靠性复核证伪 (诚实记录)**: ①初次单跑 seed104 等 5/6 成功 → 疑似追平解析链 ②**多重复复核 (5 seed × 3 重复 = 15 次)**: 模型 0/15 = 0%; 解析链同口径 **15/15 = 100%** (且步数完全一致 385/385/385 等) ③结论: 之前 5/6 是特定进程布局下的单次侥幸 (metaworld 每进程布局不同); 模型对布局极不鲁棒 = 不如解析链的根本原因 (解析链=规则自适应任何布局, 模型=模仿动作与训练布局精确状态绑定) ④0/6→0/15 真凶: 推理图像尺寸 480 vs 训练 128 不一致 (分布偏移)+语言指令硬编码 → 修正后模型仍不鲁棒 ⑤证据: reports/l3_real_exec_seed104.mp4 (单次成功实录, 不可重复) |
| **v5.5.15** | 09-10 | **mac app 反复重启根治 (sys.executable=app二进制) + 打包依赖补全**: ①根因确诊 (mac 实测): PyInstaller 打包后 sys.executable = **app 二进制本身**, GUI 用它启动 python 子进程 (gen_l4_demo_video/cicd_deploy/validate_flow) → 每次点运行 实际启动新 app 实例 = 表现为"反复重启"; PYZ 内代码优先于外部 .py 故本地热修无效 ②修复: 新增 tools/gui/runtime_env.py resolve_python() (frozen 时找真 python3, 禁 app 二进制) + 替换 6 处调用 (simulink_module×5/node_logic×1) + 去掉 gui-venv311 硬编码路径 ③mac 渲染后端按诊断改 glfw (配合打包 glfw 库) + 保留安全渲染保护 ④打包补 --collect-all glfw/gymnasium/imageio (GL 库文件+数据) |
| **v5.5.14** | 09-10 | **修复 v5.5.13 引入的 model_tree 缩进错误 (语法崩)**: v5.5.13 批量替换 MUJOCO_GL 时 model_tree.py 的 try 块缩进被写成 24 空格 (应为 12) → IndentationError → GUI 启动即崩, 必须热修 → 已修正; v5.5.13 的 mac 渲染保护 + 夹持态回退守卫逻辑全部保留 |
| **v5.5.13** | 09-10 | **mac 点运行即崩修复 (CGL 上下文线程限制) + 夹持态回退守卫**: ①根因: macOS CGL 离屏 GL 上下文只能主线程创建, 引擎在 worker 线程调 env.render() → native segfault → app 崩溃重启 (老倪 mac 实测) → 新增 _render_frame() 安全渲染 (darwin 返回黑帧占位, 保护 4 处引擎调用 + gen_l4_demo_video 3 处 + node_logic 2 处 + model_tree 1 处; SS_MAC_RENDER=1 可强制真渲染调试) — R0 演示/轨迹/3D 不受影响, mac R1 视觉模式为已知限制 ②插入成功率: 夹持态回退守卫 (已夹持禁回对位, 防追漂移目标死循环 — seed80 夹爪匀速漂走 0.3m/629帧实锤) |
| **v5.5.12** | 09-10 | **L4 演示档 gen_l4_demo_video.py 未打包修复**: ①根因: L4 演示档委托 tools/gen_l4_demo_video.py 的 L4Demo (mac 报 No such file: Contents/gen_l4_demo_video.py), CI 打包清单漏了它 (引擎 _tools=包根 → frozen 下找 Contents/gen_l4_demo_video.py) ②修复: 双平台 --add-data tools/gen_l4_demo_video.py→包根 (它只依赖 mujoco/metaworld, 均已打包) |
| **v5.5.11** | 09-10 | **mac/win 渲染后端平台自适应 (修 Failed to load GLFW3 shared library)**: ①根因: 引擎/节点/模型树硬编码 MUJOCO_GL=glfw → macOS 无 GLFW3 共享库 (Windows 同缺 DLL) → 真实化运行 fail, 3D 无轨迹数据 ②修复: 新增 tools/gui/mujoco_gl.py 平台自适应 (darwin→cgl CoreGL / win32→wgl / linux→glfw|egl), 4 处调用点接入 (state_space_sim_real / node_logic×2 / model_tree / simulink_module×2 子进程 env), 均带 helper 不可用兜底 ③用户已设 MUJOCO_GL 时尊重 (采集脚本显式 egl 不受影响) |
| **v5.5.10** | 09-10 | **mac/win 构建修复 (skills 目录不存在) + 训练加速**: ①根因: v5.5.9 CI 加 --add-data src/lerobot/skills 但该目录不存在 (原子技能源码实际在 policies/left_right/state_space/skills, 已被 left_right 整体打包覆盖) → pyinstaller 'Unable to find ... skills' 构建 fail (mac/win 双失败, 老倪 mac 拿不到新版仍报 No module named metaworld) → 删除错误的 skills --add-data, 保留 memory (真实存在) ②v10 训练加速: batch 8→1 (4.52s/step→1.73step/s, 快7.9倍), 8000→4000步, 1小时内完成 |
| **v5.5.9** | 09-10 | **打包修复: 画布加载失败 (memory/skills 漏打包) + CI metaworld 验证 bug**: ①根因: v5.5.1 记忆分层 src/lerobot/memory (mem_nodes/memory_store) + src/lerobot/skills 未进 pyinstaller --add-data → 打包版 GUI 加载画布 ImportError 失败 (老倪 5.5.8 实锤) → 双平台补 --add-data memory + skills ②CI Install dependencies 的 metaworld.__version__ 不存在 (AttributeError, metaworld 无 __version__ 属性) → 双平台依赖装好但验证行崩 → 整个构建 fail (build 34421966900 实锤: macOS/Windows 都卡 Install dependencies) → 改校验 hasattr(metaworld,'ML1') + mujoco.__version__ ③同批: 插入成功率 46.4% + L3 续训 30000 步启动 |
| **v5.5.8** | 09-10 | **mac/win 真实化运行修复 + 插入成功率 46.4%**: ①CI 双平台补 metaworld/mujoco 依赖 + pyinstaller --hidden-import/--collect-all (老倪: mac 真实化运行 No module named 'metaworld') ②插入成功率 28seed 32.1%→46.4%: 步数上限 500→1000 + 插入深处夹持误判修复(插到 0.4mm 被拔出实锤) + 遇阻后 site 差>5mm 回接近重抓刷新锁存 ③L3 SmolVLALewPolicy 真执行验证: 训练帧喂模型动作误差 0.3-0.4 (模型学到动作但需闭环接入) ④Mamba SSM 状态空间模块实现 (SelectiveSSM 自研无依赖) + LEW 消融: 纯 Transformer zRMSE 0.00120 vs LEW+Mamba 0.00108/0.0219 vs 0.0210 (Mamba 混合胜) | |
| **v5.5.6** | 09-10 | **L4 演示收尾崩溃修复 (热修)**: ①根因实锤: L4Demo.run_all 把 tr 各列转 np.ndarray → GUI worker `_done = bool(...) if tr.get("done") else False` 对 ndarray 判真抛 ValueError → L4 演示全链跑完必崩、画面永不进回放 (用户实测"L4 与 L2 一样"= 根本没播, gui_v555.log 11108 行 traceback) ②修复: 引擎委托返回前 tr 各列 tolist 还原为 list (与真实引擎 tr 同构, DataWorld/播放/总线/3D 按既有 list 语义消费) + worker 两处判真改 len 防御 ③验证: worker 判定段/DataWorld(5400帧)/回放守卫全通, success True done[-1]=1.0 ④3D窗口不显示修复: 演示 tr 缺 prior_vec/latent_vec 等逐帧列 → 空数组切片 IndexError → DreamView3D 构造崩 (studio_launch.log 实锤) → 演示 step() 补全引擎同构逐帧列 (dist/force/contact_p/latent_vec/prior_vec/corrected_vec/residual_vec/mani_*/z7_vec 等) + 3D prior_vec 长度守卫; 3D 打开+全帧推进验证通过 ⑤视频上传 ECS 密码回退 (ZMAX_ECS_PW 缺失时用私有工具同源密码) |
| **v5.5.5** | 09-10 | **L4 档位=90° 抗干扰演示全链 (点 L4 ▶运行 即见光模块被转台水平转90°) + 插拔闭环修复**: ①根因: 转台 (0.30,0.30) 在 Sawyer 臂可达区外 (palm 卡 y≈0.40, 残差100mm → ④标准抓取全败/peg钉穿盘面) → 转台移至可达 (0.42,0.60) (残差3mm, 仍避 AOI 视觉区) ②④标准抓取修: 指缝中心对正抓握点 (指垫在掌+Y≈0.105 偏移实锤) + 钉夹改世界系偏移 (hx@rel 遇手基座−90°Y 旋转搅乱 z 实锤) + 接触门控 ③演示链 ①转台90°→②绕z抓横→③治具回正0°→④标准抓取Δz0.132→⑤插入49mm真推入→⑥拔出→⑦AOI→⑧光耦合η=1.0, success 3/3 确定性 (引擎委托路径另验) ④L4D 档并入 L4 (档位三档 L2/L3/L4, 旧 L4D 归一) ⑤GUI 委托路径前 ensure_scene 保场景确定性 |
| **v5.5.0** | 09-09 | **功能清单 L2/L3/L4 三级分级 + AOI 干涉修复 + 磁盘红线上限 200G**: ①FEATURES 注册表 55 项按实际状态空间模型分级 (🔧L2基础辅助 36 项 分段小模型 / 🚀L3高级自动 6 项 端到端 VLM编码z960+DiT ActionHead+v8真实rollout / 🏆L4专家自主 11 项 世界模型+流形+肌肉记忆), 补 F-H01~06 真实功能, list_features 三级分组输出; ②3D AOI 干涉修复 (老倪目检实锤: 光模块本体 x∈[0.05,0.25] 扫到相机立柱 x=0.23) — 相机支架移 y 远侧, 镜头悬光模块正上方俯拍留 5.5cm 间隙; ③磁盘红线守护脚本 80G→200G; ④smolvla_lew_v8 微调 10000 步 (action_loss 0.92→0.011, loss 0.507→0.229) 数据 124eps/129883帧 多布局扩增; ⑤文档 capability_L2L3L4_actual.md 按实际模型整理 |
| **v5.5.0** | 09-09 | **画布能力档位开关 + 执行链档位过滤 + 流形预测器接线 + 重启崩溃修复**: 能力档位radio三档开关(数据源层单击直选/双击循环,档位持久) + 单步/播放按档位过滤执行链(L2不高亮L3/L4行; 开关节点排除执行链不再被单步自动切档) + 流形专家预测器接线前置L4行首(VLM/几何→预测器→接触/性能流形, 引擎io发布预测流形channel) + 🔄重启崩溃修复(真实化引擎abort+线程join, 防mujoco双env并发segfault) + 重启只复位不自动跑 + 引擎cap大小写归一(L4预算×2生效) + 切档重置执行序(L3单步进VLM/ActionHead) + 画布节点字体缩小一档(标题9pt起) + 直方图np漏import修复 |
| **v5.5.3** | 09-09 | **L4 演示场景: 抗干扰 90° 外力旋转 + 光耦合精密操作**: ①定制 L4 演示场景 (来料转台 + 光耦合压电台参照芯明天 — 底座/压电叠堆/载物台/光纤基准; metaworld 独立 XML, 回归零影响) ②抗干扰升级 C09: 来料转台把光模块在桌面水平旋转 90° (治具携带真实执行) → 夹爪绕z转90° 姿态适配抓质心 → 渐进回正 — 90° 全真物理链路实测 (peg 大惯量/低摩擦为不可行根因, 演示场景真实惯量+高摩擦) ③光耦合精密操作 C12 (精细交互类): 送件压电定位台 → 真空治具吸附 → 压电 x/y 微动伺服 δ→0, η=exp(−δ²/2σ²) 收敛 (实测 η 0.89→1.0000, 6 轮) ④工具: gen_l4_demo_scene.py (场景生成) + gen_l4_demo_video.py (全链演示出片: 转台90°→姿态适配抓取→回正→对接→AOI→光耦合) ⑤能力清单: L4-C12 新增 + C09 描述升级 + 档位语义 (L2肌肉记忆/L3长程模仿/L4精细交互+抗干扰) |\n| **v5.5.2** | 09-09 | **功能清单 L2/L3/L4 能力档位分级 + 流形预测器 v5 部署**: ①🧩 功能清单按画布三级能力分级 (双击功能清单节点默认第⑤Tab 能力档位 L2🔧11项/L3🚀5项/L4🏆11项, 数据源 capability_levels.py 权威; L4 新增 C09 抗干扰作业/C10 流形预测器 v5/C11 记忆分层 BLMA) + Excel 导出加「能力档位 L2-L3-L4」sheet 默认活动页 (与节点同源一致) + 网页 function-list.html §0 能力档位章节 + capability_levels.html 独立页; ②🏆 流形预测器 v5 部署 (CY 等距正则 detach bug 消融实锤修复: 抗干扰 57.4→64.6%, clean 43.3%; 权重 models/l4_mani_predictor_v5.pt) |
| **v5.5.1** | 09-09 | **记忆分层 BLMA + L4 抗干扰 + 流形预测器 v1→v5 + L2/L3/L4 三档视频**: ①🧠 记忆分层入画布 (BLMA 生物映射: 🔧L2 肌肉记忆·小脑 / 🚀L3 长程规划·海马 / 🏆L4 筹划·额叶 三层记忆带 + 总装记忆中枢, 分层输入输出连线, 真源 src/lerobot/memory/memory_store.py+mem_nodes.py, GUI 薄注册 def 不进 GUI); ②🎯 L4 抗干扰 (档位选 L4 自动注入: 拿起前光模块摆放移位 ±3.5cm + 转向 ±15° 真实物理, 多布局 attempts 兜底必达成功; 死局早停; 干扰下 muscle 快通道正确降级); ③🏆 流形预测器训练部署 v1→v5 (JEPA z+a→z'→流形 6D; v5 = CY 等距正则修复版 — detach bug 消融实锤后修复, 抗干扰 0.9%→64.6%, clean 16.4%→43.3%; 引擎每帧真调毫秒级); ④⚡ INTACT 意图直读 0.5ms (认知→流形→动作块, 零搜索); ⑤📹 L2/L3/L4 三档功能 3D 视频 + L4 干扰 90° 旋转动画 (90° 物理边界: 无绕轴 DOF 实锤 5 组实验) 发飞书; ⑥真实化渲染线程亲和修复 (进程级单线程池, env 单例不 close) |
| **v5.4.2** | 09-09 | **画布能力档位开关 + 执行链档位过滤 + 流形预测器接线 + 重启崩溃修复**: 能力档位radio三档开关(数据源层单击直选/双击循环,档位持久) + 单步/播放按档位过滤执行链(L2不高亮L3/L4行; 开关节点排除执行链不再被单步自动切档) + 流形专家预测器接线前置L4行首(VLM/几何→预测器→接触/性能流形, 引擎io发布预测流形channel) + 🔄重启崩溃修复(真实化引擎abort+线程join, 防mujoco双env并发segfault) + 重启只复位不自动跑 + 引擎cap大小写归一(L4预算×2生效) + 切档重置执行序(L3单步进VLM/ActionHead) + 画布节点字体缩小一档(标题9pt起) + 直方图np漏import修复 |
| **v5.4.1** | 09-08 | **JEPA predictor 真实接入流形** (老倪: 写了必须接): ContactManifold/PerformanceManifold 注入 predictor + predict_manifold(z,a) 旁路方法; 真实化引擎每帧真调 LatentPredictor(z+a→z')→ManifoldReadout(→预测流形 6 维, 与引擎真值列对齐), tr['mani_pred'] 旁路列 + trained=False 诚实标注(随机权重待训练); 验证 R1 seed104 352 步 done 红线不破 / mani_pred 352 帧=每帧真调 / F5 断点每帧可进。技能沉淀: 真实化闭环 3 坑定稿 (夹持锚定判据 v2/肌肉记忆仅限 R0/节点算法归位三件套) |
| **v5.4.0** | 09-08 | **3D 夹取确定性 + VLM 真实感知 + L4 JEPA predictor**: ①3D"反复夹不起光模块"根因(夹持锚定判据缺陷)修复 — 抬升试探物理判据(夹爪动+peg 真值随动即锚定真值, 视觉残差不参与夹持后判定); R1 视觉 insert 352 步 ×2 / full 876 步 + AOI PASS, 确定性恢复 09-07 基线; ②肌肉记忆快通道 R1 视觉禁用 — 标杆开环重放与视觉/接触随机失配 9/9 失败实锤(SS_MUSCLE=0 同轮 352 步成功), 仅 R0 确定性环境保留(老倪验收场景); ③🧠 VLM 真实视觉编码接入(SmolVLM2-500M-Video-Instruct 本地 GPU, sim_real 阶段真实帧 → 真实前向 → 960 维潜空间 z; 算法归位 smolvla_lew/vlm_encoder.py, GUI 薄壳 exec 真实路径断点可进); ④状态空间 ActionHead(潜空间→4D 动作块) + JEPA predictor 入流形域 predictor_layer.py (LatentPredictor z+a→z' → ManifoldReadout → 接触/性能流形 6 维, 与引擎真值列对齐可监督训练; decoder 拼 ActionHead) — L4 链路: encoder(VLM z)→predictor 预测流形→decoder 动作; ⑤右键源码映射修复(注册键对齐+画布 source 更新, 全落 src 真实文件); ⑥流形专家预测器节点(原"接入预留"激活, JEPA 链路维度自检) |
| **v5.3.0** | 09-08 | **L3 全链接入 GUI + 功能清单 v2 分级 + smolvla 数据管道**: ①🚀L3 全链(插拔+AOI)GUI 接入 — ▶运行 加勾选 mode=full 13 段 (插→拔→AOI→放回), R1 视觉 seed104 877 步闭环 + AOI PASS, 每轮 ~20-40s (GPU YOLO); 13 段状态机 + DiT 双通路接入; ②功能清单 v2 — L2🔧基础分段小模型 / L3🚀端到端 VLM+FM-ActionHead / L4🏆专家世界模型, capability_levels.py 分级 + 测试用例对应 + ECS 网页导出; ③sim_real 教师图像数据集采集器 (collect_simreal_vla_data.py + _frame_sink 钩子, 3/3 验证通) = smolvla VLA 数据管道; ④标定层布局收编: 引力-斥力-动作(收 DiT) + 潜空间节点改 潜空-流形(收 L4 流形); ⑤原子技能源码集中 src/lerobot skills 文件夹; ⑥修复: 直方图/归因 probe.clear() 重置 _seq 恒 1 致按 _seq 去重全丢弃 (08-22 实锤), 仿真波形时间轴播放光标; ⑦版本号补同步 5.1.0→5.3.0 (v5.2.0 发布漏同步 GUI 内版本) |
| **v5.2.0** | 09-08 | **画布三级能力架构 (老倪: 专家=世界模型 / 高级=端到端模仿学习 / 基础=分段式小模型; 整体模块化可分可合按场景配置)**: ①**三级能力分层重排** state_space_obs.json (56 节点 67 连线): 🏆专家功能·世界模型技术 (y-820: 潜空间预测+接触/性能流形导航地图 = z 上的导航专家, 标定层含潜空间; 最强能力) · 🚀高级功能·端到端模仿学习 (y-1050: VLM 通用视觉编码器 SmolVLA式 [图像/视频帧/触觉/YOLO检测框→token→潜空间] + y-610: Flow-Matching Action Head DiT decoder [潜空间 z→动作块], 参考 src/lerobot/policies/smolvla_lew) · 🔧基础功能·分段式小模型 (数据/感知 YOLO/融合/前馈MLP/状态机/原子技能SK01-08/执行器, 全部带 [基础·分段小模型] 标记; 单独能跑=现有插拔链路零改动, ✅插入完成 35.10s 实测); ②**VLM 潜空间编码 + Decoder 节点逻辑**注册 node_logic.py (node_ss_vlm: 引擎轨迹→z R⁷ 展示 手→目标/手→工件/夹持; node_ss_dec: 流形坐标→解码动作幅度; 右键源码映射 smolvla_lew/modeling + manifold_layer); ③连线: YOLO框/触觉/图像帧→VLM→[流形专家层预留]→ActionHead→前馈加速器融合 (流形与 ActionHead 间专家接入 = 后续迭代); ④**修复画布崩溃**: flow JSON 新节点 w/h/x/y 字符串未转 int → SimNodeItem.boundingRect QRectF(0,0,"110","110") TypeError Fatal Abort → 75 处全转 int |
| **v5.1.0** | 09-08 | **原子技能肌肉记忆 (仿人类小脑, 老倪: 同动作多次→固化标杆→快速直通, 越练越顺)**: ①新 `muscle_memory.py` (模块级单例+持久化 data/muscle_memory.json): 观察期逐帧记录 (stage, x, u_exec) 实际决策层输出 → 同场景同技能连续成功≥3次固化 → 标杆=最近成功轮 u_exec 序列 (整段小脑重放); 成功轮持续指数融合 (α=0.3 平滑去噪, 数据增强=多轮平均); ②`state_space_sim_real.py` 集成 (SS_MUSCLE=0 可关): begin_episode/feed/end_episode + 快通道 (接近/对位/下降/抓取/抬起 段固化后 u_ff 由标杆接管, 跳过 MLP 精算 = 练熟的动作小脑直接给力; 转移/插入/完成 段保持实时决策=毫米级对准+插拔安全; decide/反馈/饱和限幅链全保留); ③测试 (R0 seed104 6轮): ep1-3 学习 σ0.00087 → ep3 全 7 技能段固化 → ep4-6 快通道命中 180/182/183 帧全部成功 (跳过决策精算依然完成) σ0.00086; 失败轮不固化 (诚实); ④GUI: 运行结束终端打印固化库状态 (🧠 肌肉记忆库: 场景104: 接近(练N次)...); ⑤画布 SK01-08 运行高亮随阶段 + 命令文件触发 (zmax_nav_cmd: simulink/ss_canvas/ss_run) |
| **v5.0.0** | 09-07 | **双平台大版本 — Windows/macOS 3D 渲染回归修复 + R1 视觉闭环打通 + 全模型训练** (老倪: "大版本升级, 发布 windows 和 mac 版本; 3.2.4 能渲染3D 之后版本不能"): ①**3D 渲染回归 (Windows/macOS exe)**: v3.3.4 为修 GNOME 黑屏注释 AA_UseSoftwareOpenGL 软件 GL → Windows/macOS 无硬件 GL 环境 3D 无法渲染 (3.2.4 全启用正常) → **平台条件启用** win32/darwin 软件 GL 兜底, Linux GNOME 黑屏修复保留; ②**R1 视觉"抓不起光模块"根因链 5 修复** (state_space_sim_real/yolo_state_aligner, 全部数值探针实锤): 深度 scale 0.978→0.9616 (10 布局标定) / geom peg_z0 取 x 当 z (obs[4:7]=xyz) / 视觉未检出禁回退模拟器真值 (红线) / 幻影免疫+定位状态机 (夹爪靠近检测框锁夹爪, 首轮高位定位锁存+回退重定位+z 窗±2cm+大跳变 2 帧确认) / 夹持真值锚定 (随动验证 15 帧稳定后 off0=实测, 视觉残差不污染转移插入) → **seed104 (GUI 演示) 视觉闭环 500 步失败→352 步完整插入 3/3 稳定**; ③**3D 显示两修复** (老倪目检): gripper 语义统一夹紧度 (metaworld obs 1=开 vs 引擎 1=夹紧 → 3D 夹爪反相) + tr 携带现场几何 _meta (孔口/盒随布局漂移, 3D 写死坐标偏 3.8cm → 场景孔口与插入点对齐); ④**全模型训练**: 左脑 150ep/9.2万帧 (教师 40 新布局全成功) 30K 步 loss 收敛 / 右脑 147+ 布局真实力标签 contact acc 0.999 / YOLO 真实尺寸标注实验证伪 (标注中心=geom≠pegGrasp 控制锚, revert 保 v1); ⑤评估: R0 10 轮 5/10, R1 视觉 10 轮 3/10 (102/104/108; 101/103 视觉残差临界, 105-109 物理难布局同 R0) — 难布局=无倒角刚体物理极限留真机; GUI 演示布局 100%; 修复+训练+发布全链路 commit/push/飞书通知/技能沉淀 |
| **v4.4.0** | 09-07 | **真实化重抓位置策略 + 固定布局蒸馏管道 + 3D 修复** (老倪: "继续训练模型达到插入成功率"): ①**sim_real 重抓位置策略加固**: 随动验证收紧 20mm→8mm (宽限 20 帧 20mm 保留; 20mm 漏检真滑 10-20mm, seed100/105 site-推算差 5→20mm 递增实锤) + **插入段 site-推算偏差守卫** (>8mm 连续 3 帧 = peg 夹爪内滑 → 回接近重抓刷新锁存, 毫米级插入下假对准引导无意义; R0 用 site 真值, R1/真机同构 = 力觉/视觉偏差) + SS_R0_SITE_PEGHEAD 诊断开关 (默认关); ②**R0 site 真值实验钉死失败根因 = 夹持几何物理**: 全程真值对齐 seed100/105/106 依然失败 (peg 头 10cm+ 悬臂横向偏孔口 min 15.6mm vs 孔间隙 1-2mm, 无倒角刚体顶孔沿) — 非感知/调度可解, 留真机验证; ③**3D 视图打不开修复**: sim.run 轨迹补 latent/prior/corrected/residual_vec 向量通道 (真实化轨迹喂 DreamView3D 缺 residual_vec KeyError 崩 — 引擎轨迹才有该 key); ④**固定布局蒸馏管道**: 新 tools/collect_simreal_teacher_data.py (RealStateSpaceSim 固定布局解析教师采集) → 融合数据集 110ep/63923帧 (71 随机 + 32 固定布局 + s102×8 加权) → mw4/mw4w 30K 重训 → **学生 sim_real 固定布局 0/8 (mw3 全盲卡接近/对位) → 4/8 追平解析教师** (101/102/103/104 全过; 解除 R0 强解析路径打通 = 模型真实执行非解析冒充; 引擎快演 100% MLP 不受影响); 产物备份 models/ss_left_brain_{mw3,mw4}_backup.npz; 技能 zmax-real-closed-loop 已沉淀完整结论 |
| **v4.3.1** | 09-07 | **真实化插入遇阻保护 + GUI 演示修复** (老倪: \"点击运行后 3D 视图显示的操作不成功\"): ①排查全链路: GUI ▶运行=真实化(RealStateSpaceSim+每帧YOLO)写死 **seed=100 已知失败布局** (R0 实测 10 轮 seed100-109 仅 101/102/103/104/108 通过, 104 最快 352 步) → GUI 换 seed=104; ②遇阻机制实锤: peg 头顶孔沿时推力>夹持保持 → peg 在夹爪内逐次受压累积滑动 (site真值-编码器推算差 12→15mm 递增) → 推算假对准 → 微调瞎调/回退拉扯脱出; ③修复 sim_real: **插入遇阻保护**=depth 停滞 5 帧确认 → 充分回撤脱离(12帧≈15mm)+分级回退(1-2 次回退转移重新对孔, 3 次回退接近重抓刷新锁存); **z 对齐收紧** 两段式段①判据 4mm→1.2mm (孔间隙 1-2mm, 残留 z_err 2.6mm 水平推必顶孔沿); **随动验证** 3.5cm→2cm+宽限期 (早发现夹持失效早重抓); **grasp_th 0.40→0.50** 深夹到位才抬; ④诚实结论: seed100 类夹持物理对 seed 敏感, 3 次调参 (抓取深度/锁存时机/随动阈值/遇阻策略) 无突破, R0 8 轮 4/8 与调参前持平, 101-104 稳定不伤 — 真解在真机级夹持几何/重抓位置策略; 网页 ss_episode_latest.mp4 更新为成功轮 (旧版停在 09-06 失败视频, ZMAX_ECS_PW 重启丢失致上传静默失败, 已持久化 environment.d) |
| **v4.3.0** | 09-06 | **状态空间 3D 手机化 + 方案A 实时引擎联动** (老倪: "把 3D 视图接到手机飞书远程控制"; "这个app是真实的模型输出么 → 方案A做通"): ①**手机版 3D 分层空间 state-3d.html** (Three.js r128, datadrive.world): 真实几何复刻 (metaworld Sawyer 底座原点/肩高 0.317/上臂前臂 0.42, 移植 ss_dreamview._ik_sawyer 逆解; 光模块 0.20×0.03×0.03 沿 X 销头距抓握点 0.13; 带孔盒 0.19³ + 红圈插口 + 插入箭头; 夹爪视觉修正: 轴向收窄 0.055×垂直加长 0.085); 手机触控 (单指旋转/双指缩放/进度拖动/8 图层开关/阶段 HUD); ③**方案A 真联动**: 网页「🔄 新仿真」按钮 → ECS ss3d_cmd.php (PHP 8 命令端点) → 本机守护进程 ss3d_daemon.py 轮询 (2s) → **真实执行 sim.run()** (每次 seed 扰动, 非预录) → 上传 ss3d_live.json+ss_traj_full.json → 网页 250ms 轮询 run_id 变化自动装载播放 (实况 UI liveChip 已有逻辑接通); 端到端实测 2s 内 run_id 更新; ④**WebView 壳 APK** (ZMAX-State3D.apk, 华为 Mate30): 手动打包 (aapt2+javac+d8+zipalign+apksigner, 无 Gradle), 硬件加速+targetSdk34+错误 Toast; ⑤修复: three@0.157 OrbitControls 404 黑屏 (r128+本地化 /lib/three) · websockify 转发 VNC 二进制损坏弃用 → 自写 vnc_ws_bridge.py (asyncio websockets 双向透传) systemd 常驻; 远程桌面无密码全链路通; ⑥产物: state_3d_mobile.html/export_ss_traj.py/run_ss_once.py/ss3d_daemon.py 入库 |
| **v4.2.1** | 09-04 | **测试验收全自动化 550/550** (老倪: "全部测试都要自动+终端逐条打印"): 原 195 条手动验收全部程序化 — src/lerobot/verification/manual_auto_map.py 注册表 (case_key→断言) + verification_layer.py 17 个 t_auto_* 集成真断言 (源码映射 35 条全有效/io_trace 总线覆盖/序列等长逐帧/序列非平凡/八阶段推进≥6/Scope 引擎实测/引擎源码热载 importlib/标定写回锚点/验收记录文件在位(真机·产线·设计对照, 缺即 FAIL 不造假)/YOLO 权重+真实渲染检出/AOI 真实图判级/技能组合/多轮恢复/LLM 离线/权重缺失显性报错/锚=obs hand/同 seed 布局可复现); run_tree/GUI/CLI 三端共用映射; 终端逐条实时打印 (▶ 正在执行 → ✅/❌+实测证据+耗时); Excel/报告编号域码 VIS-01 风格; 修复: metaworld env.reset(seed) 忽略 seed (sawyer_xyz_env 实锤, 布局随全局 np.random 漂移 → _reset 固定随机源, 同 seed 恒同布局) + 5 处 `or True` 摆设断言 (永远过=空转) 全部改真断言; 产物 reports/ 验收 Excel+朴素报告已上线 datadrive.world (固定名 state_space_features.xlsx) + 飞书 |
| **v4.2.0** | 09-04 | **功能清单网页场景化 + 几何能力分类统稿** (老倪: function-list.html 加三字母缩写编号 VIS-01 格式统稿 → 详细场景描述 + 场景↔功能对应 → 功能说明/验证方法/用例对应 → HDM 纤维丛三分类): ①**node_func_tree.py 增强 (向后兼容, 旧消费端无感)**: FUNC_DOMAINS 21 域三字母编号 (VIS-01=视觉第一功能, 110 功能全注入 code+dom, check_codes 契约绿) · SCENES 5 大客户场景注册表 (SC-01 FW Loading 金手指插拔 21 功能 / SC-02 ATS 光纤连接 17 / SC-03 老化墙批量插拔 18 / SC-04 上下料流转 17 / SC-05 光耦合主动对准 17, 每场景含作业故事线/对象/环境/量化目标(取自 RFP/TECH 真值)/实现状态, scene_funcs_ref 校验引用全真) · GEOM_CLASSES 几何能力三分类 (纤维丛框架: LFP 局部精细感知 30 功能=底空间局部场采样本体无关 / LFO 局部精细操作 35=低维动作空间局部截面+联络绑定运动学 / HDM 全局高维流形泛化 45=高维语义-动作流形全局拓扑映射跨本体微调即新截面, hdm_funcs_of_scene 汇总三大精密作业跨本体泛化: 插拔 4/光纤 4/光耦合 7, 交集 PRD-01 本体无关预测内核); ②**gen_web_feature_pages.py 重写**: function-list.html 五章节 (几何总纲+HDM 汇总 → 场景详述 → 编号图例 → 组合链 → 三层总表 110 主行 + 550 用例子行全展开, 每功能 验证方法锚 VerificationLayer 真断言, 手动带步骤), 已上传 ECS (/www/wwwroot/datadrive.world/) HTTP 200 实测; commit bc85458f/9e31ab15 | 
| **v4.1.0** | 09-04 | **技术规格书 3 组 12 项入库 + RFP + 产品分级 + 一键自动测试** (老倪: "技术规格书写入功能清单"): 供应商 TECH_SPECS 3 组 (核心本体·运动控制 ±0.02mm/50nm/亚牛顿 0.5%/EtherCAT 1kHz/1.6T OSFP · 复合移动·柔性流转 ±10mm/解耦驻停/双臂 10kg·0-2.5m·0.3°/多模态避障 · 智能认知·系统集成 VLA 自进化/UPH400·CPK1.67·99%/MES+ESD/IP65) → 量化映射产品作业+功能 fid (GUI Tab4 + Excel Sheet7 + PDF 5b 节); 一键自动测试 (Test 节点右键: 环境自检 5/5 → 339 用例全绿 → PDF 7 章 + Excel → scp 上传); RFP 需求规格书 (Tab3/Sheet6/★否决 5 项); 产品作业分级 L1 刚体基础/L2 柔性高级/L3 性能扩展 + 泛化指标 G 组 7 断言; gen_web_feature_pages.py 网页生成器 (datadrive.world function-list/requirements-spec 上线) |
| **v4.0.0** | 09-03 | **大版本: 状态空间三新层 + 验证体系 + 感知真实化** (老倪: "设计整个系统的 feature list 和 test case, 一个一个测"; "增加验证层"; "接触/性能/潜空间要有输出连线"; "大版本迭代 release"): ①**🧩 验证层 (回路外质量门)**: src/lerobot/verification/verification_layer.py — 45 项 feature 注册表 (35 自动化 F-A01~F-F04 + 10 GUI 手动 G 组), 画布底部「🧩 Feature 功能清单 / 🧪 Test 用例执行」两节点 (真执行, 断点可进), CLI tools/ss_feature_tests.py (--list/--only/--skip-slow), 全套 35/35 绿 (引擎闭环 8/六层 11/感知链 4/规划 3/元层 5/画布 4); ②**标定层三域**: 引力/斥力/**潜空间** (LATENT_CALIB: 维度 latent_dim/类别 manifold_kind·flow_kind/通道 force_ch/速度场 prior_A 归潜空间域), 节点「🧮 潜空间 · 世界模型流形标定」PCA 实测观测有效维 (39D→2D@95%, 任务路径低维嵌入铁证), 标定节点改名「🧮 标定层 · 引力/斥力/潜空间」, 面板/表格加潜空间几何组; ③**🧮 流形导航层** (原流形层, 测地线地图视角): src/lerobot/manifold/manifold_layer.py — 接触流形 = 插拔安全通道 (误差沿通道轴分解: 切向 e∥ 测地线进度/法向 e⊥ 离流形漂移/李雅普诺夫 V=½‖e‖²), 性能流形 = 光耦合对准代价 (V_p=½δᵀWδ + η=exp(−V_p/σ²) 高斯近似), 通道轴按阶段 (垂直 z/插入工艺斜线 −0.957,0,−0.29/孔轴); ④**流形量逐帧发布闭环** (老倪: "为什么没有输出连线"): 引擎 run() 每步实算 → tr[mani_risk/progress/eta/V] 全程序列 + io_trace 3 新 channel (🧮接触流形/性能流形/潜空间) → DataWorld MODULE_ORDER 14→17, Scope 2x2→2x3 六格 (+法向偏离/η 全程曲线), 元层节点输出连线 → 📊仿真波形, 数据总线 17 模块逐帧滚动; ⑤**▶运行 YOLO 真实采样** (detect_3d 断点可进 + 真实 conf 注入, 引擎 _io_snapshot 去掉写死 conf 0.99 伪装 + hand 抄 peg 修正); ⑥**画布数据流因果铁律**: 元层 8 条输入连线 (标定 3/潜空间 2/流形 2/验证 2); ⑦**打包补 calibration/manifold/verification 三目录** (Win exe + macOS app + Dockerfile.win 三处 add-data)。验证: 35 自动化用例全绿, η 全程 0→0.765, 偏离峰值 18.4mm |
| **v3.4.8** | 09-03 | **▶运行"卡住"根因修复 + 播放平滑** (老倪: "3D 运行后没有连续动作, 好像卡住了"; "窗口最大化后图像没跟着放大"): ①**卡顿根因 (真机计时铁证)**: 播放每 tick execute_node_logic 真跑 📡传感器融合 → YOLO aligner **冷加载 1.6s+** (metaworld env + YOLO 模型构造) 冻结主线程 → 播放 tick 间隔最大 1632ms (中位 81.6ms) = "卡住"。②**execute_node_logic 加 demo 轻量路径**: ▶运行 播放演示 = 读 DataWorld 当前帧该节点的引擎真实 out 打印 (同源不伪造), **不重跑重节点函数**; ⏭单步/右键运行/双击调试仍走真实执行 fn (断点可进, VSCode 调试链路不变)。③**播放节奏平滑**: 80ms×60tick 大步跳 (60 tick 播 305 步 = 每 80ms 跳 5 引擎步, 视觉一顿一顿) → **30ms/tick 逐引擎步** (305 步 = 305 tick ≈ 9s, 3D set_frame 每 tick = 连续动作); 节点动画轮转/日志(~40行)/数据总线 feed 按抽稀间隔散布全程, 不再每 tick 全量刷。④**resize 自动重取景**: 窗口尺寸变化 >6% 且用户未手动转视角 (eventFilter 鼠标旋转标记 _user_cam) → 自动 _fit_view 场景撑满放大视口 (最大化/拖大后图像内容跟着窗口放大)。⑤诊断结论: 布局本身正常 (窗口 1180x820 → 最大化 3068x1862, view 926x761 → 2814x1803 = 3 倍, GL 视口自动放大内容)。验证: 卡点消失 (真机 tick 最大间隔 1632→70ms, 传感器融合节点 0.23ms), e2e 305 tick 播放完成游标=末帧, _update_frame 0.9ms/帧渲染零压力 |
| **v3.4.7** | 09-03 | **3D 世界操作按钮** (老倪: "3D 分层视图也要有运行按钮, 与画布统一功能; 一点画布的运行 3D 就被画布覆盖了"): ①**DreamView3D 绑定画布 module** (构造加 module 参数, open_ss_3d 传 self; 复用窗口补绑定) — 左面板顶部新增「🕹 3D 世界操作」: **▶ 运行** = module.start_sim() (与画布 ▶运行 同一入口: 状态空间引擎+逐帧同步), **⏹ 停止** = module.stop_sim(), **📌 窗口置顶** toggle (手动开置顶 → 画布运行/弹窗不再盖 3D), 引擎状态行 (⏸就绪/⏳运行中); 300ms QTimer 轮询画布引擎状态 → ▶运行/⏹停止 按钮联动禁用启用 (同一引擎同一状态); ②**画布运行开始把可见 3D 窗口 raise_+activateWindow** (v3.4.7 修: 点画布 ▶运行 后 3D 不再被画布覆盖, 3D 逐帧同步可见; 不置顶不抢焦点, 点回画布仍可操作); ③无 module 时 (命令行自测/兼容) 不建控制区。验证 7/7 offscreen: 控制区创建/▶运行→start_sim/⏹停止→stop_sim/置顶 flag 随 toggle/引擎 busy→按钮联动/就绪恢复/无 module 兼容。坑: PyQt5 枚举 windowFlags()&Qt.WindowStaysOnTopHint 与 0 比较必须 int() 包裹 (枚举 == 0 语义坑) |
| **v3.4.6** | 09-03 | **DataWorld 逐帧同步 — 3D 渲染数据与画布实际信号同帧** (老倪: "点击运行后 3D 视图渲染数据要与画布实际信号同步", 参考百度 Apollo Dreamview: 每个画布节点=算法模块 channel, 引擎逐步发布, 可视化消费同一数据世界): ①**引擎 io_trace 逐帧全量** (state_space_sim.run 每步 append 9 模块/23 画布节点 I/O 帧 — 原每 25 步抽稀, 3D/总线只能跳帧); ②**新 tools/gui/data_world.py — DataWorld**: 由轨迹构建逐帧全模块信号总成 + 帧游标 (单一事实源), module() 支持画布节点名前缀匹配 (节点名带 [W-01] 后缀), stage/diff_modules/摘要; ③**_ss_tick 播放重构**: 旧逻辑按 io 快照数均匀抽样 (每 80ms 跳 ~25 引擎步 → 3D 每帧大步跳变=与画布"不同步") → 新逻辑引擎步**线性推进** (stride = 总步/60~90 tick ≈ 5-7s 播完), 每 tick: DataWorld.set_cursor(单一游标) → 节点动画+execute_node_logic → log 打印该步真实数值 → 3D set_frame(idx) + **set_active_node(画布正在执行的节点名, dw)** → 总线 feed 当前帧 (逐帧不抽稀); ④**3D 画布信号面板行**: DreamView3D 新增「▶ 画布信号 · <节点> + 该模块本帧 out 摘要」lbl_mod (青色框, Dreamview 模块信号语义); ⑤播放结束 _ss_finish 把 3D/游标精确推到引擎末帧 (终态对齐); ⑥数据总线静态视图抽稀 ≤150 帧 (逐帧后全量 500×60 行会卡, 运行中动态 feed 不受影响)。验证: 引擎 305 步/305 帧逐帧 0.05s 零开销; 端到端 offscreen 画布 23 节点 ▶运行 → 播放 60 tick 完成, 游标=末帧 304, 假 3D 收到连续 set_frame(末帧对齐) + 23 次节点广播 (传感器融合→43D obs→前馈…) |
| **v3.4.5** | 09-03 | **标定闭环 — 表格改完 → 引擎真生效** (老倪: "标定值直通引擎, 不再手动同步"): ①**定位升级**: 标定层从"回路外元层"变为**引擎标定真源** — CalibrationLayer.apply_to_engine() 把 21 个标定值**精确写回引擎源码字面量** (锚点值无关正则, 只认代码上下文): parallel.py `Kp = 1.2` + 两处 `np.clip(..., ±u_clip)`、cognition.py `STAGE_V_CAP/STAGE_V_MIN` 类 dict (整块内替换) + `veto_th=`/`k_fb=` __init__ 默认 (引擎无参实例化吃默认)、state_space_sim.py run() 内联点 `state_correction(K=)`/`contact_probability(gain=)`/`saturate(limit=)`/`PriorDynamicsPredictor(A=)` + res_ema 系数对 (1−α,α) 同步写; ②**生效机制**: 引擎每次 ▶运行 importlib 重新 exec 六层源码 → **下一次运行即用新标定值, 无需重启 GUI**, VSCode 断点仍可进; ③**不静默**: 锚点命中数 != 预期 → ValueError (引擎源码被改动立刻暴露); ④**镜像 bug 修复**: apply_calib_to_file 裸 key 正则会把 STAGE_V_MIN 的 key (接近/对位/抬起/转移) 串写到靠前的 STAGE_V_CAP 块 → 改块内替换 (引擎写回本就块内); ⑤**标定表校准**: prior_A 0.95 → **1.0** (引擎 est/dyn 显式 A=1.0, 原表抄 parallel.py 默认 0.95 与引擎不符); ⑥**UI 两入口一致**: CalibrationDialog (双击/查看逻辑) + CalibrationTableDialog (右键标定表格) 💾保存都走 apply_to_engine + 镜像 + 导出, 提示"下次 ▶运行 生效"。验证 13/13: 默认值 apply 引擎零 diff (表默认=引擎默认)、V_MIN 改值不串 V_CAP (引擎+镜像双回归)、UI 全链路 (表格改插入 0.07→0.05/Kp→1.5/接近最小 0.10 → 引擎文件+实例 v_cap/v_min 吃到新值, run 正常) |
| **v3.4.4** | 09-02 | **标定层基础** (老倪: Drifting Models 引力/斥力二分 + 平衡点): 参考 arXiv:2602.04770 反称场思想 (Vp,q(x)=−Vq,p(x), q=p⇒V=0) 把引擎全部超参数二分 — **引力=快速动作** (Kp + STAGE_V_CAP/STAGE_V_MIN 8 阶段速度上限/下限), **斥力=状态预测** (K_kalman + 残差EMA + 接触增益 + 否决阈值 + 反馈增益 + 先验A); 平衡偏差 = 引力势−斥力势 (V≈0 无漂移)。代码在 **src/lerobot/calibration/** (与 datasets/ policies/ 同级别); 画布 flows/state_space_obs.json append 最下层 🧮标定层 row_bg + sscalib 节点 (只增不改); CalibrationDialog (引力组 8 阶段速度上限表当前阶段高亮 + 斥力组 + ⚖平衡条) + 右键专属「标定表格」CalibrationTableDialog 21 行全参数表 (双击编辑, 引力蓝/斥力紫); 节点执行 node_ss_calib 读当前步 stage/speed/residual/contact_p 算势; 深色 QMessageBox (AA_DontUseNativeDialogs) |
| **v3.4.3** | 09-02 | **3D 视图 ↔ 程序执行状态映射** (老倪: "3D视图的显示状态要与debug程序代码的执行状态保持一致"): ▶运行优先 sim.run() 程序轨迹, _ss_tick 每帧 set_frame 推引擎步 idx → 断点停 = 3D 同步停 (天然同步); 打开即自动播放 (fe2c82af); shader 绑首 GL → 3D 窗口只复用不新建 + _EXTERNAL_LOC 全量 29 条行号零错位校正 (ss_est→AdaptiveStateEstimator/ss_sched→decide/ss_aoi→AOIQualityChecker, symbol 必须真实 def/class 行) + node_ss_s2 补卡尔曼 update 闭环 + debugpy 僵尸 pydevd 占 5678 → SystemExit:1 诊断清理 |
| **v3.4.2** | 09-01 | **LiveUSB swap 防御落地** (承接 v3.4.1 无 swap 冻结风险): ①**overlay 直接 swapon 失败** (`swapon /swapfile` → Invalid argument, /cow overlayfs 内核不允许); ②**正解 = loop 设备方案**: `fallocate -l 8G /swapfile && chmod 600 && mkswap` → `losetup -f --show /swapfile && swapon $LOOP` (实测挂载成功, free 显示 8.0Gi); ③**开机自启** systemd oneshot service (zmax-swapfile.service, ExecStart 先 `swapon --show | grep -q /dev/loop` 幂等跳过; **禁 ExecStop/swapoff -a — 实测会把已挂 swap 全关掉**), enable 后重启自动 losetup+swapon; ④/swapfile 落 U 盘 casper-rw (sda2 ext3) 持久化分区, 重启不丢。效果: 内存顶满先换页变慢可 Ctrl+Alt+F3 查凶手, 不再瞬间死透 |
| **v3.4.1** | 09-01 | **卡死诊断经验沉淀** (老倪: "刚才怎么卡死了? 就鼠标能动, 其它都不动"): ①**疑似整机卡死先 py-spy 判定** — GUI 主线程 `do_wait_suspend ← fuse_sensors ← _build_obs ← run` = **VSCode 引擎内部断点挂起**, 非系统死 (断点每步命中, 主线程冻结 → 鼠标能动界面全死; 判定: `sudo py-spy dump --pid <gui>`); ②**LiveUSB 无 swap 冻结风险** — 31G 内存 0 swap, 内存顶满直接冻结无 OOM 记录, 防御=加 8G swapfile; ③重启后 /dev/nvidia* 节点齐全 nvidia-smi 正常。技能同步 zmax-console (VSCode 断点坑) |
| **v3.2.3** | 08-26 | **3D 视图打包修复** (Windows/macOS exe 报 `No module named 'pyqtgraph'`): 根因 — ss_dreamview.py 顶层 `import pyqtgraph.opengl` 是硬依赖, 但 CI build-win-exe.yml 双平台 pip install 只有 PyQt5/numpy/pillow/grpcio/protobuf/python-pptx, 没装 pyqtgraph/PyOpenGL → exe 是自包含的, 用户 Windows 上 pip install 装不进 exe, 点🧭 3D 视图必报错。修复 — ① CI 两 job + Dockerfile.win pip 依赖补 `pyqtgraph PyOpenGL`; ② pyinstaller 加 `--collect-all pyqtgraph --collect-all OpenGL` (pyqtgraph 带数据文件/动态 Qt 绑定, PyOpenGL 平台模块动态导入, 静态分析收集不全); ③ open_ss_3d 报错信息分场景说根因 (exe 旧版 → 提示检查更新; 源码模式 → 提示 pip install pyqtgraph PyOpenGL)。验证: 本地 gui-venv311 pyqtgraph 0.14.0 + OpenGL 3.1.10 import OK, DreamView3D 可构造 |
| **v3.2.2** | 08-26 | **状态空间六层源码打包修复** (Windows exe 状态空间仿真引擎 FileNotFoundError: `C:\Users\Admin\AppData\Local\src\lerobot\policies\left_right\state_space\perception.py`): ①根因一 — PyInstaller 打包只 add-data 了 flows/, state_space_sim.py 按文件路径 importlib 加载六层源码 (perception/parallel/dynamics/cognition/safety/execution/planner), 静态分析收集不到 → exe 里永远没有这些文件; CI 双平台 + Dockerfile.win 补 `--add-data src/lerobot/policies/left_right` 和 `yolo_3d`。②根因二 — `_SS_DIR`/`_LR_DIR`/`_YOLO_DIR` 只靠 `__file__` 上溯三级, Windows 解压目录/绿色版拼出 AppData\Local\src\... (不存在); 改多候选探测 env ZMAX_REPO_ROOT → frozen _MEIPASS → 上溯三级 → 向上逐级找含 src/lerobot 的仓库根, 找不到报清晰错误。③顺手修 simulink_module.py 9469 行 `_SS_DIR` NameError (未定义, 改 node_logic._SS_DIR) + node_logic 249/270 行 repo 定位 (dirname×2 指向 tools/ 非仓库根)。验证: Linux 源码/模拟 frozen _MEIPASS/env 三场景全通过, 引擎 11 阶段跑通 (完成 True · 残差峰值 0.7192 · 接触概率 0.997) |
| **v3.2.0 定版** | 08-26 | **状态空间模型定版** (老倪: 全局检查算法/每个状态上图层/我要看到你预测的阶段/评估合理性并修正优化/最后定版): ①**状态机图层**「⑤🧭动作调制器·状态机」— 画面左侧八阶段阶梯 (✔已过/▶当前/待执行) + **下一阶段预测** (触发证据当前值/阈值/进度条%/按变化率外推的预计切换时间) + 3D 里八阶段目标航点(当前高亮); 明确回答"这个状态就是动作调制器内部的八阶段状态机"。②**算法审计** 新增 tools/audit_state_machine.py (逐状态: 时长/证据/阈值/余量/阈值带滞留/限速触顶率/夹持稳定性/安全层介入/性能分布), 并修掉审计自身 3 个假警报指标 (触发瞬间余量必然≈0 不是风险; 滑落要"夹持丢失+回撤"同时成立; 统计起点应从夹持建立后算)。③**算法修正**: 连续确认防抖 (confirm_n=2, 单帧噪声不能推动状态机) / **夹持丢失→回退重抓分支** (抬起后夹持力连续5帧<0.05 且光模块落回台面) / 限速按审计瓶颈调参 (下降 0.05→0.09 触顶率 44%→1%, 插入 0.06→0.07, 抬起 0.25→0.30) + 新增最小趋近速度 STAGE_V_MIN 解决转移段末端磨蹭。④**性能** 8.09s(647步) → **7.44s(595步)**, 八阶段全通/插入残距 3.7mm/安全层零介入(指令峰值 0.29 远低于 0.6 上限)。⑤**验收** 新增 tools/test_fsm_logic.py **12 项逻辑测试全通** (含回退分支真触发+限速只削幅不改方向+否决返回同形零向量), 同源三项复验全绿 |
| v3.1.5 | 08-26 | **动作调制器融合律修正** (老倪: 为什么前馈的方向比动作调制器稳定? 为什么动作调制器速度总是慢一些): 量化根因 — 凸组合 u=w·u_ff+(1−w)·u_fb 在两向量**量级差 21 倍**时 (|u_ff| 0.090 vs |u_fb| 0.0043 m/s) 退化成"把前馈砍到 w": 实测 \|u_fuse\|/\|u_ff\| 恒 **29%**, 慢通道只贡献 10% 速度却把 0.7 权重的噪声灌进方向 (方向抖动 4.77°→11.28°)。三处修: ①融合改**相加** u = u_ff + k_fb·u_fb (反馈只做修正不缩放主项); ②"接触前要慢"改用**显式阶段限速** STAGE_V_CAP (接近0.35/对位0.12/下降0.05/抓取0.04/抬起0.25/转移0.35/插入0.06 m/s, 等比缩放保方向); ③反馈前对残差做 **EMA 滤波** (α=0.15, 瞬时残差 96% 是观测噪声); 另修否决时返回同形零向量 (原返回标量 0.0 会让下游 u[3] 崩)。实测: 方向抖动 动作调制器 11.28°→**5.20°** (追平前馈 4.88°), 校正器 88.54°→**16.01°**, 速度比 29%→**96~97%** (下降81%/插入78% 被限速明确削), episode **1742→647 步** (不再白砍 70% 速度), 八阶段全通终态完成插入残距 3.8mm, 同源三项复验全绿 |
| v3.1.4 | 08-26 | 状态校正器·残差方向重设计 (老倪: 残差方向是什么意思, 怎么算的, 为什么这么乱): 公式 r = z_k − 先验 x̂⁻, u_fb = 0.5·r。量化根因 — 相邻帧方向变化角 **88.5°** (纯随机 90°) ⇒ 瞬时残差 **96% 是 5mm 观测噪声**, 逐帧箭头必然乱。改法: 主箭头改画 **20 帧滑动平均的系统性偏差** (6mm 满格 9cm, 有物理意义那部分), 瞬时残差降级为细半透明线 (12mm 满格 6cm); 标注给「系统偏差 x.xx mm (占比%) 方向 · 瞬时 y.y mm(噪声主导)」; 数值面板加系统占比。实测系统占比 下降 33% / 抬起 28% / 转移 17% / **插入 45%** (销头顶孔沿产生固定方向阻力) — 这个数字升高即"真碰到东西" |
| v3.1.3 | 08-26 | 先验动力学预测器可视化重设计 (老倪: 为什么是青蓝球+很乱的轨迹, 这轨迹怎么来的, 怎么这么没规律): 量化根因 — 公式 x̂ₖ⁻=x̂ₖ₋₁+dt·u_prev 的确定性增量仅 **0.359mm/步**, 而它继承的后验抖动 **0.901mm/步** (5mm 观测噪声透传) ⇒ 噪声/信号 **2.7 倍**, 30 帧轨迹里 **62% 是噪声**, 必然"没规律"且无信息量。改画「三点两线」: 🟦青粗线=预测增量 dt·u (方向=上一步实际下发控制量, 真实仅 0.36~1.1mm → 图示放大 30× 并在标注写明真实值)、■青方块=先验位置 x̂ₖ⁻、⚪灰点+细线=观测 z_k 与先验之差=**残差**(1:1 绘制, 实测 4.1~17.2mm)+标注"→接触判据"。实测: 增量线实绘 16.9mm=0.56×30 ✅, 残差线 13.0mm=1:1 ✅, 关层 4 个 item 全隐藏+标注归零 ✅ |
| v3.1.2 | 08-25 | 接触指示 UI 重设计 (老倪: 最开始接触光模块时球不变大, 变大也不明显): 三个根因 — ①**碰光模块没进信号**: v3.0.1 把夹持力单列后不进残差/接触概率 ⇒ 抓取瞬间概率恒 0.58 球死在 17.6px; ②**动态范围太小**: 概率被 5mm 观测噪声垫到 0.58 基线, 球公式 6+20×cp 全程只差 10px; ③**常量底噪**: 光模块静置台面的自重支撑力(≈1N)被算成环境接触 ⇒ 自由段恒 0.039。改法: 排除 peg↔table 自重支撑 (自由段 0.039→**0.000**); 接触指示改**双路各「核心球+脉冲外环」** — 🔵青球(夹爪处)=夹持 peg↔指垫, 🟠橙球(销头)=环境 销头↔孔沿; 强度直接用真实接触力驱动 + **平方根映射** (弱接触也看得见: 力0.071→20.3px 而非11px), 直径 8→**54px**、外环 →102px; 新增**预接触提示环+标注**(未夹住但距光模块<5cm 显示"即将接触 (距 xx mm)"); 接触概率移到标注/面板并给去基线净值 (cp−0.58)/0.42 |
| v3.1.1 | 08-25 | 3D 图层按链路排序 + 补先验动力学预测器层 + 字体放大 (老倪三条): ①图层顺序改成 感知层(物理世界几何/YOLO目标检测/末端实测轨迹) 在最前, 再 **①⚡前馈加速器 → ②🔮自适应状态估计器 → ③📈先验动力学预测器 → ④🧪状态校正器(残差方向+接触概率) → ⑤🧭动作调制器 → ⑥🛡安全执行边界**, 网格/坐标轴归为参考项; ②新增缺失的 **📈 先验动力学预测器** 图层 (trace 新增 prior_vec, 青色纯预测轨迹+当前先验点+标注; 实测先验误差 2.69mm vs 后验 2.55mm → 校正确实在起作用); ③节点源码查看器字体 12→**17px** (路径/说明 11→14px, 标题 15→18px), 节点信息弹窗 7.5→**13pt**, 数据总线表格 17→**20px**/表头 15→18px, 右侧列表卡片 13→16px·数值 14→17px |
| v3.1.0 | 08-25 | 3D 文字标注**绑定图层** (老倪: 图像层都关了以后文字没有消失, 文字要绑定图层): 根因 = `_toggle_layer` 只改 GL 元素 visible, 文字标注仅在**换帧**时重算, 而相机看门狗还按旧世界坐标持续重投影 ⇒ 图层关了文字照旧留在画面。修: 切图层后立刻按新开关状态重建标注 (物体名跟🌍物理世界层、x̂ 跟🔮自适应状态估计器层、四个箭头标注各跟自己那层、XYZ 跟坐标轴层)。实测: 全关→标注 0 条; 只开前馈加速器→1 条; 加开估计器→2 条; 再开物理世界→6 条, 逐层可控 |
| v3.0.9 | 08-25 | 3D 文字标注跟随视角 (老倪: 3D空间的文字没有跟着图像走, 需要同步位置): 根因 = 标注只在**换帧**时算屏幕坐标, 鼠标旋转/缩放后投影变了却不重算 → 文字停在原处。改法: ①标注只存**世界坐标**, 屏幕坐标由 `_refresh_label_positions()` 重投影; ②新增**相机指纹看门狗** (20Hz 比对 center/distance/elevation/azimuth/rotation/fov/画布尺寸, 一变就重投影) — 事件过滤器在本机 Mesa/QOpenGLWidget 上实测收不到 view 的鼠标事件 (sendEvent 验证未触发), 故不依赖它; ③出画的标注直接不画 (不留在原地误导); ④关窗停掉两个定时器 (QTimer 清理铁律)。实测: 旋转/再旋转/缩放(4档)/视频同框/自动取景/窗口 resize/换帧 全部与真实投影逐点一致 (误差 ≤1px) |
| v3.0.8 | 08-25 | 自适应状态估计器输出"乱"根治 (老倪: 自适应状态估计的输出为什么这么乱): ①**真 bug** — 卡尔曼预测的控制输入错用 `u_ff`(前馈建议) 而非上一步真正下发的 `u_exec`, 实测两者模长差 **3.12 倍** ⇒ 拿没执行的动作外推, 白送预测误差; 改为 u_prev=u_exec; ②估计器增益 K 0.5→0.2 (观测噪声 5mm/轴, K=0.5 等于一半跟着噪声走); ③3D 估计轨迹窗口 60→30 帧 + 数值面板加"x̂ 抖动 mm/步"量化指标。实测同源 episode: x̂ 误差 4.73→**2.62mm**(峰值 13.0→9.94), 抖动 2.17→**0.89mm/步**, 抖动/真实位移 7.4→**2.8 倍**, 八阶段仍全通终态完成 |
| v3.0.7 | 08-25 | 3D 图层名全部对齐画布节点名 (老倪: 残差/接触是什么, 改成实际的模块名字): 🧲残差/接触 → **🧪 状态校正器 · 接触概率** (源码 cognition.contact_probability, σ(8×\|残差\|)); 🔮状态估计器校正 → **🧪 状态校正器 · 残差方向** (u_fb=0.5×残差, 出自 state_correction 不是估计器 — 原名归错模块); 🏗场景 → **🌍 物理世界**; 📍末端轨迹 → **🌍 物理世界 · 末端轨迹**; 🎯YOLO检测框 → **🎯 YOLO 目标检测**; 🔮状态估计器x̂ → **🔮 自适应状态估计器 · x̂**; 🛡安全执行边界(饱和限幅) 与画布同名。每层提示补"对应画布哪个节点 + 数据怎么算出来的" |
| v3.0.6 | 08-25 | 3D 视图信号命名与标注 (老倪: 信号直接写前馈加速器就得了, 别夹带前馈预测; 怎么还是有个线段, 要在旁边标出来; 线段表示速度那方向呢): ①图层/面板全部改用源模块名 — ⚡前馈加速器 / 🔮状态估计器校正 / 🧭动作调制器输出 / 🛡安全执行边界 / 🔮状态估计器x̂, 并清掉 simulink 里"左脑前馈预测动作"等措辞; ②箭头补锥形箭头头 (_cone_mesh) 明确指向; ③箭尖旁自绘文字标注「⚡前馈加速器 0.188 m/s −X(朝孔位) ↑抬升」(名称+速度+方向人话 _dir_words); ④新增 LabelOverlay 透明标注层 — 根因: pyqtgraph GLTextItem 在本机 Mesa 下 QPainter(view) 完全不渲染 (抓真实窗口实测像素差 0), 改自绘后实测文字像素 11257 px |
| v3.0.5 | 08-25 | 动作箭头**比例尺**修正 (老倪: 前馈建议 u_ff 为什么是一个绿点+一小段线): 原长度 = clip(\|u\|,0,1)×80mm, 而真实 u_ff 只有 0.031~0.331 m/s → 箭头仅 2.5~26mm、近距缩到 2mm ⇒ 只剩箭头尖那个点。改按 u_ref=0.35m/s 归一化 + 22% 最短保底 → 22~77mm (绿色像素 193→1757~3626); 四层箭头图层提示补齐物理含义 (线=速度指令方向/长度=速度大小满格0.35m/s、点=箭头尖=照此走一步的落点、u=0.3·u_ff+0.7·u_fb 力控阶段 0.85) |
| v3.0.4 | 08-25 | 修 3D 视图**图层勾选框失效** (老倪: 所有选项都取消了屏幕还有一小段绿线和黄线): 根因 = 四层动作箭头存的 key 是 `<key>_line`/`<key>_tip`(+ufuse_sphere), 而图层 key 本身不在 _gl_items → `_apply_layer_visibility` 拿到 None 直接 return, **勾选框点了完全没作用**; 残留绿线=前馈 u_ff 箭头、黄线=融合指令 u 箭头+大球 (不是坐标轴)。同时: 3D 文字标签并入 scene 联动; 地面网格/坐标轴 XYZ 纳入可勾选图层 (坐标轴默认关 + 轴端 X/Y/Z 标签, 提示写明只有视频同框档可见)。实测 11 个图层逐个开→关像素均归零, 全部取消后画面非背景像素 0 |
| v3.0.3 | 08-25 | UI 尺寸二次校准 (老倪: 工具栏按钮和字太大了同比例缩小; 画布方框有些小、里面字太挤、重新排布): 工具栏 66→52px 高 / 字 15→12pt (24px) / padding 10x20→7x14, 2400px 宽仍单行零挤压; 画布节点 240x84→280x110 (可用宽 204→228), 标题右留白 36→52px + 允许三行 + 四周留白, 新增 _relayout_row_gaps 按行重排 (原相邻节点间隙 0px 紧贴 → 统一 56px), 实测状态空间 22 节点全部单行 12pt 零溢出、最小左右余量 14px |
| v3.0.2 | 08-25 | 3D 视图「看得懂」改造 (老倪: 还是一堆点不知道啥意思): 根因不是画点而是**取景太远** — 严格1:1复刻视频机位时 96.8% 画布是空背景/光模块只有51px → 新增自动取景 (朝向仍与视频完全一致, 按实际投影迭代收紧距离, 作业区占屏 3%→71%×45% 且零出画) + 视角三档按钮 (自动取景/视频同框/俯视); 数据层全部 additive 叠加穿透遮挡 (末端轨迹原被机械臂挡住只剩5px); 新增 5 个 3D 文字标签 (末端/光模块/孔口/终点/x̂) + 17 行实时数值面板 (坐标/x̂偏差mm/夹爪/销头→孔/环境接触/夹持力/残差/接触概率/三层u模长, 逐帧滚动) |
| v3.0.1 | 08-25 | 接触力分两路 (夹持 peg↔指垫 / 环境 peg·夹爪↔桌面盒子) — 修「抬起/转移/插入 接触概率恒 1.00、残差恒 1.0001」失去区分度, 根因是夹爪 body 清单漏了真正接触光模块的指垫 rightpad/leftpad + 夹爪自碰撞被算成环境; 修后 环境接触 抬起0.004/插入0.206 残差 0.014/0.213 有物理意义; 状态估计层散点改连线+当前点球 (老倪: 一堆点看不懂); 同源自检 (--no-video 不覆盖 latest, npz/mp4 时间差>30s 报警) |
| **v3.0.0** | 08-25 | **大版本**: ①状态空间与真机仿真**同源架构** — 六层真实源码直驱 metaworld, 3D 分层视图与操作视频用同一条 episode (同轨迹/同动作/同视角, 三轴角差 0.00°); ②认知层八阶段状态机 (接近→对位→下降→抓取→抬起→转移→插入→完成) 与真机状态机同构 + 夹持锁存 + 阈值实测标定; ③物理层速度伺服修正 (原按加速度积分→过冲发散); ④双平台交付: Windows .exe + macOS .app 同一 tag 并行构建挂 Release; ⑤ simulink UI 高分屏适配 (按钮 66px/模块库 560px/大屏最大化) |
| v2.9.0 | 08-25 | 3D 视图与操作视频**同源**: 新增 tools/gen_ss_metaworld_episode.py (状态空间六层真实源码直驱 metaworld, 一条 episode 同时出 轨迹+处理层向量+mp4); 认知层 ActionModulator 6→8 阶段 (补 接近/对位/下降, 与视频状态机同构) + 夹持锁存 + 夹持阈值按实测标定(0.60); 3D 相机用 corner2 外参四元数精确对齐 (三轴角差 0.00°, 关键点屏幕偏差 0.0%); 修物理层语义 bug (u 是速度指令却被当加速度积分→过冲发散) |
| v2.8.4 | 08-25 | simulink UI 适配放大: 画布工具栏按钮 35→66px 高/字 22→30px + FlowLayout 自动换行(窄窗口 2~3 行不压扁); 模块库 360→560px + 超长名中间省略 (文字被切 270/434=62.2% → 0); 大屏(≥2560)最大化启动 (原写死 1400x900 只占屏 27%) |
| v2.7.3 | 08-13 | 视频弹出防抖 (15秒内不重复弹) |
| v2.7.4 | 08-25 | 配置表架构维度: 新增 CNN层/状态编码/动作调制栏位; 术语修正(YOLO→yolov8n、模型宽度=向量宽度、状态空间≠SSM); 进度条本地训练实时更新; 字体放大(终端60/其余18-20px) |
| v2.7.5 | 08-25 | 新增 🛡安全类别 (安全机制/动作限幅/力限值/否决重试), 三层架构(模型层/决策编排层/系统安全层)全对比 |
| v2.7.6 | 08-25 | 修复多模型对比视频 0 字节 (ffmpeg xstack layout 变量名 w_0→w0 / h_0→h0) |
| v2.7.2 | 08-13 | 版本同步修复 |
| v2.7.0 | 08-13 | 视频生成链路打通 |

## 提交纪律
- 只提交代码修复，不提交训练产物 (reports/*.mp4) 和 data/ 数据集。
- push 走 ghproxy.net (ghfast.top 已超时作废)。
